# CONTEXT.md — Project 2.0

> **Leia este arquivo após o SYSTEM.SPEC.md e CONVENTIONS.md.**
> Ele reflete o estado atual do projeto. Ao finalizar uma sessão, atualize as seções
> correspondentes antes de encerrar — a próxima sessão depende dessa informação.

---

## Última Atualização

**Data:** 2026-06-30
**Task concluída (esta sessão):** 81-validacao-caracteres-proibidos-nomes — **shared + backend + frontend + docs** (sem migration/DB). Passa a **rejeitar** os caracteres `` '  "  `  ~  ^  \  ´ `` (aspas simples/duplas, crase, til, circunflexo, barra invertida, acento agudo) nos **nomes** de projeto/demanda/atividade/tag e na **descrição de execução** — **higiene de dados** (não é defesa contra SQL injection, que segue sendo os parâmetros nomeados). Autoritativo no backend (`class-validator`), espelhado no frontend (Reactive Forms). **Shared (fonte única):** novo `shared/src/validators/caracteres-proibidos.validator.ts` exportando `CARACTERES_PROIBIDOS`, `REGEX_SEM_CARACTERES_PROIBIDOS` (`` /^[^'"`~^\\´]*$/ ``) e `MENSAGEM_CARACTERES_PROIBIDOS` — **constantes puras, sem `class-validator`** (o frontend bundla o TS-fonte do shared); barrel novo `validators/index.ts` + re-export em `shared/src/index.ts` (antes de `./dtos/index`). Pasta `validators/` em **inglês** (conceito arquitetural, §16 linguagem); identificadores do conteúdo em português de negócio. **Backend:** `@Matches(REGEX_SEM_CARACTERES_PROIBIDOS, { message: MENSAGEM_CARACTERES_PROIBIDOS })` **acrescentado** (decorators existentes preservados) a **12 DTOs de entrada** — `ProjetoCriarDto`/`ProjetoAlterarDto`, `DemandaCriarDto`/`DemandaAlterarDto`, `AtividadeCriarDto`/`AtividadeAlterarDto`, `TagCriarDto`/`TagAlterarDto` (campo `nome`) e `ExecucaoIniciarDto`/`ExecucaoEncerrarDto`/`ExecucaoRegistrarDto`/`ExecucaoAlterarDto` (campo `descricao`). Import **relativo** `../../validators/caracteres-proibidos.validator` (como os enums do shared); `Matches` acrescentado ao import de `class-validator` onde faltava; ordem preserva `@IsOptional` antes e `@Matches` por último. DTOs **internos** (`*InternoAlterarDto`) e de **saída** intocados. **Frontend:** `Validators.pattern(REGEX_SEM_CARACTERES_PROIBIDOS)` no control + mensagem `MENSAGEM_CARACTERES_PROIBIDOS` exibida quando `errors?.['pattern']` (exposta via propriedade pública `mensagemCaracteresProibidos` — evita hardcode da string com crase/barra no template) em **9 componentes / 11 controles**: `projeto-formulario-dialog` e `projeto-detalhe` (nome); `demanda-formulario-dialog` e `demanda-edicao-dialog` (nome — `demanda-detalhe-dialog` **não** edita nome, só descrições da demanda (fora de escopo), então não tocado, conforme o "(se editar nome)" da spec); `atividade-formulario`, `atividade-listagem` (nova `nome` + execução `descricao` + registro `descricao`) e `atividade-visualizar-dialog` (nome inline); `tag-listagem` (formulários nova+editar); `execucao-historico` (editar execução `descricao`). **Detalhe não óbvio:** em `atividade-listagem`, `alternarExecucao` faz `setValidators` **dinâmico** da descrição de execução conforme o dono ser gestor (`[]`→opcional) ou desenvolvedor (`[Validators.required]`) — o `Validators.pattern` foi incluído em **ambos** os ramos, senão abrir o dialog **apagaria** a validação de caracteres. Import do shared via **root barrel** `@project20/shared` (todo o projeto usa o barrel raiz — nem backend nem frontend usam subpaths; o `@project20/shared/validators` citado na spec **não resolveria no backend**, que não tem path-mapping de subpath). Stub `frontend/src/stubs/class-validator.stub.ts` já tinha `Matches` (task 80) — sem mudança. **Fora do escopo (não tocado):** cor, código de projeto, descrições de demanda (`descricaoTecnica`/`descricaoCliente`/`documentacao`), descrição de **atividade** (os controles `descricao` de `atividade-*` que editam a atividade, não a execução), anotações e login/senha; nenhuma migration ou limpeza de dados existentes; regra é **rejeitar**, não sanitizar. **Docs:** `CONVENTIONS.md` (seção DTOs — bloco "Caracteres proibidos…") e `SYSTEM.SPEC.md` §10 Camada 1 (novo bloco "Caracteres proibidos em nomes e descrição de execução"). **Verificação:** `npm run build --workspace=backend` (nest build — type-check cobre o shared) e `--workspace=frontend` OK (só warnings pré-existentes de budget/CommonJS); `grep` confirma a regex literal `` [^'"`~^\\´] `` **apenas** em `shared/src/validators/caracteres-proibidos.validator.ts` (0 duplicações em DTOs/frontend) e `REGEX_SEM_CARACTERES_PROIBIDOS` referenciada em **22 arquivos** (12 DTOs + 9 componentes + barrel). **Não exercitado:** fluxo HTTP real (400 do backend) e digitação no browser (bloqueio do submit) — apenas builds estáticos e checagem de tipos. **Ajuste pós-pedido do usuário (mesma sessão):** (1) **bloqueio na digitação** — os caracteres proibidos agora **não podem ser digitados nem colados**: nova **diretiva standalone** `BloquearCaracteresProibidosDirective` (seletor `appBloquearCaracteresProibidos`, em `frontend/src/app/shared/directives/`) que ouve o evento `input` do `<input>`/`<textarea>`, remove os proibidos via novo helper do shared `removerCaracteresProibidos(texto)` (regex `` /['"`~^\\´]/g ``), reescreve o valor no `FormControl` (via `NgControl` injetado, opcional) e **restaura a posição do cursor** (desconta os removidos antes do caret); cobre digitação, colagem e arrastar-soltar (todos disparam `input`) e, numa colagem mista, preserva os caracteres válidos. Aplicada aos **12 controles** (atributo no elemento + import da diretiva nos 9 componentes). (2) **hint persistente** abaixo de cada campo — novo constante shared `MENSAGEM_CARACTERES_PROIBIDOS_DICA` ('Não é permitida a utilização de caracteres especiais'), exposta por componente como `dicaCaracteresProibidos` e renderizada num `<small class="block mt-1 text-muted-color">` sob cada um dos 12 controles. O `Validators.pattern` + a exibição do erro `pattern` permanecem como **defesa** (backend segue autoritativo), embora com o bloqueio o erro dificilmente dispare. (3) **acento agudo `´` (U+00B4)** somado ao conjunto — o acento agudo morto do teclado ABNT, distinto da crase; como tudo deriva da fonte única, bastou incluí-lo em `CARACTERES_PROIBIDOS`/`REGEX_SEM_CARACTERES_PROIBIDOS`/`MENSAGEM_CARACTERES_PROIBIDOS`/`removerCaracteresProibidos` e propagou automaticamente aos 12 DTOs, aos 12 controles, à diretiva e ao hint. **Docs** (CONVENTIONS.md, SYSTEM.SPEC.md §10) atualizados com `´`. **Verificação:** builds de backend e frontend OK após os ajustes (só warnings pré-existentes de budget/CommonJS); grep confirma a classe literal só em `caracteres-proibidos.validator.ts`. **Não exercitado (ajustes):** runtime no browser (digitar/colar proibido some na hora; hint visível) — só builds estáticos.
**Task anterior:** 80-projeto-relatorio-execucoes — **shared + backend + frontend** (sem migration). Novo **módulo `relatorio`** (controller + service, **sem repositório próprio** — consome `ExecucaoRepository`/`ProjetoRepository`, padrão do `ponto`): dentro do detalhe de um projeto, o **gestor** gera um relatório de **todas as execuções** de um período (**ANUAL**/**MENSAL**/**CUSTOM**), pré-visualiza numa tabela, **baixa CSV** e tem a base (**groundwork**) da **revisão por IA**. **Shared:** 2 enums (`RelatorioPeriodoTipoEnum` ANUAL/MENSAL/CUSTOM; `RelatorioFormatoEnum` CSV/XLSX) + barrel; 8 DTOs em `dtos/relatorio/` (+ barrel + registro em `dtos/index`): entrada `RelatorioExecucaoConsultarDto`/`RelatorioExecucaoBaixarDto`(+`formato`)/`RelatorioRevisaoSolicitarDto` (campos de período com `@ValidateIf` por `periodoTipo`; `projetoId` **opcional na validação** — injetado pela controller, padrão `DemandaConexaoCriarDto`), interno `RelatorioExecucaoFiltrarDto`, saída `RelatorioExecucaoLinhaDto` (value-object por execução) / `RelatorioExecucaoDto` (recorte computado) / `RelatorioInconsistenciaDto` / `RelatorioRevisaoDto`. Recortes sem verbo-particípio (§5.1); **sem `@ApiProperty`** (segue o estilo de `ponto`/`execucao`, não de `demanda`). **Backend:** `ExecucaoRepository.listarParaRelatorio(filtros)` — SELECT sem paginação da cadeia `execucao→atividade→usuario→demanda→projeto` filtrando `projeto.id` + `DATE(inicio_data) BETWEEN :dataInicio::DATE AND :dataFim::DATE`, `is_deleted = false` em todas. **Reconciliação spec×código:** duração via `COALESCE(fim_data, NOW())` (padrão pós-75; a spec pedia o `:agora`/`CAST` que a task 75 **reverteu**) e `statusAtividade` via **JOIN `tipo_atividade_status`** (`codigo AS "statusAtividade"`, pós-79) em vez de `atividade.status`; o DTO usa `TipoAtividadeStatusEnum` (não o obsoleto `AtividadeStatusEnum` citado na spec). `RelatorioService`: públicos `gerarRelatorio`/`baixarRelatorio`/`revisarRelatorio` + privados `montarRelatorio` (resolve período → `ProjetoRepository.recuperar` (404 `ResourceNotFoundException`) → `listarParaRelatorio` → totais), `validarEResolverPeriodo` (ANUAL `[ano-01-01, ano-12-31]`; MENSAL 1º/último dia **real** do mês; CUSTOM valida `fim>=inicio`; campos faltantes → `BusinessException`), `montarCsv` (BOM UTF-8 `String.fromCharCode(0xFEFF)`, CRLF, escape de aspas/vírgula/quebra, colunas `Projeto,Demanda,Atividade,Usuário,Data Início,Data Fim,Tempo (h),Descrição` — **início/fim são data+hora completas** (`DD/MM/YYYY HH:MM` no fuso `APP_TIMEZONE` via `Intl`, `hourCycle:'h23'`) porque a execução pode virar o dia; no **CSV** as duas células de data-hora são **forçadas a texto** via fórmula-string do Excel (`="..."`, helper `forcarTextoPlanilha`) para o Excel **não** reconhecer como data-hora e "comer" a meia-noite (00:00 → só a data) — mantendo o formato com `:`; **sem coluna Data/Status** separadas — `statusAtividade` continua no DTO/SQL só para enriquecer o prompt da IA; duração `HH:MM`), `montarPromptRevisao` (amostra ≤200 linhas + agregados) e `parsearRevisao` (parse defensivo do 1º bloco `{…}`; falha de rede/parse → `BusinessException('Serviço de IA temporariamente indisponível')`; cliente Anthropic montado do `ConfigService`, como o `AssistenteService`). **XLSX = follow-up:** sem a dependência `exceljs` nesta task → `formato=XLSX` lança `BusinessException('Formato XLSX ainda não disponível')` e o front oferece só CSV (o enum já prevê os dois). `RelatorioController` (`@Controller('relatorio')`, `@UseGuards(JwtAuthGuard)`, **todas as rotas `@GestorOnly()`**): `GET /relatorio/execucao`, `GET /relatorio/execucao/download` (`@Res({ passthrough:true })` + `StreamableFile`), `POST /relatorio/execucao/revisar`; microinteligência de injetar `projetoId` de `@Query('projetoId', ParseIntPipe)` no DTO (`{ ...dto, projetoId }`; service tipa `Dto & { projetoId: number }` — sem `!`, padrão task 72). `RelatorioModule` importa `ExecucaoModule`/`ProjetoModule` (ambos já exportavam o repositório) e é registrado no `app.module`. **`ResponseFormatInterceptor` (core) — guarda nova:** `dados instanceof StreamableFile` **não** é envelopado em `StandardResponse` (senão o download binário quebraria; a spec já previa essa rota fora do envelope). **Frontend:** `RelatorioService` (`modules/relatorio/services/`) — `gerarRelatorio` (`GET`), `baixarRelatorio` (`GET` `responseType:'blob'` + `observe:'response'` p/ ler o `Content-Disposition`), `revisarRelatorio` (`POST`; `projetoId` na query + período no corpo). `RelatorioExecucaoDialogComponent` (standalone, imperativo `abrir(projetoId)`) — Reactive Form com `periodoTipo` + campos condicionais (`p-select` ano/mês **ou**, no personalizado, um **único `p-datepicker` `selectionMode="range"`** — um controle `periodo: Date[]` p/ início+fim, que já ordena as datas), botões **Pré-visualizar** (`p-table` de 7 colunas com **Data Início/Data Fim** completas + rodapé `totalExecucoes`/`totalMinutos | minutosParagHoras`), **Baixar CSV** (download via `objectURL`/`<a download>`/`revoke`) e **Revisar com IA** (`[disabled]="true"` + tooltip "Em breve" — groundwork; painel `resumo`/`inconsistencias` fica pronto); usa `MinutosParaHorasPipe`; `appendTo="body"` nos selects/datepickers. Botão **Relatório** (só-gestor) no cabeçalho do `projeto-detalhe` abrindo o dialog (`#relatorioDialog`). **Stub do front:** `frontend/src/stubs/class-validator.stub.ts` ganhou `IsInt` e `ValidateIf` (decorators novos usados pelos DTOs do shared, que o front bundla). **Verificação:** `npm run build --workspace=backend` e `--workspace=frontend` OK (só warnings pré-existentes de budget/CommonJS). **Não exercitado:** fluxo HTTP/DB e download real no browser (sem servidor/DB up nesta sessão) — apenas builds estáticos e checagens de tipo.
**Task anterior:** 84-guarda-alteracoes-nao-salvas-dialogs — **frontend apenas** (sem backend/shared/migration; spec direto em `docs/specs/done/`). Guarda de **alterações não salvas ao fechar** os diálogos de editor de texto rico. Em **qualquer** forma de fechar (X do cabeçalho, ESC, botão Cancelar) um diálogo cujo conteúdo foi editado mas não persistido, o sistema abre um `p-confirmDialog` **"Alterações não salvas — Salvar / Descartar"**; **Salvar** persiste e **Descartar** fecha sem salvar. Cobre os três pontos pedidos: **anotações do usuário** (`usuario-anotacoes-dialog`) e os campos de descrição da demanda — **descrição técnica, descrição para o cliente e documentação** — nos **dois** diálogos que os editam: `demanda-detalhe-dialog` e `demanda-arvore-painel` (via context menu da árvore). **Padrão de implementação ("fechar-e-perguntar", uniforme p/ X/ESC/Cancelar):** o `p-dialog` passou de `[(visible)]` two-way para **`[visible]="signal()"` + `(visibleChange)="aoTentarFechar($event)"`** e o botão **Cancelar** passou a chamar o mesmo `aoTentarFechar(false)` (antes fazia `set(false)` direto). Detalhe não óbvio do PrimeNG 21: o `close()` interno seta `this._visible=false` imperativamente; como o valor do binding one-way (`signal()===true`) **não muda**, o Angular **não reescreve** o input e o diálogo fecha de fato — ou seja, **não dá** para "vetar" o fechamento mantendo o editor aberto sem desabilitar o close nativo (o que mataria o ESC). Por isso o handler **fecha de fato** (`signal.set(false)`, mantendo o estado coerente e evitando o bug de reabrir com signal preso em `true`) e **depois** mostra o confirm; o **Salvar** do confirm relê o valor do `FormControl`, que **persiste** após o editor desmontar (o `p-editor` não tem `ngOnDestroy` e só propaga `onModelChange` em `source==='user'`, então o teardown não zera o controle — sem perda de dados). **Detecção de "alterações não salvas":** novo signal `descricaoConteudoSalvo`/`conteudoSalvo` (baseline = valor carregado do servidor, atualizado a cada salvamento) comparado com o valor atual do editor; como o `writeValue`→`setContents` do Quill usa `source==='api'` (ignorado pelo handler de `text-change`), o baseline bate com o controle até o usuário digitar — **sem confirm espúrio** ao abrir+fechar sem editar. **`usuario-anotacoes-dialog`:** removido o `(onHide)="aoFecharDialog()"`; novos `aoTentarFechar`/`fecharEfetivamente`/`salvarEFechar` (este reaproveita o auto-save de 30s/`conteudoSalvo`/`houveAlteracao` e o output `aoAlterar` que recarrega a listagem). **Dialogs de descrição:** novos `aoTentarFecharDescricao`/`descricaoTemAlteracoesNaoSalvas`; `salvarDescricao` agora carimba `descricaoConteudoSalvo` no sucesso (semântica do DTO preservada — continua enviando `valor ?? undefined`). O `demanda-arvore-painel` **não** provia `ConfirmationService` nem importava `ConfirmDialogModule` → ambos adicionados, mais `<p-confirmDialog key="descricao-fechar" />` no template; `demanda-detalhe-dialog` ganhou um segundo `<p-confirmDialog key="descricao-fechar" />` (o default sem key segue na exclusão); anotações ganhou `<p-confirmDialog key="anotacoes-fechar" />` ao lado do `anotacoes-limpar`. **Botão "Fechar"** (modo leitura, sem permissão de edição) e o caminho sem alterações fecham direto (sem confirm). **Verificação:** `npm run build --workspace=frontend` OK (só warnings pré-existentes de budget/CommonJS); `grep aoFecharDialog` → 0. **Não exercitado:** runtime no browser (abrir editor, digitar, X/ESC/Cancelar → confirm; Salvar persiste, Descartar descarta) — validação só por build estático.
**Task anterior:** 79-enums-para-tabelas-referencia — **DB + backend + shared + docs** (sem mudança de comportamento de frontend; só rename mecânico de import de enum). Converteu os **7 enums de domínio fechado** (antes `VARCHAR + CHECK`) em **tabelas de referência** `tipo_<tabela>_<complemento?>`; as 5 tabelas de negócio passam a usar `INTEGER` FK para o `id` da referência. **(§0) Rename dos enums TS** (`shared/src/enums/`, arquivo + símbolo): `usuario-tipo.enum`→`tipo-usuario.enum` (`UsuarioTipoEnum`→`TipoUsuarioEnum`), `usuario-status`→`tipo-usuario-status` (`TipoUsuarioStatusEnum`), `projeto-status`→`tipo-projeto-status` (`TipoProjetoStatusEnum`), `demanda-status`→`tipo-demanda-status` (`TipoDemandaStatusEnum`), `atividade-status`→`tipo-atividade-status` (`TipoAtividadeStatusEnum`), `dia-nao-util-tipo`→`tipo-dia-nao-util` (`TipoDiaNaoUtilEnum`), `dia-nao-util-duracao`→`tipo-dia-nao-util-duracao` (`TipoDiaNaoUtilDuracaoEnum`); barrel `enums/index.ts` reescrito; **valores e semântica inalterados** (viram os `codigo`). Rename mecânico aplicado por `sed` em **90 arquivos** de `backend/src`/`frontend/src`/`shared/src` (símbolos + os import-paths diretos `'../../enums/...'` dos DTOs do shared); `grep` confirma 0 símbolos/paths antigos e 0 `TipoTipo`. **(Migration NOVA `20240025_enums_para_tabelas_referencia.ts`)** — config-driven (arrays `TABELAS_REFERENCIA` + `CONVERSOES`). `up`: cria as 7 tabelas (BaseEntity em `timestamptz` + `codigo VARCHAR(40)` + `descricao VARCHAR(100)`, `uix_<tabela>_codigo` parcial, `trg_<tabela>_updated_date` com `fn_set_updated_date`), seeda (INSERT … SELECT … RETURNING, sem VALUES/DEFAULT) e, por coluna de enum: `ADD COLUMN <ref>_id` → `UPDATE` via JOIN por `codigo` → `SET NOT NULL` → FK `fk_<neg>_<ref>` → drop CHECK antigo + índice antigo → cria `ix_<neg>_<ref>_id` (só onde havia índice) → `DROP COLUMN` antiga. `down`: reverte (recria VARCHAR+CHECK+índice antigo, repopula por `id`, dropa FK/coluna) e dropa as 7 tabelas. **Constraints reais** consultadas no banco: `chk_demanda_status`/`chk_dia_nao_util_duracao` (renomeadas pela 70) vs. auto-gerados `usuario_tipo_check`/`usuario_status_check`/`projeto_status_check`/`atividade_status_check`/`dia_nao_util_tipo_check`. **Reconciliação spec×banco:** a spec seedava `tipo_demanda_status` com `EM_DESENVOLVIMENTO`, mas o estado real (pós-`20240013`) é `PENDENTE/PLANEJADA/CONCLUIDA` — seedado com os valores reais (senão o `SET NOT NULL` falharia). **(§3) Repositórios** (`usuario`, `projeto`, `demanda`, `atividade`, `calendario`): toda query que tocava coluna de enum reescrita — INSERT/UPDATE por subselect `(SELECT id … WHERE codigo = :campo AND is_deleted=false)`; SELECT/RETURNING por JOIN/subselect expondo `codigo AS <campo>`; filtros de listagem comparam `<ref>.codigo`. CTE recursivo de `demanda.listarDescendentes`: `status` removido do CTE e obtido do JOIN externo. **Fora da lista da spec (necessário p/ compilar/rodar):** `execucao.repository.recuperarTipoUsuarioDaAtividade`, `demanda.repository.listarMembrosDemanda` e `atividade.repository.listar` também liam `usuario.tipo` (cross-module) → ganharam JOIN em `tipo_usuario`. **Models/DTOs/services/frontend:** só o símbolo do enum mudou (campo continua tipado pelo enum; tradução `codigo ⇄ id` fica no repositório). **Doc** — `SCHEMA.md` (5 tabelas de negócio com coluna FK + índice renomeado; nova seção "Tabelas de referência (enums)" com molde + tabela de seeds; histórico `20240025`); `SYSTEM.SPEC.md` §13 (linhas de enum → FK + nova subseção "Tabelas de Referência (Enums)"). **Verificação:** `npm run build --workspace=backend` e `--workspace=frontend` OK (só warnings pré-existentes); `db:migrate` aplicou (Batch 14) — banco confirma 7 tabelas `tipo_*` seedadas, `usuario` com `tipo_usuario_id`/`tipo_usuario_status_id` (sem `tipo`/`status`/CHECK), 0 FK nulo, joins resolvem (usuário 2 DEV+2 GESTOR ATIVO; 17 dias não úteis mapeados); **`db:rollback`** restaurou VARCHAR+valores (`demanda`: CONCLUIDA/PENDENTE/PLANEJADA, 15 linhas) e dropou as `tipo_*`; **`db:migrate`** reaplicou sem erro nem perda de dados. **Não exercitado:** smoke HTTP via servidor rodando (validado pelas queries SQL equivalentes + round-trip da migration).

**Task anterior:** 78-dto-core-heranca-e-execucao-lista — **shared + backend + frontend + docs** (sem migration/DB; só forma/herança de DTO e importadores). Esclareceu e aplicou a regra de **herança de DTO**: negócio nunca herda negócio (campos explícitos, mesmo idênticos); negócio herda **core** (`PaginatedResult`/`StandardResponse`) sem duplicar campos genéricos. **(1) `PaginatedResult`** (`shared/src/interfaces/paginated-result.interface.ts`) virou **`export class`** (mesmos campos) — para poder ser estendida; barrel/localização inalterados; retornos por object-literal nos services seguem válidos por tipagem estrutural; `StandardResponse` **permanece interface**. **(2) Par item/wrapper de execução:** o **item** (13 campos) `ExecucaoResumoDto` → **`ExecucaoItemDto`** (novo arquivo, DTO de negócio com campos explícitos); o **wrapper** `ExecucaoListaDto` foi **removido** e substituído pelo novo **`ExecucaoResumoDto extends PaginatedResult<ExecucaoItemDto> { totalMinutosDia: number }`** (não redeclara `itens`/`totalItens`/`paginaAtual`/`itensPorPagina`/`totalPaginas`). Barrel `dtos/execucao/index.ts` atualizado (`ExecucaoItemDto` exportado; `ExecucaoListaDto` removido). **Importadores** (grep `ExecucaoResumoDto|ExecucaoListaDto`): _item → `ExecucaoItemDto`_ em `backend/execucao.repository` (retorno de `listar` + `executarConsulta<>`), `backend/ponto.service` (`calcularIntervalos`), `shared/dtos/ponto/PontoDiarioDto` (import relativo + `execucoes`), `frontend/atividade.service` (`PaginatedResult<>`), `frontend/atividade-listagem`, `frontend/atividade-detalhe` (signal + `PaginatedResult<>`), `frontend/atividade-visualizar-dialog`, `frontend/execucao-historico` (signal/edição); _wrapper → `ExecucaoResumoDto`_ em `backend/execucao.service` (`Promise<StandardResponse<ExecucaoResumoDto>>`) e `frontend/execucao.service` (`StandardResponse<ExecucaoResumoDto>`). **Sem mudança de SQL/campos** — só nomes, tipos de retorno e a herança; a forma do JSON de `GET /execucao` é preservada. **(3) Seis subclasses vazias de DTO de negócio → campos explícitos** (espelhando o pai, **com `@ApiProperty` quando o pai tinha**): `UsuarioAlteradoDto` (mirror de `UsuarioRecuperadoDto` — inclui `anotacoesAlteracaoData`, **não** presente na lista da spec por ser anterior à task 77), `ProjetoRecuperadoDto`/`ProjetoAlteradoDto` (mirror de `ProjetoCriadoDto`), `TagRecuperadaDto`/`TagAlteradaDto` (mirror de `TagCriadaDto`, sem decorators — o pai não tem), `DemandaAlteradaDto` (mirror de `DemandaRecuperadaDto` — **sem `prioridade`** (removida na task 76) e **com `podeEditar`**, divergindo da lista da spec, que era anterior à task 76). Os DTOs "pais" permanecem intactos. **Doc** — `SYSTEM.SPEC.md` §5.1 (novo bloco "Herança de DTO — negócio nunca herda negócio; negócio herda core", com exemplos ✅/❌ e o par item/wrapper de execução) e §16 #23 (reescrita: alias/re-export/**subclasse de DTO de negócio** proibidos; única herança permitida é de DTO core); `CONVENTIONS.md` (seção DTOs: bloco de herança + exemplo lado a lado; tabela de proibições: linha de alias/re-export distingue negócio❌/core✅); `CONTEXT.md` (Decisões Tomadas: 2 entradas — regra esclarecida + rename do par de execução). **Verificação:** `npm run build --workspace=backend` (nest build — cobre type-check do shared) e `npm run build --workspace=frontend` OK (só warnings pré-existentes de budget/CommonJS); `grep -rn "extends .*Dto {}" shared/src/dtos` → **0**; `grep -rn "ExecucaoListaDto"` fora de `docs/` → **0** (arquivo removido); única subclasse restante em `shared/src/dtos` é `ExecucaoResumoDto extends PaginatedResult<ExecucaoItemDto>` (core). **DB não exercitado** (task não tem migration); fluxo HTTP `GET /execucao` e renderização no browser não executados (builds estáticos OK).
**Task anterior:** 77-usuario-anotacoes-data-alteracao — **DB + backend + shared + frontend + docs**. A entidade `usuario` ganhou a coluna **`anotacoes_alteracao_data`** que registra **quando as anotações foram alteradas pela última vez**; o **dialog de anotações** passa a exibir essa data **abaixo do título**. **Migration NOVA** `20240024_adicionar_anotacoes_alteracao_data_usuario.ts` — `up`: `ALTER TABLE usuario ADD COLUMN anotacoes_alteracao_data TIMESTAMPTZ` (nullable, **sem `DEFAULT`** §16 #7 — linhas existentes ficam `NULL` = "sem alteração registrada"); `down`: `DROP COLUMN IF EXISTS`. Nasce já em **`timestamptz`** (decisão de escopo da spec): a coluna é escrita por `NOW()`, então é correta independentemente da spec 75 (que não a converteria — a lista da 75 é fixa); aceita-se a assimetria temporária de `usuario` ter esta coluna em `timestamptz` enquanto as irmãs seguem `timestamp` até a 75. **Backend** — `usuario.model.ts`: `+ anotacoesAlteracaoData: Date | null` (junto a `anotacoes`). `UsuarioRepository`: `recuperar` ganhou `usuario.anotacoes_alteracao_data AS "anotacoesAlteracaoData"` no SELECT; `alterar` — **dentro** do ramo `if (dto.anotacoes !== undefined)** carimba a data **só quando o conteúdo muda** via `anotacoes_alteracao_data = CASE WHEN anotacoes IS DISTINCT FROM :anotacoes THEN NOW() ELSE anotacoes_alteracao_data END` (na RHS do SET o `anotacoes` ainda é o **valor antigo** → compara antigo vs. novo; evita que o auto-save de 30s reenviando o mesmo conteúdo bombardeie a data) + coluna acrescentada ao `RETURNING`. `inserir`/`alterarSenha`/`listar` **não** tocados. `usuario.service.ts`: o helper privado `mapearParaRecuperado` ganhou `anotacoesAlteracaoData: usuario.anotacoesAlteracaoData` (**obrigatório p/ compilar** — monta o `UsuarioRecuperadoDto` manualmente; sem isso o `tsc` acusa `TS2741`). Sem mudança de lógica/autorização. **Shared** — só `UsuarioRecuperadoDto` ganhou `@ApiProperty({ nullable: true }) anotacoesAlteracaoData: Date | null` (logo após `anotacoes`); `UsuarioAlteradoDto extends UsuarioRecuperadoDto` herda. `UsuarioInternoAlterarDto`/`UsuarioAlterarDto`/`UsuarioCriarDto`/`UsuarioResumoDto` **inalterados** — a data é **derivada no repositório**, nunca entrada do cliente. **Frontend** (`usuario-anotacoes-dialog`) — `.ts`: novo signal `anotacoesAlteracaoData = signal<Date | string | null>(null)`, `DatePipe` adicionado aos `imports`, `carregarAnotacoes` captura `resposta.dados.anotacoesAlteracaoData ?? null`, `salvar` e `salvarSilencioso` atualizam o signal do `resposta.dados?.anotacoesAlteracaoData` (a linha reflete o salvamento sem reabrir o dialog); `tituloDialog()` inalterado. `.html`: `[header]` string trocado pelo **`<ng-template #header>`** (padrão PrimeNG 21 da task 68) com 2 linhas — título + `@if (anotacoesAlteracaoData())` "Última alteração: {{ … | date: 'dd/MM/yyyy HH:mm:ss' }}" senão "Sem alterações registradas". `.scss`: BEM novo `&__cabecalho`/`&__titulo`/`&__alteracao` (texto secundário via `--p-text-muted-color`, ícone `pi-history`). `UsuarioService` (front) **não** muda. **Ajuste pós-pedido do usuário (mesma sessão):** (1) formato da data exibida ganhou **segundos** (`dd/MM/yyyy HH:mm:ss`); (2) **chip "Alterado"** (`p-tag` `severity="warn"`, ícone `pi-pencil`) ao lado do título indicando **edições não salvas** (dirty state) — implementado com signals: `conteudoSalvo` (último conteúdo persistido — atualizado em `carregarAnotacoes`/`salvar`/`salvarSilencioso`, capturando o valor **enviado** para não perder digitação concorrente durante o request) vs. `conteudoEditor` (`toSignal` de `valueChanges` com `startWith`), e `temAlteracoesNaoSalvas = computed(() => (conteudoEditor() ?? '') !== conteudoSalvo())`; o chip aparece ao digitar e some após salvar. SCSS ganhou `&__titulo-linha` (flex row título+chip) e `&__chip`. Imports `computed`/`toSignal`/`startWith`/`TagModule` adicionados. (3) **Brilho de "tem anotações" na listagem** (`usuario-listagem.page`, classe `--ativo` com `text-shadow`/glow, dirigida por `UsuarioResumoDto.temAnotacoes`): o flag era calculado no `UsuarioRepository.listar` como `btrim(usuario.anotacoes) <> ''`, que considerava "preenchido" o **HTML vazio do p-editor** (`<p><br></p>`, `<p></p>`, `<p>&nbsp;</p>`, só espaços) — então o ícone ficava aceso para anotações visualmente vazias. Corrigido para `regexp_replace(usuario.anotacoes, '<[^>]*>|&nbsp;|&#160;|[[:space:]]+', '', 'gi') <> ''` (remove tags HTML, `&nbsp;`/`&#160;` e todo whitespace antes de testar) — sobrando algum caractere real, há anotação; senão fica apagado. Validado no banco vivo contra os casos `<p><br></p>`/`<p></p>`/`''`/só-espaços/`&nbsp;`/NULL → `false`; texto real/lista → `true`. **Só SQL** (sem mudança de DTO/assinatura); frontend e demais métodos intocados. (4) **Recarregar a listagem ao fechar o dialog de anotações:** o `UsuarioAnotacoesDialogComponent` ganhou `output aoAlterar` + signal privado `houveAlteracao` (marcado em `salvar`/`salvarSilencioso` no sucesso, resetado em `abrir`); novo handler `aoFecharDialog()` (ligado ao `(onHide)` do `p-dialog`) emite `aoAlterar` **só quando houve algum salvamento** desde a abertura. `usuario-listagem.page.html`: `<app-usuario-anotacoes-dialog (aoAlterar)="buscarUsuarios()">` — assim, ao fechar o dialog após editar/auto-salvar, a listagem recarrega e o brilho de `temAnotacoes` reflete o novo estado (incl. anotação esvaziada → apagado). Import `output` adicionado. Sem recarga desnecessária quando o dialog é só aberto e fechado sem edição. **Doc** — `SCHEMA.md` (`CREATE TABLE usuario` + linha de histórico `20240024`), `SYSTEM.SPEC.md` §13 (linha `anotacoes_alteracao_data TIMESTAMPTZ` na tabela Usuario, com nota de tipo). **Verificação:** `npm run build --workspace=backend` (nest build — cobre o type-check do shared; o `TS2741` do `mapearParaRecuperado` foi corrigido) e `npm run build --workspace=frontend` OK (só warnings pré-existentes de budget/CommonJS); `db:migrate` aplicou (Batch 13), `db:rollback`+`db:migrate` reverteu/reaplicou sem erro, `information_schema.columns` confirma `anotacoes_alteracao_data` = `timestamp with time zone`; **lógica do carimbo validada no banco vivo** (transação revertida + teste com restauração do estado original): mudança real → data setada; reenvio do mesmo conteúdo → data **inalterada**; alterar só o nome → data **inalterada**; mudanças em requests separados → data **avança** (dentro de uma única transação `NOW()`=`transaction_timestamp` é constante, por isso o avanço só se observa em requests/conexões separados). **Não exercitado:** fluxo HTTP `PUT/GET /usuario/:id` via servidor e a renderização do dialog no browser (build estático OK).
**Task anterior:** 76-remover-prioridade-demanda — **DB + backend + shared + frontend + docs**. Removido por completo o campo `prioridade` da entidade **demanda** (`BAIXA/MEDIA/ALTA/CRITICA`) e todo o seu rastro; o conceito não tinha mais utilidade prática (sem ordenação ou regra de negócio dependente — só enfeite em formulários e raio/cor no grafo). **Migration NOVA** `20240023_remover_prioridade_demanda.ts` — `up`: `DROP INDEX IF EXISTS ix_demanda_prioridade` + `ALTER TABLE demanda DROP COLUMN IF EXISTS prioridade`; `down`: recria a coluna no padrão da `20240005` (sem `DEFAULT`, §16 #7) — `ADD COLUMN VARCHAR(20)` → `UPDATE ... = 'MEDIA'` → `SET NOT NULL` → `ADD CONSTRAINT chk_demanda_prioridade CHECK (prioridade IN ('BAIXA','MEDIA','ALTA','CRITICA'))` → recria `ix_demanda_prioridade` (índice parcial `WHERE is_deleted = false`). **Backend** — `DemandaRepository`: `prioridade` removido do INSERT (colunas/SELECT/RETURNING/parâmetros) de `inserir`, do SELECT de `recuperar`, do ramo de filtro + dos dois SELECTs de `listar`, do ramo do SET dinâmico + RETURNING de `alterar`, e do CTE recursivo de `listarDescendentes` (âncora + recursivo `demanda_filho.prioridade` + SELECT externo + tipo inline de retorno) e do SELECT de `recuperarGrafo`; removido o import `DemandaPrioridadeEnum`. `DemandaService`: `prioridade` removido de `criar`/`alterar` e do mapeamento de `recuperarArvore`. **Model** `demanda.model.ts`: campo + import removidos. **Shared** — enum `demanda-prioridade.enum.ts` **deletado** + linha removida de `enums/index.ts`; campo `prioridade` (e imports/decorators órfãos) removido de `DemandaCriarDto`, `DemandaAlterarDto`, `DemandaInternoAlterarDto` (comentário "dez"→"nove campos"), `DemandaListarDto`, `DemandaCriadaDto`, `DemandaRecuperadaDto`, `DemandaResumoDto`, `DemandaArvoreItemDto`, `DemandaGrafoNoDto`. **Frontend** — `demanda-formulario-dialog` e `demanda-edicao-dialog` (`.ts`+`.html`): removidos campo/`p-select` de Prioridade, control do form, `prioridadeOpcoes`, valores de reset/patch, `prioridade` no DTO e import do enum — e, no mesmo passo, **Status + Previsão de Término + Horas Estimadas** passaram a dividir **uma única `__linha`** (3 colunas) nos dois dialogs, com larguras proporcionais via modificadores BEM (`__campo--previsao { flex: 1.4 }` para caber `dd/mm/aaaa` + ícone, `__campo--horas { flex: 0.85 }`, Status `flex: 1`). **Dois detalhes não óbvios** descobertos medindo no browser: (1) `&__campo` precisou de `min-width: 0` — itens flex usam `min-width:auto` e não encolhem abaixo da largura intrínseca do controle, fazendo o `p-inputnumber` ocupar ~207px e roubar a coluna da previsão (que clipava a data); (2) mesmo com isso, o `<input>` interno do `p-inputnumber` mantinha largura intrínseca (~207px) e estourava o campo de 105px (`linhaScrollW` 620 vs 518) — resolvido com `[inputStyle]="{ width: '100%' }"` no `p-inputnumber` (idiomático PrimeNG, sem `::ng-deep`). Validado visualmente via browser headless com a data preenchida — Status `Planejada`, Previsão `29/06/2026` e Horas `0h` cabem na linha sem corte/quebra/overflow (medições: 150/209/127px, `clipped:false`), e sem campo de prioridade; `demanda-detalhe-dialog.html`: bloco "Prioridade" removido; `demanda-arvore-item.ts`: método `severidadePrioridade()` + `Record` + import removidos (o `p-tag` da árvore usa **status**, sem alteração no `.html`); `demanda-grafo.ts`: `calcularRaioNo` agora depende **só de `isEstrutural`** — estrutural `28`, não-estrutural `28 × 0.6 = 16.8` (40% menor); `raioPorPrioridade` e import removidos; `demanda-projeto.page.ts` e `projeto-detalhe.page.ts`: `prioridade: no.prioridade` removido da montagem dos nós; `demanda.model.ts` (front): campo + import removidos. **Doc** — `SCHEMA.md` (coluna + índice removidos do `CREATE TABLE demanda`; histórico ganhou `20240022`/`20240023`); `SYSTEM.SPEC.md` (§5.4 linha `demanda.prioridade`; §6.2 árvore de enums; §13 tabela Demanda). **Verificação:** `npm run build --workspace=backend` (nest build, type-check cobre o shared) e `npm run build --workspace=frontend` OK (só warnings pré-existentes de budget/CommonJS); `grep -ri prioridade` em `backend/src`/`frontend/src`/`shared/src` retorna **apenas** as migrations `20240023` (nova) e `20240005` (original, não editada). Migration **não** aplicada nesta sessão (DB não exercitado) — verificações de runtime (criar/editar demanda, árvore/grafo, `db:migrate`/`db:rollback`) ficam pendentes de uma sessão com banco up.
**Ajuste (mesma sessão, pós-76 — frontend apenas, sem spec):** corrigido bug em que, no **modo grafo** da tela de demandas (`demanda-projeto.page`), clicar num nó abria o diálogo de detalhe **invisível** (só aparecia ao trocar para a lista). **Causa raiz:** o `app-demanda-arvore-painel` (`#painel`, que hospeda os diálogos e expõe `abrirDetalhe` para o grafo) fica **dentro** do container `.demanda-projeto__lista` que tem `[hidden]="modoVisualizacao() !== 'lista'"` — no modo grafo o `[hidden]` aplica `display:none` ao subtree inteiro, suprimindo o `p-dialog` aberto (reproduzido via browser headless: após o clique, `.p-dialog` existia com `offsetParent === null`/`width 0`). **Correção:** adicionado `appendTo="body"` aos `p-dialog` da cadeia alcançável a partir do grafo, para escaparem do container oculto e renderizarem em nível de `body` sobre o grafo — `demanda-detalhe-dialog` (diálogo principal + sub-diálogos de descrição e de tags), `demanda-edicao-dialog` (principal) e `demanda-formulario-dialog` (principal, p/ "Nova Sub-demanda" aberta de dentro do detalhe). Os diálogos próprios do `arvore-painel` (tags/membros/descrição via menu de contexto) **não** foram tocados — só são acessíveis no modo lista, onde o container é visível. **Verificação (browser headless):** no modo grafo, clique no nó → detalhe renderiza sobre o grafo (`visible: true`, `width 1100`); dentro dele, "Editar" → `Editar Demanda` também sobre o grafo (`visible: true`, `width 600`); regressão no modo lista / `projeto-detalhe` → diálogos seguem abrindo normalmente; `npm run build --workspace=frontend` OK.
**Task anterior:** 75-datas-timestamptz-correcao-fuso — **backend + migration** (sem frontend, sem shared). Correção de **raiz** do problema de fuso: as colunas de data/hora deixam de ser `timestamp without time zone` (naïve, com semânticas conflitantes — `execucao.inicio_data`/`fim_data` em relógio BRT gravado pelo app vs. `created_date`/`updated_date`/`deleted_date` em UTC gravado por `NOW()`) e passam a `timestamptz` (instante UTC no banco), com a **sessão do banco fixada no fuso da app** para que `NOW()` e o bucketing por dia fiquem corretos. **Config** — nova var `APP_TIMEZONE` (default `America/Sao_Paulo`): adicionada a `.env`/`.env.example` (comentário obsoleto do `TZ` atualizado — `TZ` mantido só p/ logs/exibição, a correção não depende mais dele) e ao `README` (tabela de envs); exposta em `ConfiguracaoAplicacao.fusoHorario` (`config.interface.ts` + `config.service.ts`, `this.opcional('APP_TIMEZONE') ?? 'America/Sao_Paulo'`). **Sessão do banco** — `core/database/database.provider.ts` e `database/knexfile.ts` ganharam `pool.afterCreate` executando `SET TIME ZONE '<fusoHorario>'` em cada conexão (provider lê do `ConfigService.obter().aplicacao`; knexfile lê `process.env.APP_TIMEZONE` — intencional, é CLI); interpolar a config do servidor neste bootstrap é aceitável (não é entrada de request). **Migration NOVA** `20240022_converter_datas_para_timestamptz.ts` — itera as **11 tabelas com BaseEntity** (`usuario, projeto, tag, demanda, demanda_usuario, demanda_conexao, demanda_tag, atividade, atividade_tag, execucao, dia_nao_util`) convertendo `created_date`/`updated_date`/`deleted_date` com `USING ... AT TIME ZONE 'UTC'` (foram gravadas por `NOW()` → UTC naïve) e, **só em `execucao`**, `inicio_data`/`fim_data` com `USING ... AT TIME ZONE 'America/Sao_Paulo'` (gravadas pelo app em relógio BRT). `USING ... AT TIME ZONE 'zone'` é determinístico (independe da sessão) → preserva o instante real e corrige de quebra os `created_date` que apareciam +3h. **`down`** reverte para `timestamp` com o `USING` inverso (BRT p/ inicio/fim, UTC p/ auditoria), restaurando o relógio de parede. Colunas **`date` puras NÃO tocadas** (`projeto.inicio_data`, `projeto.previsao_fim_data`, `demanda.previsao_fim_data`, `dia_nao_util.dia_data`); `fn_set_updated_date()` intocada (`NOW()` sobre `timestamptz` já grava o instante correto). **`execucao.repository.ts` — paliativo `:agora` revertido:** as 3 expressões `COALESCE(..., CAST(:agora AS timestamp))` (em `listar` — total do dia e linha — e em `alterar`) voltaram a `COALESCE(..., NOW())`; removidos `parametros.agora = new Date();` (+ comentário) e `agora: new Date()` do `alterar`. Manter o cast sobre `timestamptz` reintroduziria o bug (volta a ser naïve→coerção UTC). **Bucketing por dia inalterado** (`DATE(execucao.inicio_data)`, `::text`, `BETWEEN`, `GROUP BY DATE(...)` em `execucao.repository.ts`; `atividade.created_date::date` em `atividade.repository.ts`) — com a sessão em BRT já extraem o dia local. **Doc** — `SYSTEM.SPEC.md` (§9.1 bloco BaseEntity `TIMESTAMP`→`TIMESTAMPTZ`; §13 tabela `execucao` idem; §9.2 regra 5 ganhou nota de tipo `timestamptz`/sessão-no-fuso/`date` puro); `CONVENTIONS.md` (seção campos de data: bloco "Tipo de coluna de data" — `timestamptz` p/ instante, `date` p/ calendário puro). **Verificação:** `npm run build --workspace=backend` (nest build) OK; `db:migrate` aplicou (Batch 11); `information_schema.columns` confirma `timestamp with time zone` em todos os `*_date` de auditoria + `execucao.inicio_data/fim_data`, e `date` preservado em `projeto.inicio_data`/`*.previsao_fim_data`/`dia_nao_util.dia_data`; `db:rollback` reverteu (`execucao.inicio_data` → `timestamp without time zone`) e `db:migrate` reaplicou sem erro (ida-e-volta OK) — o `afterCreate`/`SET TIME ZONE` do `knexfile` foi exercitado pelas próprias migrations sem erro. **Correção fora da premissa da spec (calendário — regressão da sessão BRT):** a spec afirmava "calendário não afetado", mas a revisão pós-implementação detectou **off-by-one**: `calendario.repository.ts` (`validarDia` e `recuperarTipo`) recebe `CalendarioVerificarDiaDto.data` como `Date` (instante em **meia-noite UTC** — convenção do calendário, que usa `getUTCDay`/`Date.UTC`) e fazia `:data::DATE`; com a sessão agora em BRT, o cast de um instante UTC-meia-noite extrai o **dia anterior** (validado: `new Date('2026-01-01')` → `2025-12-31` → Confraternização **não** detectada). Corrigido trocando as **6 ocorrências** de `:data::DATE` por `(:data AT TIME ZONE 'UTC')::date` (extrai o dia de calendário em UTC, independente do fuso da sessão) — validado empiricamente via knex com a sessão BRT (01/01 e 25/12 → feriado=true; 27/06 → false). **Apenas o calendário** passava `Date`; execução/atividade usam strings `YYYY-MM-DD` (`ExecucaoListarDto.data`, `AtividadeListarDto.dataInicio/Fim`, `ExecucaoDiaAgruparDto`) e o **write** de dia não útil (`inserir`, `:diaData::DATE`) usa `DiaNaoUtilCriarDto.diaData: string` — todos TZ-independentes; o bucketing `DATE(execucao.inicio_data)`/`created_date::date` passa a bucketizar no **dia local BRT** (comportamento desejado pela spec). **Dado preservado verificado:** em sessão BRT, `execucao.inicio_data` mantém o wall-clock original (ex.: 23:51 BRT) e `created_date` — antes +3h — passou a coincidir com o mesmo instante (sintoma do bug eliminado). **Não exercitado nesta sessão** (exige app + browser rodando): `SHOW TimeZone` numa conexão viva do NestJS, exibição idêntica no front (ponto/histórico/timer ao vivo) e a duração de execução em andamento (verificações 4/5/7/8 da spec). **Frontend não tocado** (a serialização ISO dos instantes é a mesma que o front já consome).
**Task anterior:** 74-seed-feriados-nacionais-fixos — **migration de seed apenas** (sem DTO, service, repository, controller, frontend ou shared — exatamente o escopo da spec). Popula `dia_nao_util` com os **9 feriados nacionais brasileiros de data fixa** (recorrentes), para o gestor não cadastrá-los um a um. **Migration NOVA** `20240021_seed_feriados_nacionais.ts` — segue o padrão de `20240017_seed_tags_padrao.ts`: constante `ReadonlyArray` `FERIADOS_NACIONAIS_FIXOS` (mês/dia + descrição) + helper `montarDataReferencia` (monta `YYYY-MM-DD` no `ANO_REFERENCIA = 2024`). **`up`:** loop com `INSERT INTO dia_nao_util (dia_data, descricao, tipo, recorrente, duracao, created_date, updated_date, is_deleted) SELECT :diaData, :descricao, 'FERIADO', true, 'INTEGRAL', NOW(), NOW(), false WHERE NOT EXISTS (… recorrente = true AND is_deleted = false AND EXTRACT(MONTH/DAY …) = :mes/:dia)` — idempotente, parâmetros nomeados, sem `VALUES`/`DEFAULT`. **`down`:** `DELETE` pelas linhas **ativas** com mesmo critério mês/dia + `recorrente = true` + `is_deleted = false` (não toca soft-deletados nem não-recorrentes homônimos). **Valores fixos:** `tipo = FERIADO`, `recorrente = true`, `duracao = INTEGRAL`. **Feriados:** Confraternização Universal (01/01), Tiradentes (21/04), Dia do Trabalho (01/05), Independência (07/09), N. Sra. Aparecida (12/10), Finados (02/11), Proclamação da República (15/11), Zumbi/Consciência Negra (20/11), Natal (25/12). **Feriados móveis ficam de fora** (Carnaval/Sexta-feira Santa/Corpus Christi dependem da Páscoa — sem dia/mês fixo). Como `recorrente = true`, `ehDiaNaoUtil`/`validarDia` casam só por mês/dia (qualquer ano) — o ano 2024 em `dia_data` é só referência. **Nota de número:** a seção de doc da spec citava `20240020` (número obsoleto, já usado por `padronizar_nomes_constraints`); usado `20240021` — próximo livre, conforme o header da seção de Escopo da spec e a tabela de migrations do CONTEXT. **Doc** — `SCHEMA.md`: linha no histórico de migrations (`20240021` — seed dos 9 feriados, idempotência por mês/dia); `CONTEXT.md`: tabela "Banco de Dados" (`20240001`–`20240021`). **Verificação:** `npm run build --workspace=backend` (nest build) OK; `db:migrate` aplicou (Batch 10) e no banco vivo a consulta retornou **9** linhas `FERIADO`/`true`/`INTEGRAL` (mais 1 RECESSO recorrente `Véspera de Natal` em 23/12 **pré-existente**, não tocado pelo seed); idempotência confirmada — `db:rollback` → 0 feriados, `db:migrate` de novo → 9 (sem duplicar, RECESSO preservado); replicando a query do `validarDia`, `2030-12-25` e `2031-01-01` → não útil (casa por mês/dia em ano arbitrário) e `2030-03-10` (quarta comum) → útil. Endpoint HTTP `GET /calendario/verificar` não exercitado via servidor (validado pela query SQL equivalente do repositório).
**Task anterior:** 73-remover-ngmodel-seletor-cor — **frontend apenas** (sem backend, sem shared, sem migration). Eliminado o último uso de `ngModel`/`FormsModule` do projeto, no componente `seletor-cor` (achado C2 do audit de consistência; o "0 ngModel" da `AUDITORIA.md` virou inválido quando o `seletor-cor` foi adicionado depois da auditoria). O componente é um `ControlValueAccessor` que embrulha o `p-colorpicker` do PrimeNG e o dirigia por `[ngModel]="cor()"`/`(ngModelChange)="atualizarDoPicker($event)"` — literalmente template-driven, proibido pelo §8.2/CONVENTIONS. **`seletor-cor.component.ts`** — `FormsModule` → `ReactiveFormsModule` nos `imports`; novo `controleCor = new FormControl<string>('#3b82f6', { nonNullable: true })` como **única fonte de verdade** do valor; subscription `controleCor.valueChanges` (no `constructor`, com `takeUntilDestroyed()`) propaga o CVA (`aoMudar(valor)` + `aoTocar()`) e espelha o signal `cor` (mantido só para **exibição**: amostra/hex-input/`corSelecionada`); `writeValue` faz `controleCor.setValue(valor ?? '#3b82f6', { emitEvent: false })` + `cor.set(...)`; `setDisabledState` faz `controleCor.disable()/enable({ emitEvent: false })` (signal `desabilitado` mantido para os botões da paleta); `selecionarPredefinida`/`digitarHex` agora chamam `controleCor.setValue(valor)` (a subscription cuida da propagação) — removidos `atualizarDoPicker` e o `private atualizar()`. **`seletor-cor.component.html`** — `<p-colorpicker>` passou de `[ngModel]`/`(ngModelChange)` para `[formControl]="controleCor"`; restante (amostra, hex-input, paleta) inalterado, ainda lê `cor()`/`corSelecionada()`. **Contrato externo (CVA via `formControlName`) preservado** — projeto/tag/calendário não mudam. **Doc** — `CONVENTIONS.md`: nova seção "Frontend (Angular)" (standalone/Signals/Reactive Forms sem `ngModel`/lazy + bloco ✅/❌ de CVA com `FormControl` interno); `AUDITORIA.md`: errata da task 73 (o "0 ngModel" foi reintroduzido pelo `seletor-cor` e só foi efetivamente removido agora — substitui a atribuição do C2 à task 62). **Verificação:** `npm run build --workspace=frontend` OK (só warnings pré-existentes de budget/CommonJS); `grep -rn "ngModel\|FormsModule" frontend/src` → só `ReactiveFormsModule` (zero `ngModel`, zero `FormsModule`). Runtime (paleta/picker/hex refletindo no Reactive Form do pai) não executado nesta sessão.
**Task anterior:** 72-remover-non-null-assertions-injetadas — **backend apenas** (sem shared, sem frontend, sem migration). Eliminados os `!` (non-null assertions) que existiam nos services **apenas** porque o `id`/`*Id` chega como campo **opcional** no DTO de `@Body` e é injetado pela controller; o parâmetro do service passou a ser tipado em **interseção** (`& { … }`), tornando o campo injetado obrigatório — mesmo padrão já aceito no `ExecucaoService` (exceção C3 do audit). **As controllers não mudaram** (já passavam o objeto mesclado). **4 alvos E4:** `atividade.service.ts` `alterarTags(dto: AtividadeTagsAtribuirDto & { id: number }, …)` → `const atividadeId = dto.id;` (sem `!`); `demanda.service.ts` `criarConexao(dto: DemandaConexaoCriarDto & { demandaOrigemId: number })`, `alterarTagsDemanda(dto: DemandaTagsAtribuirDto & { demandaId: number }, …)` e `atribuirMembro(dto: DemandaUsuarioAtribuirDto & { demandaId: number }, …)` — os três leem `dto.demandaOrigemId`/`dto.demandaId` sem `!`. **C4 (JSDoc):** `execucao.repository.ts` — comentário do `recuperarUsuarioIdPorExecucao` trocou "encerrar, **atualizar** e excluir" por "encerrar, **alterar** e excluir" (§16 #24, coerência — é só comentário). **Seção 2 da spec (opcional, não-alvo do audit) — decisão: NÃO alterada.** Os `!` de `ponto.service.ts:131/133/146` (`diaNaoUtil!.duracao`/`.tipo`, sob o guard `registradoComoNaoUtil`) e `demanda.service.ts:369/379` (`mapa.get(item.id)!`, `dados: raiz!`) são `!` de **invariante real** (não injetados pela controller); mexê-los exigiria reestruturar lógica de cálculo de ponto/montagem de árvore **sem cobertura de testes** (DB não up nesta sessão) e extrapolaria o alvo do audit (§16 #14). A própria spec marca-os como "decidir na execução" e "manter (invariante real)" é opção válida. **Verificação:** `npm run build --workspace=backend` (nest build) OK; `grep -rn "dto\.\(id\|demandaId\|demandaOrigemId\)!" backend/src/modules` → **0 ocorrências**; confirmado que as 4 controllers já injetam o campo (`alterarTags({ ...dto, id })`, `criarConexao({ ...dto, demandaOrigemId: id })`, `alterarTagsDemanda({ ...dto, demandaId: id })`, `atribuirMembro({ ...dto, demandaId: id })`); `grep atualizar execucao.repository.ts` → 0. **Comportamento, validações, regras e SQL inalterados** — só assinaturas de service + 1 comentário.
**Task anterior:** 71-demanda-gestor-acesso-total-sem-atribuicao — **backend apenas** (sem migration, sem frontend). Dois ajustes ao acesso do gestor às demandas. **(1) Auto-atribuição:** ao criar demanda, o sistema deixa de gerar linhas em `demanda_usuario` para os gestores — o gestor tem acesso total a qualquer demanda/projeto **por ser gestor**, sem depender da tabela. Apenas o **criador desenvolvedor** (e membros explicitamente selecionados na criação) é atribuído. **(2) `GET /demanda/atribuidas`** (criação de atividade) passa a filtrar **`status IN ('PLANEJADA','PENDENTE')`** (exclui `CONCLUIDA`) e a devolver ao **gestor todas** as demandas não-concluídas (sem join em `demanda_usuario`); desenvolvedor continua vendo só as suas, agora também filtradas por status. **Shared** — `DemandaAtribuicaoInserirDto` trocado de `{ criadorId, gestorIds, membroIds }` para **`{ usuarioIds: number[] }`** (lista única de quem atribuir; pode ser `[]` na criação por gestor); `DemandaAtribuidasListarDto` ganhou **`apenasAtribuidas: boolean`** (true=dev filtra por `demanda_usuario`, false=gestor vê tudo). **Reconciliação spec×código:** a spec foi escrita contra um estado anterior sem a feature de selecionar membros na criação (`DemandaCriarDto.usuarioIds`); preservei essa feature — o service monta `usuarioIds = (criador-dev ? [sub] : []) ∪ membros selecionados` (dedup via `Set`), em vez do literal `[sub] : []` da spec. **`demanda.service.ts`** — `criar()`: removidos `listarGestoresAtivos()`/`gestorIds`; monta `usuarioIds` e chama `inserirComAtribuicao(dados, { usuarioIds })`; validações existentes (dev exige pai/acesso; pai mesmo projeto/estrutural; dev só adiciona a si; membros existem; tags gestor-only) intactas. `listarAtribuidas()`: passa `{ usuarioId, apenasAtribuidas: tipo===DESENVOLVEDOR }`. `usuarioRepositorio` **mantido** (ainda usado em `criar`/`atribuirMembro` via `recuperar`). **`demanda.repository.ts`** — `inserirComAtribuicao()`: loop simples sobre `dto.usuarioIds` (transação atômica preservada); `listarAtribuidas()`: join condicional por `apenasAtribuidas` + `WHERE ... status IN ('PLANEJADA','PENDENTE')` (literais, seguem o CHECK do banco — sem interpolação de runtime). **`atividade.service.ts`** — `criar()`: `validarAcessoDemanda` agora **só para desenvolvedor** (gestor cria atividade em qualquer demanda sem 403), alinhando ao padrão de `recuperar`/`alterar`/`listarTags`; cálculo de `executorId` intacto. **`demanda.controller.ts`** — textos `@ApiOperation` de `POST /demanda` e `GET /demanda/atribuidas` atualizados. **Efeito colateral aceitável:** lista de membros (`GET /demanda/:id/membro`) de demandas criadas por gestor vem vazia (coerente com "acesso por ser gestor"); criador-dev continua membro; fluxo `atribuirMembro`/`removerMembro`/último-membro (task 46) inalterado. **Doc** — `SYSTEM.SPEC.md` §13 (DemandaUsuario: tabela deriva acesso do **dev**; gestor não depende) e §14 (auto-atribui só o criador-dev; nota "Overlap futuro" da task 71 convertida em regra vigente). **Verificação:** `npm run build --workspace=backend` (nest build) OK — type-check cobre o TS-fonte do `shared` (sem script de build próprio); `grep gestorIds|criadorId|\.membroIds` no shared/backend só retorna a variável local `membroIds` do service e o nome do DTO/import; `listarGestoresAtivos` não é mais usado no módulo demanda. **Verificações de runtime 3-8 da spec** (gestor sem linhas em `demanda_usuario`, dev com 1 linha, listagem por status/tipo, atividade gestor sem 403) não executadas nesta sessão (sem DB up) — apenas build/checagens estáticas.
**Task anterior:** 70-padronizacao-nomes-constraints — formaliza a convenção de nomes de objetos de banco e alinha as **duas** constraints que fugiam do padrão `chk_` (achado A4 da auditoria de consistência). **Migration NOVA** `20240020_padronizar_nomes_constraints.ts` — `up`: dois `ALTER TABLE … RENAME CONSTRAINT` (não drop/recreate, preserva a definição) — `demanda.demanda_status_check → chk_demanda_status` e `dia_nao_util.ck_dia_nao_util_duracao → chk_dia_nao_util_duracao`; `down`: reverte os dois nomes. **Migrations já aplicadas (`20240013`/`20240014`) não editadas.** **Doc** — `SCHEMA.md`: novo bloco "Nomenclatura de objetos de banco" na seção "Convenções deste Schema" (tabela de prefixos `pk_`/`fk_`/`uix_`/`ix_`/`chk_`/`trg_`/`fn_`, padrão `<prefixo>_<tabela>_<proposito>`, genéricos em inglês), constraint do `dia_nao_util` atualizada para `chk_dia_nao_util_duracao` (CREATE TABLE + nota), `demanda.status` ganhou `CONSTRAINT chk_demanda_status` explícita no CREATE TABLE (antes anônima), e histórico de migrations citando `20240020`. `CONVENTIONS.md`: tabela de prefixos + par ✅/❌ na seção SQL (junto da regra de idioma de objetos genéricos; `ck_` proibido, nome auto-gerado pelo PostgreSQL proibido). `CONTEXT.md`: tabela "Banco de Dados" (migration `20240020`). **Nota de número:** a spec citava `20240017` nas seções de doc/verificação (número obsoleto, já usado por `seed_tags_padrao`); usado `20240020` — próximo número livre, conforme a própria seção de Escopo da spec e a tabela do CONTEXT. **Sem mudança de schema/colunas/comportamento** — apenas nomes de constraint + documentação. **Verificação:** `npm run build --workspace=backend` (nest build) OK; `db:migrate` aplicou (Batch 9) e no banco vivo `\d` confirma só `chk_demanda_status` + `chk_dia_nao_util_duracao` (antigos sumiram); `db:rollback` restaurou `demanda_status_check` + `ck_dia_nao_util_duracao` e `db:migrate` reaplicou para o estado final; `grep "demanda_status_check\|ck_dia_nao_util_duracao" backend/src` retorna só as migrations `20240013`/`20240014`/`20240020` (criação/rename), nenhum código de aplicação.
**Task anterior:** 69-reconciliacao-documental — **task puramente documental** (não toca código, DTO, migration ou comportamento; só `docs/`). Reconciliou os documentos-fonte com o estado real/aprovado do código. **`SYSTEM.SPEC.md`** — §4 (exemplo de enum trocou `EM_DESENVOLVIMENTO` removido por `DESENVOLVENDO`); §5.4 (`TipoDemandaStatusEnum` → `PENDENTE, PLANEJADA, CONCLUIDA`); §6.2 (lista de enums + `dia-nao-util-duracao.enum.ts`); §8.5 (tema **claro/escuro** habilitado via `darkModeSelector: '.app-escuro'` + `TemaService`/toggle; regra de cores reescrita p/ aliases `--app-surface-*`, não `--p-surface-N` direto); §12 (ponto: diário/mensal/todos + fração de meta; execucao: registro manual gestor-only + edição altera datas); §13 (Projeto `codigo` derivado do nome; Demanda `status`; DiaNaoUtil `duracao`; Execucao edição cobre datas); §14 (matriz de permissão real da demanda — dev só cria sub-demanda, vê qualquer demanda de projeto acessível mas só edita se membro, `descricaoCliente` gestor-only, auto-inclusão/remoção como membro, último membro protegido, ref. à task 71); E2 (numeração: `### 8.1`→`7.1`, `### 10.4`→`9.4`). **`SCHEMA.md`** — histórico de migrations `20240013`–`20240019`; `demanda.status` CHECK (`PENDENTE, PLANEJADA, CONCLUIDA`); `dia_nao_util.duracao` + constraint `ck_dia_nao_util_duracao` (nota: task 70 padroniza p/ `chk_`). **`CONTEXT.md`** — task 45 encerrada (toggle habilitado); tabela de migrations atualizada; entradas obsoletas dos aliases `AtividadeAtualizadaDto`/`ExecucaoAtualizadaDto` marcadas como histórico. **`AUDITORIA.md`** — errata (relatório de 2026-06-19, anterior às specs 45/46 e migrations 15/16; C1→task 63, C2→task 62). **Verificação:** `grep EM_DESENVOLVIMENTO docs/SYSTEM.SPEC.md docs/SCHEMA.md` vazio (só backlog/done/AUDITORIA históricos); numeração de seções do SPEC contínua (§1–§16, sem `8.1` no cap. 7 nem `10.4` no cap. 9). **Nada fora de `docs/` foi tocado.**
**Task anterior:** ajuste-execucao-foco-descricao-e-remocao-tela-ativa — **frontend apenas**, sem spec numerada (ajuste pós-68). **(1) Autofocus na Descrição** nos dialogs de execução que abriam focando o campo **Início**: o PrimeNG `p-dialog` foca o **primeiro elemento focável em ordem de DOM** (o `p-datepicker` de início), ignorando ordem lógica/atributo `autofocus` (confirmado no fonte do `Dialog._focus` → `DomHandler.getFocusableElements`, que **não** prioriza `[autofocus]`). Corrigido nos dois dialogs com `[focusOnShow]="false"` + `(onShow)="…"` chamando um método que dá `.focus()` na textarea de descrição via `@ViewChild` (`ElementRef<HTMLTextAreaElement>`): **Editar execução** (`ExecucaoHistoricoPage`, ref `#descricaoEdicao` → `focarDescricaoEdicao()`) e **Registrar execução** manual da tela de Atividades (`AtividadeListagemPage`, ref `#descricaoRegistro` → `focarDescricaoRegistro()`; imports `ViewChild`/`ElementRef` adicionados). O dialog *Iniciar/Encerrar execução* da mesma tela já tinha a descrição como 1º campo focável (sem mudança). **(2) Remoção da tela órfã `ExecucaoAtivaPage`** — a rota `/execucao/ativa` e a página (`modules/execucao/pages/execucao-ativa/` — `.ts`/`.html`/`.scss`) foram **deletadas**: o fluxo iniciar/encerrar já havia migrado para a tela de Atividades (play/pause + dialogs) no ajuste pós-27/fase 3, e a página não era referenciada por nenhum `routerLink`/`router.navigate` nem pelo menu da topbar (o item "Execuções" aponta para `/execucao` = histórico). **Service e controller de execução mantidos intactos** (decisão do usuário — uso futuro): `ExecucaoService` (front/back), `ExecucaoController`, repository e DTOs (`ExecucaoAtivaDto` etc.) preservados; só a tela/rota saíram. **Verificação:** `tsc -p tsconfig.app.json --noEmit` (frontend) OK nas duas etapas; `grep execucao-ativa|ExecucaoAtivaPage` no código não retorna mais nada (só o DTO `ExecucaoAtivaDto` e specs/docs históricos).
**Task anterior:** 68-atividade-gestor-editar-nome — no dialog de **visualização de atividade** (`AtividadeVisualizarDialogComponent`), o **gestor** passa a poder **editar o nome** da atividade **inline no título** (para corrigir nome digitado errado pelo desenvolvedor). **Frontend apenas** — sem backend/shared/migration (o `AtividadeService.alterar` backend já aceita `nome` e libera gestor por spec 46; `AtividadeAlterarDto.nome` já tem `@MinLength(3) @MaxLength(255)`). **`.ts`** — novos imports `output`/`Validators` e `InputTextModule`; injetada `sessao: UsuarioSessaoService` (pública, p/ `sessao.eGestor()` no template); estado `editandoNome`/`salvandoNome` signals + `formularioNome` (`nome` required/min3/max255) + output `aoAlterar`; `abrir()` reseta `editandoNome` p/ `false` (junto dos demais resets); métodos `iniciarEdicaoNome()` (preenche o form com o nome atual e entra em edição), `cancelarEdicaoNome()` e `salvarNome()` (trim → `alterar(id, { nome })`, no sucesso faz `atividadeVisualizada.update(... nome ...)`, fecha edição, toast "Nome alterado", emite `aoAlterar`) — espelha o padrão do `salvarDescricao()` existente, **sem tocá-lo**. **`.html`** — `p-dialog` deixou de usar `[header]` string e passou a usar `<ng-template #header>` (PrimeNG 21): **modo leitura** (`@if (!editandoNome())`) com `__titulo` + botão lápis `__editar-nome` (`@if (sessao.eGestor() && atividadeVisualizada())` → `iniciarEdicaoNome()`); **modo edição** (`@else`) com `form [formGroup]="formularioNome"` (ngSubmit→salvarNome), `input pInputText` autofocus + `p-button` ✔ (submit, `[loading]="salvandoNome()"`, `[disabled]="formularioNome.invalid || salvandoNome()"`) e ✗ (`cancelarEdicaoNome()`), mais `__nome-erro` quando inválido e touched/dirty. O X de fechar do dialog é separado do template de header (intacto). **`.scss`** — classes BEM novas `&__header`, `&__titulo`, `&__editar-nome` (hover usa `--app-realce-sutil`/`--p-primary-color`), `&__nome-form`, `&__nome-linha`, `&__nome-input`, `&__nome-acoes`, `&__nome-erro` (`--p-red-500`, padrão dos mixins `_form`); tokens compatíveis com tema claro/escuro. **`AtividadeListagemPage` (.html)** — `<app-atividade-visualizar-dialog #visualizarDialog (aoAlterar)="buscarAtividades()" />` (refresh **só** na listagem; histórico de execução e ponto **não** assinam o output — exibem o nome novo só ao reentrar, decisão de escopo da spec). **Não tocado:** descrição/abas, demais campos do header (status/executor/tags), backend/shared, e a permissão do desenvolvedor (lápis só p/ gestor; backend é a fonte da verdade). **Verificação:** `npm run build --workspace=frontend` OK (só warnings pré-existentes de budget/CommonJS).
**Task anterior:** 57-remover-ordem-exibicao — removida por completo a coluna `ordem_exibicao` das tabelas **demanda** e **atividade** e todo o seu rastro; o campo era preenchido manualmente e não tinha mais utilidade prática — a ordenação dessas entidades passa a ser **sempre por nome**. **Migration NOVA** `20240019_remover_ordem_exibicao.ts` — `up`: `ALTER TABLE ... DROP COLUMN IF EXISTS ordem_exibicao` nas duas tabelas; `down`: recria no padrão da `20240014` (sem `DEFAULT`): `ADD COLUMN INTEGER` → `UPDATE ... = 0` → `SET NOT NULL`. **Backend** — `DemandaRepository`: removido `ordem_exibicao`/`ordemExibicao` do INSERT, do RETURNING (`inserir`), dos SELECTs (`recuperar`, `listar`, RETURNING do `alterar`) e do ramo do SET dinâmico; os dois `ORDER BY demanda.ordem_exibicao ASC, demanda.nome ASC` (`listar`) e `ORDER BY demanda.ordem_exibicao ASC` (`recuperarGrafo`) viraram `ORDER BY demanda.nome ASC`. `AtividadeRepository`: idem no INSERT/RETURNING/SELECTs/SET — a `listar` **já** ordenava por `usuario.nome_completo, created_date, nome` (nem usava o campo), `ORDER BY` intocado. `DemandaService.criar`/`alterar` e `AtividadeService.criar`/`alterar` deixaram de repassar `ordemExibicao`; models `demanda.model.ts`/`atividade.model.ts` perderam o campo. **Shared (13 DTOs)** — removido `ordemExibicao` de `Demanda{Criar,Alterar,Criada,InternoAlterar,Resumo,Recuperada}Dto` e `Atividade{Criar,Alterar,Criada,Alterada,Resumo,InternoAlterar,Recuperada}Dto`; em `AtividadeAlterarDto` os imports `IsNumber`/`Min` ficaram órfãos e foram removidos; comentário do `DemandaInternoAlterarDto` atualizado ("onze"→"dez" campos). **Frontend** — campo "Ordem de Exibição/exibição" (label + `p-inputnumber` + form control + reset/patch + campo no DTO) removido de `demanda-formulario-dialog` (eram **dois** blocos de `reset` com indentação diferente), `demanda-edicao-dialog` e `atividade-formulario.page`; neste último o `InputNumberModule` ficou órfão e saiu dos imports; `atividade-listagem.page` deixou de enviar `ordemExibicao: 0` na criação rápida. `InputNumberModule` **mantido** nos dois dialogs de demanda (ainda usado em Horas Estimadas). **Doc** — `SCHEMA.md` (os dois `CREATE TABLE`) e `SYSTEM.SPEC.md` (as duas linhas de tabela, incl. a nota "ordenação em listas e kanban") sem a coluna; spec `57-remover-ordem-exibicao.spec.md` em `done`. As notas históricas do CONTEXT (tasks 50/51 e ajuste pós-27) que citam o campo permanecem como registro. **Verificação:** `npm run build --workspace=backend` (nest build) e `npm run build --workspace=frontend` OK (só warnings pré-existentes de budget/CommonJS); `db:migrate` aplicou (Batch 8) e `db:rollback`+`db:migrate` reverteu/re-aplicou sem erro; `grep -ri "ordemExibicao\|ordem_exibicao"` em `backend/src`/`frontend/src`/`shared/src` só retorna a migration `20240019` e os dois `CREATE TABLE` originais (`20240005`/`20240009`, intocados por serem migrations já aplicadas).
**Task anterior:** projeto-criar-em-dialog — a criação de novo projeto deixou de ser uma **tela** (`/projeto/novo`) e passou a abrir um **dialog**, alinhando ao padrão do sistema (mesmo padrão imperativo de `UsuarioFormularioDialogComponent` da task `usuario-criar-em-dialog`). **Frontend apenas** (sem backend/shared/migration). **Novo** `ProjetoFormularioDialogComponent` (`modules/projeto/components/projeto-formulario-dialog/`) absorveu **toda** a lógica de `ProjetoFormularioPage`: o mesmo `formulario` (nome `maxLength(255)`, codigo `maxLength(50)`, cor pattern hex default `#3b82f6`, status default `ATIVO`, inicioData/previsaoFimData + cross-validator `validarPrevisaoFim`), a derivação **nome → código** (`gerarCodigoDoNome`, signal `codigoEditadoManualmente`, com os dois `valueChanges` movidos de `ngOnInit` para o `constructor` por causa do `takeUntilDestroyed`) e o mesmo `salvar()` (`ProjetoService.criar` + `formatarData`), agora num `p-dialog` (`header="Novo Projeto"`, `width 42rem`, footer Cancelar/Salvar; `p-select` e os dois `p-datepicker` com `appendTo="body"`). Expõe método imperativo `abrir()` (reseta o form com `cor:'#3b82f6'`/`status:ATIVO` + `codigoEditadoManualmente.set(false)` e abre) e output **`aoCriar = output<number>()`** que emite o **id do projeto criado** — preservando o comportamento original de ir direto ao detalhe (`resposta.dados.id`). SCSS via mixins de `shared/styles/_form` (`form-dialog-form`/`form-dialog-linha`/`form-campo`/`form-label`/`form-obrigatorio`/`form-erro`) + `&__dica`/`&__input-codigo`. **`ProjetoListagemPage`** — botão "Novo Projeto" troca `navegarParaNovo()` (removido) por `formularioDialog.abrir()`; novo handler `aoCriarProjeto(projetoId)` que faz `router.navigate(['/projeto', projetoId])` (Router mantido — ainda usado em `navegarParaDetalhe`); template ganhou `<app-projeto-formulario-dialog #formularioDialog (aoCriar)="aoCriarProjeto($event)" />` junto ao `p-confirmDialog`. **Removida** a rota `novo` de `projeto.routes.ts` (e o import `gestorGuard`, que só ela usava — a restrição a gestor segue no `@if (sessao.eGestor())` do botão) e **deletada** `ProjetoFormularioPage` (.ts/.html/.scss + diretório `pages/projeto-formulario/`). Nenhuma outra referência a `/projeto/novo` no código (só docs históricos). **Verificação:** `npm run build --workspace=frontend` OK (só warnings pré-existentes de budget/CommonJS).
**Task anterior (mesma sessão):** 56-execucao-correcao-padroes — última spec da auditoria 44. **(1) Nomenclatura (§5, errata `AUDITORIA.md` §8):** os dois DTOs internos de execucao tinham o verbo no meio do complemento (forma ❌ do §5). Renomeados (arquivo + classe, campos idênticos): `ExecucaoAlterarInternoDto` → **`ExecucaoInternoAlterarDto`** e `ExecucaoEncerrarInternoDto` → **`ExecucaoInternoEncerrarDto`**; barrel `shared/src/dtos/execucao/index.ts` e os usos no `ExecucaoRepository` (`alterar(dto)`/`encerrar(dto)`) atualizados. **(2) Boundary controller→service DTO-only (decisão "DTO em tudo", spec 48):** `ExecucaoService` não recebe mais `id` solto — `recuperar(dto: ExecucaoRecuperarDto, usuarioAtivo)` e `excluir(dto: ExecucaoRecuperarDto, usuarioAtivo)` reutilizam `{ id }`; `encerrar(dto: ExecucaoEncerrarDto & { id }, usuarioAtivo)` e `alterar(dto: ExecucaoAlterarDto & { id }, usuarioAtivo)` recebem o DTO público + `id` (intersection, mesmo padrão do `alterarSenha` da spec 48 — não foi adicionado `id?` aos DTOs públicos); `ExecucaoController` monta `{ id }` / `{ ...dto, id }` a partir de `@Param('id')`. **(3) `ExecucaoRepository.listar` — primitivo de restrição de escopo:** novo `ExecucaoAcessoFiltrarDto { usuarioId }` (espelha `DemandaAcessoFiltrarDto`); `listar(filtros, restricao?: ExecucaoAcessoFiltrarDto)` (era `usuarioIdRestricao?: number`); SQL inalterado. **Callers atualizados:** `ExecucaoService.listar` (`{ usuarioId: sub }`) e **`PontoService.montarPontoDiario`** (`{ usuarioId: usuario.id }` — caller não citado na spec, mas obrigatório p/ compilar). **Sem mudança de campos/SQL/comportamento/autorização.** **Doc** — `SYSTEM.SPEC.md` §5 (novo bloco: quando `Interno` é o único complemento, mesma ordem verbo-no-fim, com par ✅/❌ de execucao) e `CONVENTIONS.md` (seção DTOs: duas linhas ✅ `ExecucaoInternoAlterarDto`/`ExecucaoInternoEncerrarDto` / ❌ formas antigas). **Verificação:** `npm run build --workspace=backend` (nest build) OK — type-check cobre o TS-fonte do `shared` (sem script de build próprio); `grep "AlterarInternoDto\|EncerrarInternoDto"` em `shared`/`backend/src` vazio; nenhum `*AlterarInternoDto.ts`/`*EncerrarInternoDto.ts` em `shared/src/dtos/execucao/`; em `ExecucaoService` os únicos `id: number` são as intersections `ExecucaoEncerrarDto & { id: number }`/`ExecucaoAlterarDto & { id: number }` (DTO carregando id, não primitivo solto). **Lote da auditoria 44 encerrado** (8 specs 48-56 todas concluídas).
**Task anterior (mesma sessão):** 55-core-correcao-padroes — correção do desvio de linguagem do achado §4.8 da `docs/AUDITORIA.md`: a função genérica de BaseEntity `fn_atualizar_updated_date()` (idioma misto: verbo `atualizar` em português + `updated_date` em inglês) viola o §16 #12 (conceito genérico/arquitetural nunca em português). **Renomeada para `fn_set_updated_date()` (inglês) via migration NOVA** — `20240016_renomear_funcao_updated_date.ts` (migrations já aplicadas **não** editadas in-place; nome `fn_set_updated_date` fixado pelo SPEC/`SCHEMA.md`). **`up`:** cria `fn_set_updated_date()` (mesmo corpo `NEW.updated_date = NOW(); RETURN NEW;`), itera as **11 tabelas** com trigger de BaseEntity (`usuario, projeto, tag, demanda, demanda_usuario, demanda_conexao, demanda_tag, atividade, atividade_tag, execucao, dia_nao_util`) fazendo `DROP TRIGGER IF EXISTS trg_<tabela>_updated_date` + `CREATE TRIGGER ... EXECUTE FUNCTION fn_set_updated_date()`, e ao final `DROP FUNCTION IF EXISTS fn_atualizar_updated_date() CASCADE` (seguro: nenhum trigger ainda depende dela). **`down`:** recria a função antiga, reaponta os 11 triggers para ela e dropa `fn_set_updated_date()`. Os **nomes dos triggers já eram inglês** (`trg_<tabela>_updated_date`) — só a função tinha verbo pt; ainda assim os triggers são recriados para apontar à nova função. **Doc** — `SYSTEM.SPEC.md` §4 (Exemplos Concretos: par ✅ `fn_set_updated_date()` / ❌ `fn_atualizar_updated_date()` + nota "objetos de banco genéricos em inglês, tabelas/colunas de negócio em português") e §9.2 (nova regra SQL #12); `CONVENTIONS.md` (seção SQL → Nomes: bloco "Objetos genéricos de banco: inglês" com exemplos ✅/❌). `SCHEMA.md` já documentava `fn_set_updated_date` — nome da migration confere exatamente. **Sem mudança de código de aplicação** (services/repositories/DTOs intocados — o nome da função só aparece nas migrations). **Verificação:** `npm run build --workspace=backend` (nest build) OK; `db:migrate` aplicou (Batch 5, 1 migration) e `db:rollback` reverteu sem erro, re-aplicado em seguida; checagem no banco vivo — só `fn_set_updated_date` existe (antiga sumiu), os **11** triggers apontam para ela, e `UPDATE` em `usuario` sobrescreveu `updated_date='2000-01-01'` com NOW() (trigger ativo). Resta só a spec `56-execucao` da auditoria.
**Task anterior (mesma sessão):** 54-ponto-correcao-padroes — fechamento da **lacuna de documentação** dos DTOs de **saída** do módulo **ponto** (achado §4.7 da `docs/AUDITORIA.md` / categoria B §5.1: DTOs de relatório/value-object fora do padrão verbo-particípio, num ponto em que o SPEC não documentava a convenção). **Decisão (opção a): nenhum rename** — DTOs de relatório/consulta computada usam `Entidade + Recorte + Dto` (sem verbo) e value-objects usam o nome do conceito (sem entidade nem verbo); ambos são **isentos** do verbo no particípio. Os nomes atuais permanecem: `PontoDiarioDto`, `PontoMensalDto` (relatórios) e `IntervaloDto` (value-object `{ inicioData, fimData, duracaoMinutos }`). `PontoDiaResumoDto` (item de lista, sufixo `ResumoDto`) e os DTOs de entrada `*ConsultarDto` já estavam conformes — não tocados. **Sem mudança de código** — `PontoService` (backend/frontend), componentes de ponto, barrels e DTOs intocados; a task é puramente de confirmação documental. **Doc** — `SYSTEM.SPEC.md` §5.1 (subseções "DTOs de relatório / consulta computada" e "Value-objects / sub-estruturas") e `CONVENTIONS.md` (seção DTOs) **já continham** a regra e os exemplos ✅; esta sessão confirmou a consistência (lacuna fechada, sem necessidade de edição adicional). **Verificação:** `npm run build --workspace=backend` (nest build — type-check cobre o TS-fonte do `shared`, que não tem script de build próprio, como nas specs 48-53) e `npm run build --workspace=frontend` OK (só warnings pré-existentes de budget/CommonJS); confirmado que os 7 arquivos em `shared/src/dtos/ponto/` (3 de saída + `PontoDiaResumoDto` + 3 `*ConsultarDto`) e o barrel `index.ts` seguem a convenção documentada. Resta a spec `55-core` da auditoria.
**Task anterior (mesma sessão):** 53-tag-correcao-padroes — correção de padrões do módulo **tag** (achado §4.6 da `docs/AUDITORIA.md`: primitivo `id` + `Partial<Tag>` em `TagRepository.alterar`) + boundary controller→service DTO-only (decisão "DTO em tudo" da spec 48). **Sem mudança de comportamento/validação/SQL/autorização — apenas assinatura/nomenclatura.** **Shared** — novo `TagInternoAlterarDto` (`{ id, nome?, cor? }` — espelha os campos do SET dinâmico do `alterar`; verbo `Alterar` no fim §5, nunca `TagAlterarInternoDto`); barrel `shared/src/dtos/tag/index.ts` atualizado. **Repository** — `alterar(dto: TagInternoAlterarDto)` (era `alterar(id: number, dados: Partial<Tag>)` — eliminado primitivo + `Partial<Model>`; `dto.id` no WHERE, demais no SET; SQL inalterado; JSDoc mantido; `import Tag` permanece — usado em `extends BaseRepository<Tag>`). **Service** — boundary DTO-only, nenhum método recebe `id` solto: `recuperar(dto: TagRecuperarDto)` e `excluir(dto: TagRecuperarDto)` (reutilizam `{ id }` de recuperação — não criado DTO novo de exclusão, conforme spec; `TagExcluirDto` segue só no repositório), `alterar(dto: TagInternoAlterarDto)` (monta `{ id: dto.id, nome, cor }` ao chamar o repositório; `validarNome` usa `idExcluir: dto.id`); `import TagAlterarDto` removido do service por desuso (segue no controller, é o `@Body`), `import TagRecuperarDto` acrescentado. **Controller** — monta o DTO de `@Param('id')`: `recuperar({ id })`, `alterar({ ...dto, id })`, `excluir({ id })`; guards/`@GestorOnly`/Swagger inalterados; `criar`/`listar` (sem `id` de rota) intocados. **Doc** — `CONVENTIONS.md` (Camadas → Repository): acrescentado o par `alterar(id, Partial<Tag>)` ❌ → `alterar(dto: TagInternoAlterarDto)` ✅ à enumeração "todo módulo". **Verificação:** `npm run build --workspace=backend` (nest build) OK — type-check cobre o TS-fonte do `shared` (sem script de build próprio, como nas specs 48-52); checagens negativas (grep): nenhum `id: number`/`Partial<` em `tag.repository.ts`, nenhum `id: number` solto em `tag.service.ts`. **Frontend não tocado** (o `TagService` Angular usa `id` na URL — fora do §16 #21). Restam as specs `54-ponto`, `55-core` da auditoria.
**Task anterior (mesma sessão):** 52-calendario-correcao-padroes — correção de padrões do módulo **calendario** (achado §4.5 da `docs/AUDITORIA.md`: primitivo `id` + `Partial<DiaNaoUtil>` em `CalendarioRepository.alterar`) + boundary controller→service DTO-only (decisão "DTO em tudo" da spec 48). **Sem mudança de comportamento/validação/SQL/autorização — apenas assinatura/nomenclatura.** **Shared** — novo `CalendarioInternoAlterarDto` (`{ id, descricao?, tipo?, duracao?, recorrente? }` — espelha os campos do SET dinâmico do `alterar`; verbo `Alterar` no fim §5, nunca `CalendarioAlterarInternoDto`); barrel `shared/src/dtos/calendario/index.ts` atualizado. **Repository** — `alterar(dto: CalendarioInternoAlterarDto)` (era `alterar(id: number, dados: Partial<DiaNaoUtil>)` — eliminado primitivo + `Partial<Model>`; `dto.id` no WHERE, demais no SET; SQL inalterado; JSDoc mantido; `import DiaNaoUtil` permanece — usado em `extends BaseRepository<DiaNaoUtil>`). **Service** — boundary DTO-only, nenhum método recebe `id` solto: `recuperar(dto: CalendarioRecuperarDto)` e `excluir(dto: CalendarioRecuperarDto)` (reutilizam `{ id }` de recuperação — não criado DTO novo de exclusão, conforme spec; `CalendarioExcluirDto` segue só no repositório), `alterar(dto: CalendarioInternoAlterarDto)` (monta `{ id: dto.id, descricao, tipo, duracao, recorrente }` ao chamar o repositório); `import DiaNaoUtilAlterarDto` removido do service por desuso (segue no controller, é o `@Body`). **Controller** — monta o DTO de `@Param('id')`: `recuperar({ id })`, `alterar({ ...dto, id })`, `excluir({ id })`; guards/`@GestorOnly`/Swagger inalterados; `criar`/`listar`/`verificarDiaUtil` (sem `id` de rota) intocados. **Doc** — `CONVENTIONS.md` (Camadas → Repository): acrescentado o par `alterar(id, Partial<DiaNaoUtil>)` ❌ → `alterar(dto: CalendarioInternoAlterarDto)` ✅ à enumeração "todo módulo". **Verificação:** `npm run build --workspace=backend` (nest build) OK — type-check cobre o TS-fonte do `shared` (sem script de build próprio, como nas specs 48-51); checagens negativas (grep): nenhum `id: number`/`Partial<` em `calendario.repository.ts`, nenhum `id: number` solto em `calendario.service.ts`. **Frontend não tocado** (o `CalendarioService` Angular usa `id` na URL — fora do §16 #21). Restam as specs `53-tag`, `54-ponto`, `55-core` da auditoria.
**Task anterior (mesma sessão):** 51-atividade-correcao-padroes — correção de padrões do módulo **atividade** (achado §4.4 da `docs/AUDITORIA.md`: primitivo `id` + `Partial<Atividade>` em `AtividadeRepository.alterar`) + boundary controller→service DTO-only (decisão "DTO em tudo" da spec 48). **Sem mudança de comportamento/validação/SQL/autorização — apenas assinatura/nomenclatura.** **Shared** — novo `AtividadeInternoAlterarDto` (`{ id, nome?, descricao?, status?, ordemExibicao? }` — espelha os campos do SET dinâmico do `alterar`; verbo `Alterar` no fim §5, nunca `AtividadeAlterarInternoDto`); `AtividadeTagsAtribuirDto` ganhou `id?: number` opcional (`@IsOptional @IsNumber`, injetado pela controller a partir de `@Param('id')` — não chega pelo `@Body`, então `whitelist`/`forbidNonWhitelisted` não rejeita); barrel `shared/src/dtos/atividade/index.ts` atualizado. **Repository** — `alterar(dto: AtividadeInternoAlterarDto)` (era `alterar(id: number, dados: Partial<Atividade>)` — eliminado primitivo + `Partial<Model>`; `dto.id` no WHERE, demais no SET; SQL inalterado; JSDoc mantido). **Service** — boundary DTO-only, nenhum método recebe `id`/`*Id` solto: `recuperar(dto: AtividadeRecuperarDto, usuarioAtivo)`, `excluir(dto: AtividadeRecuperarDto)`, `listarTags(dto: AtividadeRecuperarDto, usuarioAtivo)` (reutilizam `{ id }`), `alterar(dto: AtividadeInternoAlterarDto, usuarioAtivo)` (repassa o dto ao repositório), `alterarTags(dto: AtividadeTagsAtribuirDto, usuarioAtivo)` (lê `dto.id!` numa const `atividadeId` local; `import AtividadeAlterarDto` trocado por `AtividadeRecuperarDto`+`AtividadeInternoAlterarDto`). **Controller** — monta o DTO de `@Param('id')`: `recuperar({ id }, …)`, `alterar({ ...dto, id }, …)`, `excluir({ id })`, `alterarTags({ ...dto, id }, …)`, `listarTags({ id }, …)`; guards/`@GestorOnly`/`@ActiveUser`/Swagger inalterados; `criar`/`listar` (sem `id` de rota) intocados. **Doc** — `CONVENTIONS.md` (Camadas → Repository): acrescentado o par `alterar(id, Partial<Atividade>)` ❌ → `alterar(dto: AtividadeInternoAlterarDto)` ✅ à enumeração "todo módulo" (a generalização da spec 50 já cobria via "e assim por diante"; SPEC §7.4/§16 #21 não duplicados). **Verificação:** `npm run build --workspace=backend` (nest build) OK — type-check cobre o TS-fonte do `shared` (sem script de build próprio, como nas specs 48-50); checagens negativas (grep): nenhum `id: number`/`Partial<` em `atividade.service.ts`/`atividade.repository.ts` — só os `@Param('id') id: number` da controller (esperado). **Frontend não tocado** (o `AtividadeService` Angular usa `id` na URL — fora do §16 #21).
**Task anterior (mesma sessão):** usuario-listagem-busca — a listagem de usuários (`UsuarioListagemPage`) ganhou um **filtro de busca por nome/login**, no padrão `busca` já usado em `AtividadeListagemPage`. **Shared** — `UsuarioListarDto` ganhou `busca?: string` (`@IsOptional @IsString`). **Backend** — `UsuarioRepository.listar` ganhou a condição `(usuario.nome_completo ILIKE :busca OR usuario.login ILIKE :busca)` com `parametros.busca = '%' + filtros.busca.trim() + '%'` (só quando `busca` definido e não-vazio; aplicada nas duas queries — COUNT e itens — por compartilharem `clausulaWhere`). **Frontend** — `formularioFiltros` ganhou controle `busca` (1º campo); `ngOnInit` assina `busca.valueChanges` com `debounceTime(400)`+`distinctUntilChanged()`+`takeUntilDestroyed(destroyRef)` → `aoMudarFiltro()` (reseta p/ página 1); `buscarUsuarios` repassa `busca.trim()` ao filtro; novo `<input pInputText formControlName="busca">` (placeholder "Nome ou login") com `InputTextModule` importado e SCSS `&__filtro-campo--busca { flex: 1 }`. **`UsuarioService.listar`** passou a setar `busca` no `HttpParams` (`if (filtros.busca) params = params.set('busca', filtros.busca)`) — **sem isto o termo nunca chegava à API** (bug pego na verificação por API: backend filtrava certo, mas o service descartava o param; corrigido no padrão dos params `tipo`/`status`). **Verificação (runtime):** `GET /api/v1/usuario?busca=…` na API viva confirmou filtragem por nome **e** login, case-insensitive (`SILVA`), `busca` vazio/sem match tratados (todos / lista vazia); proxy do dev-server (:4200) repassa `busca` ao backend. Builds backend/frontend OK.
**Task anterior (mesma sessão):** usuario-editar-tipo — a edição de usuário passou a permitir **reclassificar o tipo** (Desenvolvedor ↔ Gestor); antes `tipo` só era definido na criação. **Shared + backend + frontend** (sem migration — a coluna `tipo` já existe). **Shared** — `UsuarioAlterarDto` (`@Body`) ganhou `tipo?` (`@IsOptional @IsEnum(UsuarioTipoEnum)`) e `UsuarioInternoAlterarDto` ganhou `tipo?: UsuarioTipoEnum` (espelha o novo SET). **Repository** — `UsuarioRepository.alterar` ganhou o ramo `if (dto.tipo !== undefined) { SET tipo = :tipo }` no SET dinâmico (RETURNING já trazia `tipo`). **Service (gate de segurança)** — `UsuarioService.alterar`: como `PUT /usuario/:id` **não** é `@GestorOnly` (desenvolvedor edita o próprio perfil), foi adicionado um gate **field-level** no padrão da task 39 (`descricaoCliente`): `if (usuarioAtivo.tipo === DESENVOLVEDOR && dto.tipo !== undefined) → UnauthorizedAccessException('Apenas gestores podem alterar o tipo de usuário')` — impede auto-promoção do desenvolvedor a gestor. **Frontend `UsuarioPerfilDialogComponent`** (dialog "Editar Perfil") — `formularioEditar` ganhou controle `tipo` (`Validators.required`, default `DESENVOLVEDOR`), patch de `usuarioDados.tipo` em `abrirDialogEditar`; novo `tiposOpcoes` e `computed podeEditarTipo` (= `eGestor()` **e** perfil ≠ próprio usuário — ver guardrails); o `<p-select>` de Tipo (full-width, `appendTo="body"`) é renderizado só em `@if (podeEditarTipo())` e o DTO só inclui `dto.tipo` quando `podeEditarTipo()` — desenvolvedor editando o próprio perfil nunca vê nem envia `tipo` (sem 403). **Guardrails de rebaixamento (`UsuarioService.alterar`, `BusinessException`)** — calculado `estaRebaixandoGestor = usuarioEncontrado.tipo === GESTOR && dto.tipo === DESENVOLVEDOR`; quando verdadeiro: (1) **auto-rebaixamento bloqueado** — `usuarioAtivo.sub === dto.id` → `'Um gestor não pode rebaixar a si mesmo'`; (2) **último gestor preservado** — `listarGestoresAtivos()` (reusado) filtrado por `id !== dto.id`; se `length === 0` → `'O sistema deve ter ao menos um gestor ativo'`. No front, o item (1) é prevenido escondendo o campo Tipo no próprio perfil (`podeEditarTipo` exclui self); o item (2) depende da validação do servidor (toast de erro). **Lacuna conhecida (não pedida):** o caminho de **status → INATIVO** ainda não tem guarda de "último gestor" (pode inativar o último gestor sem o tipo mudar) — consistente com o comportamento pré-existente do `status`. **Verificação:** `npm run build --workspace=backend` (nest build) OK; `npm run build --workspace=frontend` OK (só warnings pré-existentes de budget/CommonJS).
**Task anterior (mesma sessão):** usuario-criar-em-dialog — a criação de novo usuário deixou de ser uma **tela** (`/usuario/novo`) e passou a abrir um **dialog**, alinhando ao padrão do sistema (mesmo padrão imperativo de `UsuarioPerfilDialogComponent`/`UsuarioAnotacoesDialogComponent` da task 47). **Frontend apenas** (sem backend/shared/migration). **Novo** `UsuarioFormularioDialogComponent` (`modules/usuario/components/usuario-formulario-dialog/`) absorveu **toda** a lógica de `UsuarioFormularioPage`: o mesmo `formulario` (login `minLength(4)`, senha `minLength(8)`, nomeCompleto `minLength(3)`, cargoTitulo, tipo, horasDiariasNecessarias=8) e o mesmo `salvar()` (`UsuarioService.criar`), agora num `p-dialog` (`header="Novo Usuário"`, `width 40rem`, footer Cancelar/Salvar; `p-select` do tipo com `appendTo="body"`). Expõe método imperativo `abrir()` (reseta o form com `horasDiariasNecessarias:8`/`tipo:null` e abre) e output `aoCriar` (em sucesso: toast + fecha + emite). SCSS via mixins de `shared/styles/_form` (`form-dialog-form`/`form-dialog-linha`/`form-campo`/`form-label`/`form-obrigatorio`/`form-erro`). **`UsuarioListagemPage`** — botão "Novo Usuário" troca `navegarParaNovo()` (removido, junto com `Router` e o import `@angular/router`) por `formularioDialog.abrir()`; template ganhou `<app-usuario-formulario-dialog #formularioDialog (aoCriar)="buscarUsuarios()" />`. **Removida** a rota `novo` de `usuario.routes.ts` e **deletada** `UsuarioFormularioPage` (.ts/.html/.scss + diretório `pages/usuario-formulario/`). Nenhuma outra referência a `/usuario/novo` no código (só docs históricos: spec done 24 e notas antigas do CONTEXT). **Verificação:** `npm run build --workspace=frontend` OK (só warnings pré-existentes de budget/CommonJS).
**Task anterior:** 50-demanda-correcao-padroes — correção de padrões do módulo **demanda** (achado §4.3 da `docs/AUDITORIA.md`: primitivos em `DemandaRepository.alterar`/`listarAtribuidas`/`listar`) + boundary controller→service DTO-only (decisão "DTO em tudo" da spec 48). **Sem mudança de comportamento/validação/SQL/autorização/CTE — apenas assinatura/nomenclatura.** **Shared** — novos DTOs em `shared/src/dtos/demanda/` (barrel atualizado): `DemandaInternoAlterarDto` (`{ id, demandaPaiId?, nome?, descricaoTecnica?, descricaoCliente?, documentacao?, horasEstimadas?, prioridade?, status?, isEstrutural?, previsaoFimData?: string | null, ordemExibicao? }` — os **onze** campos do SET dinâmico, **incl. `demandaPaiId`** (reparenting), + `id`; verbo `Alterar` no fim §5, nunca `DemandaAlterarInternoDto`); `DemandaAtribuidasListarDto { usuarioId }`; `DemandaMembroRemoverDto { demandaId, usuarioId }` (verbo no fim). `DemandaConexaoExcluirDto` **expandido** de `{ conexaoId }` para `{ demandaId, conexaoId }` (service usa ambos; repositório usa só `conexaoId`). Aos três DTOs de `@Body` que recebem id de rota mesclado foi adicionado o campo identificador **opcional** (`@IsOptional`), injetado pela controller: `DemandaConexaoCriarDto.demandaOrigemId?`, `DemandaTagsAtribuirDto.demandaId?`, `DemandaUsuarioAtribuirDto.demandaId?`. **Repository** — `alterar(dto: DemandaInternoAlterarDto)` (era `alterar(id: number, dados: Partial<Demanda>)` — eliminado primitivo + `Partial<Model>`; `dto.id` no WHERE, demais no SET; SQL inalterado); `listarAtribuidas(dto: DemandaAtribuidasListarDto)` (era primitivo `usuarioId`); `listar(filtros, restricao?: DemandaAcessoFiltrarDto)` (era `usuarioId?: number` — carve-out de primitivo descartado pela decisão "DTO em tudo"; `DemandaAcessoFiltrarDto` reutilizado). **Service** — boundary DTO-only, **nenhum** método recebe `id`/`*Id` solto (`: number` zero no arquivo): `recuperar`/`recuperarArvore`/`recuperarAncestral`/`listarConexoes`/`listarTagsDemanda`/`listarMembros`(`dto: DemandaRecuperarDto`, +`usuarioAtivo`), `excluir(dto: DemandaRecuperarDto)`, `alterar(dto: DemandaInternoAlterarDto, usuarioAtivo)`, `criarConexao(dto: DemandaConexaoCriarDto)` (lê `dto.demandaOrigemId!`), `excluirConexao(dto: DemandaConexaoExcluirDto)`, `recuperarGrafo(dto: DemandaGrafoRecuperarDto, usuarioAtivo)`, `alterarTagsDemanda`/`atribuirMembro`(`dto`+`usuarioAtivo`, lê `dto.demandaId!`), `removerMembro(dto: DemandaMembroRemoverDto, usuarioAtivo)`; `listar`/`listarAtribuidas` montam `{ usuarioId }` ao chamar o repositório (`import DemandaAlterarDto` removido por desuso). **Controller** — monta o DTO de `@Param`/`@Query`: `{ id }`, `{ ...dto, id }`, `{ projetoId }`, `{ demandaId: id, conexaoId }`, `{ ...dto, demandaOrigemId: id }`, `{ ...dto, demandaId: id }`, `{ demandaId: id, usuarioId }`; nenhuma outra lógica, guards/`@GestorOnly`/`@ActiveUser` inalterados. **Desvio da letra da spec (registrado):** no `alterar`, removido o `new Date(dto.previsaoFimData)` do service — `previsaoFimData` passa como `string` direto ao repositório (campo do `DemandaInternoAlterarDto` tipado `string | null`, **mesmo padrão do `ProjetoInternoAlterarDto`** da spec 49). Para coluna `DATE` com valor `YYYY-MM-DD` (e `@IsDateString` no `@Body`, que nunca deixa passar `null`/`''`), o valor gravado é idêntico — comportamento preservado; o `criar` (fora do escopo) mantém seu `new Date()`. **Doc** — `CONVENTIONS.md` (Camadas → Repository): generalizada a regra `alterar(id, Partial<Model>)` ❌ para todo módulo (ex. demanda) e confirmado que o **parâmetro de restrição de escopo** também é DTO (`DemandaAcessoFiltrarDto`), sem carve-out de primitivo; SPEC §7.2/§16 #21 já cobriam (sem duplicar). **Verificação:** `npm run build --workspace=backend` (nest build) OK — type-check cobre o TS-fonte do `shared` (sem script de build próprio); checagens negativas: nenhum `: number` em assinatura de `DemandaService`; `alterar`/`listarAtribuidas`/`listar` do repositório sem primitivo. **Frontend não tocado** (o `DemandaService` Angular usa `id` na URL — fora do §16 #21).
**Task anterior:** 49-projeto-correcao-padroes — correção de padrões do módulo **projeto** (achado §4.2 da `docs/AUDITORIA.md`: primitivo `id` + `Partial<Projeto>` em `ProjetoRepository.alterar`) + boundary controller→service DTO-only (decisão "DTO em tudo" da spec 48). **Sem mudança de comportamento/validação/SQL — apenas assinatura/nomenclatura.** **Shared** — novo `ProjetoInternoAlterarDto` (`{ id, nome?, cor?, status?, inicioData?: string | null, previsaoFimData?: string | null }` — espelha **exatamente** os campos do SET dinâmico do `alterar`; **`codigo` NÃO entra** — o sistema proíbe alterá-lo, então declará-lo anunciaria capacidade inexistente); nome com verbo-no-fim §5 (`ProjetoInternoAlterarDto`, nunca `ProjetoAlterarInternoDto`); barrel `shared/src/dtos/projeto/index.ts` atualizado. **Repository** — `alterar(dto: ProjetoInternoAlterarDto)` (era `alterar(id: number, dados: Partial<Projeto>)` — eliminado primitivo + `Partial<Model>`; `dto.id` no WHERE, demais no SET; SQL inalterado). **Service** — boundary DTO-only: `recuperar(dto: ProjetoRecuperarDto, usuarioAtivo)`, `alterar(dto: ProjetoInternoAlterarDto)`, `excluir(dto: ProjetoRecuperarDto)` — nenhum recebe `id` primitivo (`ProjetoRecuperarDto { id }` reutilizado para recuperar e excluir; não criado DTO novo de exclusão; `import ProjetoAlterarDto` removido do service por desuso). **Controller** — monta o DTO a partir de `@Param('id')`: `recuperar({ id }, …)`, `alterar({ ...dto, id })`, `excluir({ id })`; nenhuma outra lógica (controller ainda recebe `@Body() ProjetoAlterarDto` e mescla `{ ...dto, id }`). **Doc** — `CONVENTIONS.md`: linha de proibição "primitivo no `alterar`" já existia (task 48); acrescentado o exemplo concreto ❌ `alterar(id, Partial<Projeto>)` / ✅ `alterar(dto: ProjetoInternoAlterarDto)` na seção Camadas → Repository. **Verificação:** `npm run build --workspace=backend` (nest build) OK; checagens negativas (nenhum método de `projeto.repository.ts` recebe primitivo/`Partial<Projeto>`; nenhum método de `ProjetoService` recebe `id: number` solto — só `id: dto.id` no literal repassado ao repositório) OK. `shared` sem script de build (TS-fonte, type-check via build do backend). **Frontend não tocado** (fora do §16 #21).
**Task anterior:** 48-usuario-correcao-padroes — correção de padrões do módulo **usuario** (achados §4.1/§8 da `docs/AUDITORIA.md`) + reconciliação documental §7.2 × §16 #21 (módulo canônico). **Sem mudança de comportamento/validação/SQL — apenas assinatura/nomenclatura.** **Shared** — novo `UsuarioInternoAlterarDto` (`{ id, nomeCompleto?, cargoTitulo?, anotacoes?, horasDiariasNecessarias?, status? }` — os **cinco** campos opcionais, incl. `status`, espelhando o SET dinâmico do `alterar`); DTO interno de senha renomeado `UsuarioAlterarSenhaInternoDto` → **`UsuarioSenhaInternoAlterarDto`** (verbo-no-fim §5; sem mudança de campos — `{ id, senhaEncriptada }`); barrel `shared/src/dtos/usuario/index.ts` atualizado. **Repository** — `alterar(dto: UsuarioInternoAlterarDto)` (era `alterar(id: number, dados: {…})` — eliminado primitivo + objeto anônimo; `dto.id` no WHERE, demais no SET; SQL inalterado); `alterarSenha(dto: UsuarioSenhaInternoAlterarDto)`. **Service** — boundary DTO-only: `recuperar(dto: UsuarioRecuperarDto, usuarioAtivo)`, `alterar(dto: UsuarioInternoAlterarDto, usuarioAtivo)`, `excluir(dto: UsuarioExcluirDto)`, `alterarSenha(dto: UsuarioSenhaAlterarDto & { id: number })` — nenhum recebe `id` primitivo. **Controller** — monta o DTO a partir de `@Param('id')`: `recuperar({ id }, …)`, `alterar({ ...dto, id }, …)`, `excluir({ id })`, `alterarSenha({ ...dto, id })`; nenhuma outra lógica. **Doc** — itens 1-3 (CONVENTIONS proibições + SPEC §7.4 ✅/❌ + §5 verbo-no-fim `Interno`) **já estavam aplicados** pela errata da auditoria; esta sessão registra a decisão da reconciliação em "Decisões Tomadas" (item 4). **Desvios da letra da spec** (registrados abaixo): `excluir` usa o DTO dedicado `UsuarioExcluirDto` (id obrigatório) em vez de `UsuarioRecuperarDto` (id opcional); `alterarSenha` do **service** recebe o DTO do corpo com `id` injetado (`UsuarioSenhaAlterarDto & { id }`) — não `UsuarioSenhaInternoAlterarDto`, que tem `senhaEncriptada` e é input do **repositório** (o service é quem encripta). **Verificação:** `npm run build --workspace=backend` (nest build) OK; checagens negativas (nenhum método de `usuario.repository.ts`/`UsuarioService` recebe primitivo; `grep AlterarInternoDto\|AlterarSenhaInternoDto` em `shared/src/dtos/usuario` vazio) OK. `shared` não tem script de build (consumido como TS-fonte via `main: src/index.ts`) — type-check coberto pelo build do backend. **Frontend não tocado** (o `UsuarioService` Angular usa `id` na URL — fora do §16 #21).
**Task anterior:** 44-auditoria-conformidade-padroes — varredura completa de conformidade contra `SYSTEM.SPEC.md`/`CONVENTIONS.md` + geração de specs de correção. **Não altera código de produção.**
**Sessão:** Auditoria + geração de specs. Produzido **`docs/AUDITORIA.md`** (sumário por módulo e por categoria + tabela detalhada de achados + verificações negativas executadas + lista de specs geradas). **Resultado:** o projeto está majoritariamente conforme (fruto das tasks 19/20); os achados concentram-se no que essas correções não alcançaram. **Achados:** (E §16 #21) `repository.alterar(id, ...)` recebe **primitivo `id`** (+ `Partial<Model>`/objeto anônimo) em **usuario/projeto/demanda/atividade/calendario/tag** — `ExecucaoRepository.alterar(dto)` é a referência conforme; demanda ainda tem `listarAtribuidas(usuarioId)` e `listar(filtros, usuarioId?)` primitivos. (A §16 #12) `fn_atualizar_updated_date()` (migration `20240001`) nomeia mecanismo genérico de BaseEntity com verbo português. (B §5.1) DTOs de saída de **ponto** (`PontoDiarioDto`/`PontoMensalDto`/`IntervaloDto`) fora do padrão verbo-particípio — **lacuna de documentação** (SPEC não cobre DTO de relatório/value-object). **Cross-cutting:** contradição §7.2 (controller passa `id` primitivo à service) × §16 #21 — boundary de service não flagrado por módulo; reconciliação atribuída à spec 48. **Conforme (sem spec):** execucao (referência), autenticacao, assistente e **todo o frontend** (varredura negativa: 0 `NgModule`/`Subject`/`ngModel`/`.css`/`style=""`/seletor-ID/rota-sem-lazy; BEM pt-BR; `models/` aliases permitidos pelo §8.1). **Specs geradas no backlog (8):** `48-usuario`, `49-projeto`, `50-demanda`, `51-atividade`, `52-calendario`, `53-tag`, `54-ponto`, `55-core` — `*-correcao-padroes.spec.md`, cada uma com reforço documental obrigatório. **Verificação:** sem build (task documental); `AUDITORIA.md` + 8 specs criados; nenhum módulo limpo gerou spec.

**Task concluída:** 45-frontend-tema-escuro (tema claro/escuro do sistema) — **concluída**, spec em `done/`. O toggle no popover de perfil da topbar está **habilitado e funcional** (`(click)="tema.alternarTema()"`, sem `[disabled]`). Infra: `app.config.ts` com `darkModeSelector: '.app-escuro'`; `core/services/tema.service.ts` + `core/models/tema.model.ts` (Signals, persistência em `localStorage`); no-flash via `<script>` inline no `index.html`; cores via aliases semânticos `--app-surface-*` (claro/escuro) em `styles.scss`. Ver `SYSTEM.SPEC.md` §8.5 (atualizado pela task 69).

**Task concluída:** 46-demanda-permissoes-visualizacao (correção das permissões de visualização e escrita por tipo de usuário — modelo final da matriz de permissão da demanda)
**Sessão:** Backend (regras em `demanda.service` + `atividade.service` + ajustes de `@GestorOnly`/`@ActiveUser` no controller) + frontend (gating de botões/abas + auto-inclusão de membro). Sem migration, sem novo DTO. **`demanda.service.ts`** — (1) `criar()`: novo gate no topo — desenvolvedor com `dto.demandaPaiId === undefined` → `UnauthorizedAccessException('Desenvolvedor só pode criar sub-demandas...')` (dev só cria sub-demanda; gestor segue criando raiz; regras de pai/projeto/`isEstrutural` preservadas, dev **pode** criar sub-demanda estrutural); (2) `criarConexao()`: removido o ramo do desenvolvedor membro — assinatura simplificada para `(demandaOrigemId, dto)` (sem `usuarioAtivo`), `recuperar` sem filtro de usuário (agora gestor-only via controller); (3) `alterarTagsDemanda()`: passou a receber `usuarioAtivo: JwtPayload` e gateia dev por `validarMembro` (não-membro → `UnauthorizedAccessException('Você só pode atribuir tags em demandas das quais é membro')`); (4) `atribuirMembro()`: recebe `usuarioAtivo` — gestor atribui qualquer `dto.usuarioId`; desenvolvedor só a si mesmo (`dto.usuarioId !== sub` → exceção) e com `validarAcessoProjeto` sobre `demandaEncontrada.projetoId`; duplicidade preservada; (5) `removerMembro()`: recebe `usuarioAtivo` — desenvolvedor só remove a si mesmo (`usuarioId !== sub` → exceção); regra do último membro preservada. **`demanda.controller.ts`** — `POST /:id/conexao` ganhou `@GestorOnly()` e perdeu `@ActiveUser`/repasse; `PUT /:id/tag`, `POST /:id/membro`, `DELETE /:id/membro/:usuarioId` perderam `@GestorOnly()` e ganharam `@ActiveUser() usuarioAtivo` repassado ao service (Swagger `@ApiResponse` 403 reescritos); `DELETE /:id` e `DELETE /:id/conexao/:conexaoId` seguem `@GestorOnly`. **`atividade.service.ts`** — `listar()`: desenvolvedor membro da demanda filtrada vê **todas** as atividades dela — `filtrosEfetivos` agora condicional: dev com `filtros.demandaId` definido **e** `validarAcessoDemanda` true → usa `filtros` inteiros; senão (sem `demandaId` ou não-membro) mantém `usuarioId = self` (preserva o autocomplete do seletor de execução). **Frontend `demanda-detalhe-dialog`** — novo `computed eMembro = sessao.eGestor() || !!demanda()?.podeEditar` + `computed abaInicial = eMembro() ? '1' : '2'` (aba Atividades, value=1, escondida para dev não-membro; `[value]` das abas passou a `abaInicial()` para não selecionar aba inexistente, cai em Conexões); aba **Atividades** (tab + tabpanel) envolta em `@if (eMembro())`; botão **Editar** das Tags `@if (sessao.eGestor())` → `@if (eMembro())`; ambos os botões **Nova Sub-demanda** (estado vazio e ação inferior) deixaram de ser gestor-only (exibidos sempre); **Editar/excluir** da demanda seguem gestor-only. **Frontend `demanda-membro-lista`** — novo `computed souMembro` (membros inclui `sessao.id()`) + métodos `participar()` (`atribuirMembro(demandaId, { usuarioId: sessao.id()! })` → recarrega) e `confirmarSaida()` (`confirm` → `removerMembro(sessao.id()!)`); cabeçalho: gestor vê "Adicionar"; dev membro vê "Sair" (`pi pi-sign-out`, danger); dev não-membro vê "Participar" (`pi pi-user-plus`); o "X" de remoção por linha segue gestor-only. **Frontend `demanda-arvore-item`** — menu de contexto sensível à sessão (injetado `UsuarioSessaoService`; `itensMenu` reconstruído como **propriedade direta**, nunca getter): Visualizar, Nova Sub-demanda (se estrutural) e as 3 descrições para todos; **Editar** e **Tags** (com seu separador) só para gestor — dev edita/tagueia pela dialog de detalhe, onde a membresia é conhecida. **`demanda-projeto`** (FAB "Nova Demanda" raiz) e `demanda-conexao-lista` **sem mudança** (já gestor-only no front). **Correção (mesma sessão):** ao abrir o detalhe da demanda como desenvolvedor (ex.: árvore → botão direito → Visualizar), o dialog dava "Acesso não permitido" e não carregava — o `carregarTudo()` do `demanda-detalhe-dialog` chamava `usuarioService.listar()` (`GET /usuario`, **@GestorOnly**) dentro do `forkJoin`, e o 403 derrubava o `forkJoin` inteiro. Como essa lista só alimenta o multiselect "Adicionar" do gestor (o dev usa "Participar"/"Sair"), a chamada `usuarios` virou condicional: `sessao.eGestor() ? usuarioService.listar(...) : of(null)` (import `of` do rxjs; guard `usuarios && usuarios.sucesso` no `next`). `GET /tag` não é gestor-only, então a lista de tags segue carregando p/ o dev membro. **Correção 2 (mesma sessão — gating de `descricaoCliente` na página de projeto):** o backend já bloqueava o dev de alterar `descricaoCliente` (`DemandaService.alterar`) e o `demanda-detalhe-dialog`/listagem de atividades já gateavam no front, mas o dialog de descrição da página `demanda-projeto` (grafo/árvore) abria sempre um `<p-editor>` editável — o dev conseguia digitar na "Desc. Cliente" e só tomava 403 ao Salvar. Alinhado ao padrão do detalhe-dialog: novo signal `descricaoEditavel` (setado de `demandaDados.podeEditar` em `abrirDialogDescricaoPorId`) + `computed podeEditarDescricaoAtual` (cliente → `sessao.eGestor()`; técnica/doc → `descricaoEditavel()`); `salvarDescricao()` ganhou guard `!podeEditarDescricaoAtual()`; HTML com modo somente-leitura (`@else`: `[innerHTML]` em `ql-editor`/`ql-snow` ou "Sem conteúdo", footer só "Fechar") e SCSS `&__descricao-leitura`/`&__descricao-vazia`. **Verificações:** `tsc --noEmit` backend OK; `ng build` frontend OK (só warnings pré-existentes de budget/CommonJS).

**Task anterior:** 47-perfil-usuario-e-atividade-em-dialog (visualizar perfil do usuário e visualizar atividade nas execuções passam a abrir um dialog em vez de navegar para uma tela)
**Sessão:** Frontend apenas (sem backend/shared/migration). Padrão: dois novos componentes standalone com método imperativo `abrir(...)` (mesmo padrão de `UsuarioAnotacoesDialogComponent`). **Perfil** — novo `UsuarioPerfilDialogComponent` (`modules/usuario/components/usuario-perfil-dialog/`) absorveu **toda** a lógica de `UsuarioPerfilPage`: dialog principal (avatar + Login/Cargo/Horas/Tipo/Status) + sub-dialogs de **Editar** e **Alterar senha** + botão **Anotações** (reusa `UsuarioAnotacoesDialogComponent`), gating por `podeEditar()` (próprio usuário ou gestor); expõe `abrir(usuarioId)` e output `aoAlterar`. `UsuarioListagemPage`: removido `editarUsuario`/navegação; botão "Visualizar perfil" chama `perfilDialog.abrir(usuario.id)` e template tem `<app-usuario-perfil-dialog #perfilDialog (aoAlterar)="buscarUsuarios()" />`. **Removida** a rota `:id` de `usuario.routes.ts` e **deletada** `UsuarioPerfilPage` (.ts/.html/.scss); `usuario-anotacoes.page.ts` (redirect órfão) passou a apontar para `/usuario` (não mais `/usuario/:id`); rota `:id/anotacoes` mantida. **Atividade** — novo `AtividadeVisualizarDialogComponent` (`modules/atividade/components/atividade-visualizar-dialog/`) extraído do dialog inline `mostrarDialogVisualizar` da listagem (mesmo conteúdo: executor/avatar/total, chips de tags, abas Descrição com `p-editor`+salvar e Últimas execuções); expõe `abrir(atividadeId)` (forkJoin recuperar+tags+execuções). `AtividadeListagemPage`: removido o `<p-dialog>` inline + signals/forms/métodos correspondentes (`mostrarDialogVisualizar`, `carregandoVisualizar`, `atividadeVisualizada`, `tagsVisualizar`, `execucoesVisualizar`, `salvandoDescricao`, `formularioDescricao`, `abrirAtividade`, `salvarDescricao`), imports `TabsModule`/`forkJoin`/`AtividadeRecuperadaDto` (órfãos) retirados, `EditorModule` mantido (ainda usado no dialog de descrição da demanda); botão "Visualizar" chama `visualizarDialog.abrir(atividade.id)`; SCSS órfão removido (`__visualizar-infos`, `__info-secundaria`, `__abas`, `__descricao-acoes`, modificador `__tags--visualizar`). `ExecucaoHistoricoPage`: o link `routerLink` da coluna **Atividade** virou `<button class="execucao-historico__atividade-link">` que chama `visualizarDialog.abrir(execucao.atividadeId)` (SCSS do link ganhou reset de botão: padding/border/background/font/cursor/text-align); `RouterModule` (órfão) trocado por `AtividadeVisualizarDialogComponent` nos imports. **Não** mexido: rota `/atividade/:id`+`AtividadeDetalhePage` (ainda usadas por execução-ativa/ponto/pós-criação) nem os outros links para `/atividade/:id`. **Verificação:** `ng build` OK (só warnings pré-existentes de budget/CommonJS).

**Task anterior:** 43-projeto-codigo-derivado-do-nome (no formulário de criação de projeto, o campo Código é auto-preenchido a partir do Nome, permanecendo editável)
**Sessão:** Frontend apenas, sem backend, sem DTO, sem migration. **Model** — novo helper puro `gerarCodigoDoNome(nome)` em [projeto.model.ts](frontend/src/app/modules/projeto/models/projeto.model.ts): `normalize('NFD')` + `replace(/[̀-ͯ]/g,'')` (remove acentos) → `toUpperCase()` → `replace(/[^A-Z0-9]+/g,'-')` → trim de hífens das pontas → `slice(0,50)` (MaxLength do `ProjetoCriarDto`) → trim final de hífen sobrado no corte (`'Sistema de Gestão' → 'SISTEMA-DE-GESTAO'`). **`ProjetoFormularioPage` (.ts)** — agora `implements OnInit`; novo `inject(DestroyRef)`, imports `takeUntilDestroyed` e `gerarCodigoDoNome`; novo signal `codigoEditadoManualmente = signal<boolean>(false)`. Em `ngOnInit`: (1) `formulario.controls.nome.valueChanges` → enquanto `!codigoEditadoManualmente()`, `codigo.setValue(gerarCodigoDoNome(nome ?? ''), { emitEvent: false })` (o `emitEvent:false` impede o laço com o valueChanges do código); (2) `formulario.controls.codigo.valueChanges` (só dispara em digitação do usuário, graças ao `emitEvent:false`) → `codigoEditadoManualmente.set(!!codigo?.trim())` — vazio devolve à derivação automática, qualquer outro valor desliga. Ambas com `takeUntilDestroyed(this.destroyRef)`. `salvar()` inalterado (segue com `.toUpperCase()` no DTO). **(.html)** — placeholder do código `"ex: PROJ-001"` → `"Gerado a partir do nome"`; novo `<small class="projeto-formulario__dica">` (em `@else if (!codigoEditadoManualmente())` após o erro) com a frase "Preenchido automaticamente pelo nome — edite se quiser um código próprio". Sem handler `(input)` (a detecção vem do `valueChanges`). **(.scss)** — novo `&__dica` (`0.75rem`, `--p-text-muted-color`). **Verificações:** `tsc --noEmit` (tsconfig.app) OK; `ng build` OK (warnings de budget pré-existentes, não relacionados).

**Task anterior:** 42-ponto-mes-linha-compacta (linhas do filtro mensal do ponto mais compactas para o mês inteiro caber na tela)
**Sessão:** Frontend apenas (SCSS), sem HTML, sem backend, sem DTO, sem migration. Único arquivo: [ponto-mes-dia.component.scss](frontend/src/app/modules/ponto/components/ponto-mes-dia/ponto-mes-dia.component.scss). Densidade aplicada **só na linha** (`&__linha`): `padding` vertical `0.625rem → 0.25rem` (horizontal `0.875rem` mantido), `gap` `0.75rem → 0.5rem`, `min-height: 1.875rem` adicionado para uniformidade (mantido `align-items: center` e o `grid-template-columns: 4rem 1fr 5rem 5.5rem 1.5rem`). Fontes reduzidas: `&__numero` `1rem → 0.875rem`, `&__semana` `0.75rem → 0.6875rem`, `&__periodo` `0.8125rem → 0.75rem`, `&__total` `0.875rem → 0.8125rem`, `&__saldo` `0.8125rem → 0.75rem` — preservada a hierarquia (número em destaque, semana/período em `--p-text-muted-color`, saldo `--positivo`/`--negativo` coloridos). Inalterados: `&__detalhe` (detalhe expandido mantém espaçamento), destaque `--nao-util`, hover `--clicavel`, timeline/intervalos e todos os binds do HTML. **Verificações:** `npm run build --workspace=frontend` OK (warnings de budget pré-existentes, não relacionados).

**Task anterior:** 41-execucao-colunas-projeto-demanda-total-dia (colunas de projeto e demanda + soma de todos os tempos de execução do dia filtrado na listagem de execuções)
**Sessão:** Shared (DTO + novo DTO de saída) + backend (repositório + service) + frontend (colunas + rodapé). Sem migration. **Shared** — `ExecucaoResumoDto` ganhou `demandaId`/`nomeDemanda`/`projetoId`/`nomeProjeto` (entre atividade e descrição); novo `ExecucaoListaDto` (`shared/src/dtos/execucao/ExecucaoListaDto.ts`, exportado no barrel) com os campos de paginação (`itens`/`totalItens`/`paginaAtual`/`itensPorPagina`/`totalPaginas`) **e** `totalMinutosDia` — campos próprios, sem re-export de `PaginatedResult`. **Backend** — `ExecucaoRepository.listar` ganhou os JOINs `INNER JOIN demanda ON demanda.id = atividade.demanda_id` e `INNER JOIN projeto ON projeto.id = demanda.projeto_id` (nas três queries: COUNT, total do dia e itens), com `demanda.is_deleted = false`/`projeto.is_deleted = false` nas condições; SELECT de itens acrescido de `demanda.id/nome` e `projeto.id/nome`; **nova query separada do total do dia** — `SELECT COALESCE(SUM(EXTRACT(EPOCH FROM (COALESCE(execucao.fim_data, NOW()) - execucao.inicio_data))::int / 60), 0)::int AS "totalMinutosDia"` com a **mesma cláusula WHERE** e **sem paginação** (em andamento conta até `NOW()`, igual à coluna Duração); retorno virou `{ itens, total, totalMinutosDia }`. `ExecucaoService.listar` passou a devolver `StandardResponse<ExecucaoListaDto>` repassando `totalMinutosDia` (imports `ExecucaoResumoDto`/`PaginatedResult` removidos por desuso); o escopo do desenvolvedor (`usuarioIdRestricao`) é inalterado — o total respeita o mesmo filtro. **Frontend** — `ExecucaoService.listar` retorna `StandardResponse<ExecucaoListaDto>`. Em `ExecucaoHistoricoPage`: novo `totalMinutosDia = signal<number>(0)` setado no `next` de `buscarExecucoes`; HTML com colunas **Projeto** (`execucao.nomeProjeto`) e **Demanda** (`execucao.nomeDemanda`) antes de Atividade (Usuário continua só-gestor como primeira; Atividade segue link); novo `<ng-template #footer>` com linha "Total do dia" (`colspan` 6/5) + valor `totalMinutosDia() | minutosParagHoras` alinhado sob Duração + célula vazia final (`colspan` 2/1); `emptymessage` ajustado de `7/5` para `9/7`; SCSS `&__rodape-rotulo` (right/bold) e `&__rodape-valor` (bold/primary). **Verificações:** `tsc --noEmit` backend OK; `ng build` frontend OK.

**Task anterior:** 40-atividade-registrar-execucao-manual (gestor registra, pela listagem de atividades, uma execução já encerrada para o dono da atividade, com validação de sobreposição)
**Sessão:** Shared (3 DTOs) + backend (endpoint + validação) + frontend (botão + dialog). Sem migration. **Shared** — 3 novos DTOs em `shared/src/dtos/execucao/` exportados no barrel: `ExecucaoRegistrarDto` (entrada: `atividadeId` `@IsNumber @Min(1)`, `inicioData`/`fimData` `@IsDateString`, `descricao` `@IsString @IsNotEmpty`), `ExecucaoRegistradaDto` (saída com campos próprios: `id`/`atividadeId`/`descricao`/`inicioData: Date`/`fimData: Date`/`duracaoMinutos`), `ExecucaoSobreposicaoValidarDto` (interno: `usuarioId`/`inicioData: Date`/`fimData: Date`). **Backend** — `ExecucaoRepository.registrar()` (novo `INSERT ... SELECT` com `inicio_data` e `fim_data` preenchidos + `duracaoMinutos` via `EXTRACT(EPOCH ...)::int / 60`; o `inserir` original com `fim_data = NULL` permanece intocado) e `ExecucaoRepository.validarSobreposicao()` (`SELECT EXISTS` com JOIN em `atividade` por `atividade.usuario_id`; sobreposição `[a,b]×[c,d]` via `inicio_data < :fimData AND :inicioData < COALESCE(fim_data, 'infinity'::timestamptz)` — execução em andamento estende ao infinito); `ExecucaoService.registrar(dto, usuarioAtivo)` valida atividade existente (404 `ResourceNotFoundException('Atividade')`), datas não no futuro, `fim > inicio` (`BusinessException`), sobreposição contra o dono `atividade.usuarioId` (`BusinessException('Já existe uma execução deste usuário que se sobrepõe a este período')`), grava e retorna `StandardResponse<ExecucaoRegistradaDto>` ("Execução registrada com sucesso") — a regra de "execução ativa única" **não se aplica** (já entra encerrada); `ExecucaoController` ganhou `POST /execucao/registro` `@GestorOnly()` com `@ActiveUser()` (caminho distinto de `POST /execucao` `iniciar`, sem conflito). **Frontend** — `ExecucaoService.registrar(dto)` → `POST /execucao/registro`. Em `AtividadeListagemPage`: novo botão na coluna Ações (só gestor) `icon="pi pi-history"` `severity="help"` `[text]` tooltip "Registrar execução" → `abrirDialogRegistro`; o botão de tags existente ganhou `severity="secondary"` para não se confundir. Novo dialog "Registrar execução" (padrão dos demais de execução): topo `projeto · atividade` + dono com avatar (`iniciais(nomeUsuario)`); `formularioRegistro` (Reactive) com `inicioData`/`fimData` (`p-datepicker [showTime]`, obrigatórios) + `descricao` (`pTextarea` obrigatório + `app-assistente-descricao tipoEntidade="execucao"`); validador cross-field `fimPosteriorAoInicio` (erro `fimAntesDoInicio` exibido inline); `salvarRegistro` envia datas como ISO (`toISOString()`), em sucesso → toast + fecha + `buscarAtividades()` (recarrega a página da tabela para a coluna **Tempo executado** refletir). Novos signals `mostrarDialogRegistro`/`salvandoRegistro`/`atividadeRegistro`, injeção de `ExecucaoService`, métodos `abrirDialogRegistro`/`salvarRegistro`/`aceitarDescricaoAssistenteRegistro`/`campoInvalidoRegistro`. **Verificações:** `tsc --noEmit` backend e frontend OK; `ng build` OK.

**Task anterior:** 38-calendario-dia-meio-periodo (dia não útil de meio período que reduz a meta diária à metade, com impacto no ponto)
**Sessão:** Shared + migration + backend (calendário e ponto) + frontend (calendário). Adiciona a noção de **duração** (`INTEGRAL` / `MEIO_PERIODO`) ao `dia_nao_util`, atributo independente do `tipo` e sem distinção de turno. **Shared** — novo enum `DiaNaoUtilDuracaoEnum` (`shared/src/enums/dia-nao-util-duracao.enum.ts`, exportado no barrel); campo `duracao` adicionado em `DiaNaoUtilCriarDto` (`@IsEnum` obrigatório), `DiaNaoUtilAlterarDto` (`@IsOptional @IsEnum`), `DiaNaoUtilResumoDto`/`DiaNaoUtilCriadoDto`/`DiaNaoUtilAlteradoDto` e `CalendarioDiaNaoUtilMesDto`. **Migration** `20240014_adicionar_duracao_dia_nao_util` — `ADD COLUMN duracao VARCHAR(20)` nullable → backfill explícito `UPDATE ... SET 'INTEGRAL'` (sem DEFAULT) → `SET NOT NULL` → CHECK `duracao IN ('INTEGRAL','MEIO_PERIODO')`; rollback dropa constraint + coluna. Aplicada (Batch 3). **Backend calendário** — `DiaNaoUtil` model ganhou `duracao`; `CalendarioRepository` (`inserir`/`alterar`/`recuperar`/`listar`/`listarDiasNaoUteisDoMes` passam `duracao`; `recuperarTipo` agora retorna `{ tipo, duracao } | null` via interface interna `DiaNaoUtilInfo`); `CalendarioService.criar`/`alterar` repassam `duracao`; `verificarDiaUtil` sufixa o motivo com ` (meio período)` quando `MEIO_PERIODO` (integral preserva o retorno cru do tipo). **Backend ponto** — núcleo migrou de "ehDiaUtil booleano" para **`fracaoMeta` ∈ {0, 0.5, 1}** na interface `InfoDia`. `resolverInfoDia` retorna `fracaoMeta` (1 dia útil; 0 fim de semana/integral não útil; 0.5 meio período) + motivo sufixado. `montarPontoDiario`: para `fracaoMeta === 0.5` → `metaMinutos = round(jornada×60×0.5)`, capacidade útil limitada à meta (excedente vira extra, **saldo do dia nunca positivo**); `0` e `1` preservam **exatamente** o comportamento atual via branch dedicado; `ehDiaUtil = fracaoMeta === 1`. `consultarMensal`: `metaMinutosMes = round(metaDiaria × Σ fracaoMeta)` (dia útil=1, meio período=0.5, fim de semana/integral não útil=0); `diasUteisMes` passou a ser a soma fracionária; saldo por dia replica a regra de meio período; usa a `duracao` de `listarDiasNaoUteisDoMes`. **Frontend calendário** — `dia-nao-util.model.ts` ganhou `DIA_NAO_UTIL_DURACAO_OPCOES` + `rotuloDuracaoDiaNaoUtil`; `CalendarioListagemPage` tem novo controle `duracao` (default `INTEGRAL`), `p-select` de Duração no dialog (com aviso de meta reduzida em meio período), coluna **Duração** na tabela (badge "½ dia" / "Integral", `colspan` do empty 5/4→6/5) e sufixo "(meio período)" no tooltip do dia marcado no calendário. **Frontend ponto** — sem mudança: o motivo já vem sufixado e os totais/saldo vêm prontos do backend. **Acréscimos (mesma task, fora do escopo original):** (1) **i18n pt-BR dos calendários em todo o projeto** — todos os `p-datepicker` mostravam meses/dias em inglês; criado `core/config/primeng-traducao-pt-br.ts` (`TRADUCAO_PT_BR: Translation`) aplicado via `translation` no `providePrimeNG` (`app.config.ts`), cobrindo calendário, verificador, ponto, execução, demanda e projeto; (2) **duplo clique no dia do calendário** (`CalendarioListagemPage`, só gestor) — abre edição se já há dia não útil na data (`abrirDialogEditar`) ou criação com data pré-preenchida (`abrirDialogNovo(data)`), via `(dblclick)` no wrapper `&__dia-celula` do `pTemplate="date"` + método `onDuploCliqueDia(date)`; (3) **remoção do "Verificador de dia útil" da UI** — bloco do topo (datepicker + botão Verificar) retirado do template/TS/SCSS por confundir mais do que ajudar; **o endpoint `GET /calendario/verificar` e o método cliente `CalendarioService.verificarDiaUtil` permanecem** (só a exibição foi cortada). **Verificações:** `tsc --noEmit` backend e frontend OK; `ng build` OK.

**Task anterior:** 39-demanda-desenvolvedor-editar-descricoes (desenvolvedor visualiza todas as descrições e edita técnica + documentação)
**Sessão:** Backend (1 regra no service) + frontend (gating de botões + modo somente-leitura em duas telas). Sem migration, sem mudança de DTO. **Backend** — em `DemandaService.alterar()`, após recuperar a demanda (mantida a verificação de acesso por `usuarioId`), nova regra: se `usuarioAtivo.tipo === UsuarioTipoEnum.DESENVOLVEDOR` **e** `dto.descricaoCliente !== undefined` → `UnauthorizedAccessException('Apenas gestores podem alterar a descrição do cliente')` (`!== undefined` barra inclusive tentativa de limpar com `''`/`null`); demais campos (`descricaoTecnica`, `documentacao`, `nome`, `status`, etc.) seguem liberados ao desenvolvedor atribuído. Controller continua sem `@GestorOnly` (gating fino por campo no service). **Frontend / `demanda-detalhe-dialog`** — os três botões de descrição ("Desc. Técnica", "Desc. Cliente", "Documentação") saíram do `@if (sessao.eGestor())` e aparecem **sempre**; "Editar" e excluir permanecem dentro do `@if` (gestor-only), tudo no mesmo `__cabecalho-acoes`. Novo método `podeEditarDescricao(campo)` (`this.sessao.eGestor() || campo !== 'descricaoCliente'`) e `computed` `podeEditarDescricaoAtual` derivado de `campoDescricaoEditando()`; guard em `salvarDescricao()` (`return` cedo se não pode editar). A dialog de descrição ganhou **modo somente-leitura** (`@else` quando `!podeEditarDescricaoAtual()`): exibe o valor em `&__descricao-leitura` (`white-space: pre-wrap`, scroll) ou `&__descricao-vazia` ("Sem conteúdo"), **sem** `app-assistente-descricao`, footer só "Fechar"; modo editável mantém textarea + assistente + "Cancelar"/"Salvar". **Frontend / `atividade-listagem`** — `podeEditarCampoDescricao()` deixou de exigir `this.sessao.eGestor()`, passou a `!!campo && this.camposEditaveis.includes(campo) && !!this.demandaDescricao()?.podeEditar`. **Correção de acesso (mesma sessão, além da spec):** ao testar, o desenvolvedor recebia "Demanda não encontrado" ao abrir, pelo grafo do projeto (ou pela árvore de sub-demandas), uma demanda do projeto da qual **não é membro** — porque `recuperar` exigia `demanda_usuario` da própria demanda, enquanto grafo/árvore expõem todas as demandas do projeto. Regra acordada com o usuário: **desenvolvedor visualiza qualquer demanda de um projeto a que tem acesso, mas só edita as que é membro.** Implementado: (1) `DemandaRepository.recuperar` — filtro do desenvolvedor passou de "membro da demanda" para **acesso ao projeto** (subquery `EXISTS` sobre `demanda_usuario` de qualquer demanda do mesmo `projeto_id`); (2) `DemandaRecuperadaDto` ganhou `podeEditar: boolean`; (3) `DemandaService.recuperar` calcula `podeEditar` (gestor sempre `true`; desenvolvedor via `validarMembro` da demanda); (4) `DemandaService.alterar` e `criarConexao` ganharam gate de **membro** para desenvolvedor (`validarMembro` → `UnauthorizedAccessException` quando não-membro), preservando a restrição de edição agora que `recuperar` é por projeto; (5) frontend gateia edição por `podeEditar` (`demanda-detalhe-dialog` via signal `descricaoEditavel` + computed `podeEditarDescricaoAtual`; `atividade-listagem` via `demandaDescricao()?.podeEditar`) — demandas só-visualização abrem a descrição em modo leitura. Verificado contra o banco: `recuperar(5)` agora retorna "IA" para joão com `podeEditar=false`. **Correção de editor (mesma sessão):** as descrições quebravam ao renderizar porque a **tela de demanda** editava o campo num `<textarea>` (texto puro) enquanto a **tela de atividades** usa `p-editor`/Quill (HTML) — o mesmo campo gravado em dois formatos (ex.: demanda 1 = `<p>…</p>`, demanda 5 = `123`). Padronizado conforme `SYSTEM.SPEC` (campos de descrição são HTML via `p-editor`): em [demanda-detalhe-dialog](frontend/src/app/modules/demanda/components/demanda-detalhe-dialog/) o `<textarea pTextarea>` virou `<p-editor>` (import `EditorModule` no lugar de `TextareaModule`, SCSS `&__editor` com `::ng-deep .p-editor-container/.p-editor-content` flex igual à listagem de atividades) e o modo leitura passou de `{{ valor }}` (texto cru, mostrava as tags) para `[innerHTML]` (renderiza o HTML). Texto puro legado (ex.: "123") é aceito pelo Quill e normalizado para HTML no próximo salvamento — sem migration. **Correção de estilo do Quill (mesma sessão):** o `p-editor` aparecia sem estilo ("quebrou os estilos") porque o **tema do Quill nunca foi importado** — `quill` estava só no `package.json`, sem CSS em lugar nenhum (afetava também a tela de atividades e as anotações de usuário). Adicionado `@import 'quill/dist/quill.snow.css';` em [styles.scss](frontend/src/styles.scss) (mesmo padrão do `primeicons.css`); confirmado `.ql-toolbar/.ql-snow/.ql-editor` no `styles.css` final. O modo leitura passou a envolver o `[innerHTML]` em `<div class="ql-snow"><div class="ql-editor">` para a tipografia do Quill (listas/títulos) renderizar igual ao editor (aplicado na leitura da `demanda-detalhe-dialog` **e** da `atividade-listagem`). **Editor de texto puro remanescente (causa do descasamento entre telas):** havia um **terceiro** editor de descrição — o dialog da página [demanda-projeto](frontend/src/app/modules/demanda/pages/demanda-projeto/) (acessado pelo grafo/árvore do projeto) — ainda usando `<textarea>` (texto puro). Era ele que gravava texto puro: ao editar ali e ver na tela de atividades as quebras de linha sumiam, e o HTML do Quill aparecia com as tags cruas ali. Convertido para `p-editor` (`EditorModule` no lugar de `TextareaModule`, SCSS `&__editor`). Agora **todos** os editores de descrição da demanda (detalhe, projeto e listagem de atividades) usam Quill/HTML e exibem via `[innerHTML]`/`ql-editor` — formato único entre as telas.

**Task anterior:** 37-execucao-data-hora-inicio-fim (data + horário de início e fim na listagem de execuções) — Frontend apenas (template + estilo), sem mudança em DTO/service/endpoint. Nas colunas **"Início"** e **"Fim"** da tabela de `ExecucaoHistoricoPage`, cada célula passou a exibir **data + horário** num bloco `&__momento` (`display: flex; flex-direction: column`): a data (`{{ ... | date: 'dd/MM/yy' }}`) em `&__momento-data` (fonte menor, `--p-text-muted-color`) acima do horário (`{{ ... | date: 'HH:mm' }}`) em `&__momento-hora` (maior, `font-weight: 600`, `--p-text-color`) — assim a virada de dia (início 23:40 / fim 01:15 do dia seguinte) fica evidente. A coluna **Fim** mantém o `<span>` "Em andamento" quando `fimData` é nulo. O dialog de edição (gestor) com `p-datepicker [showTime]` não foi tocado. Sem backend, sem migration.

**Task anterior:** 36-atividade-tempo-executado (coluna de tempo total executado na listagem de atividades) — Adicionada a coluna "Tempo executado" à listagem de atividades, somando a duração de todas as execuções encerradas de cada atividade. **Backend:** `AtividadeResumoDto` ganhou o campo `totalMinutosExecutados: number`; `AtividadeRepository.listar` calcula o total via **subconsulta escalar correlacionada** no SELECT — `(SELECT COALESCE(SUM(EXTRACT(EPOCH FROM (execucao.fim_data - execucao.inicio_data)) / 60), 0)::int FROM execucao WHERE execucao.atividade_id = atividade.id AND execucao.is_deleted = false AND execucao.fim_data IS NOT NULL)` (execuções em andamento ficam de fora; atividades sem execução retornam 0); o `COUNT(atividade.id)` da paginação permanece intocado. **Frontend (`AtividadeListagemPage`):** nova coluna "Tempo executado" entre Status e Tags, exibindo `totalMinutosExecutados` via pipe `minutosParagHoras` (já importado); `colspan` do template `empty` ajustado de 7/6 para 8/7. Sem migration.

**Task anterior:** 35-atividade-execucoes-compactas — lista de execuções compacta em `AtividadeListagemPage` (somente frontend). As duas listas de "Últimas execuções" passaram a renderizar cada execução em **uma única linha** `data · duração · descrição`: removida a `<div>` de cabeçalho, descrição migrada para `<span>` truncado com `pTooltip`; item virou `display: flex`, lista mais densa, `max-height: 16rem` + scroll preservados.

**Task anterior (34):** 34-atividade-descricoes-demanda — botões das três descrições da demanda na coluna Demanda da listagem de atividades. **Backend:** `AtividadeResumoDto` ganhou 3 flags booleanas (`demandaTemDescricaoCliente`/`demandaTemDescricaoTecnica`/`demandaTemDocumentacao`); `AtividadeRepository.listar` as deriva no SELECT via `COALESCE(TRIM(demanda.<campo>), '') <> ''` (JOIN com `demanda` já existia). **Frontend (`AtividadeListagemPage`):** na coluna **Demanda**, à esquerda do nome, três `p-button` pequenos (`[text]`+`[rounded]`+`size="small"`) e distintos — `pi pi-user` (cliente → `info`), `pi pi-code` (técnica → `help`), `pi pi-book` (documentação → `warn`); a cor de destaque só aparece quando a descrição **tem conteúdo** (via `severidadeIconeDescricao` → senão `secondary`/cinza), mas os 3 botões aparecem sempre. Clicar chama `abrirDescricaoDemanda(atividade, campo)`: carrega a demanda via `DemandaService.recuperar(atividade.demandaId)` com **cache por demandaId** (`Map` em `cacheDemandas`) e abre `p-dialog` modal **95vw × 95vh** (`contentStyle` flex column; em edição o `p-editor` preenche a altura via `&__editor` com `::ng-deep .p-editor-container/.p-editor-content` flex; em leitura o `&__descricao-rica` rola) exibindo o campo como **HTML** via `[innerHTML]`; cabeçalho `nomeDemanda — rótulo`; corpo "Sem conteúdo" quando vazio. **Edição inline** (somente gestor, somente `descricaoTecnica` e `documentacao` — `descricaoCliente` é read-only): o `<ng-template pTemplate="footer">` é **sempre renderizado** (com `@if` interno) para o PrimeNG projetar o footer mesmo quando aparece após o carregamento; botão "Editar" → modo edição com `p-editor` + `app-assistente-descricao` → "Salvar" chama `DemandaService.alterar(demandaId, { [campo]: valor })`, atualiza o cache, o `demandaDescricao` e a flag correspondente nas linhas em memória (`atualizarFlagDescricao`) + toast. Novos: tipo `CampoDescricaoDemanda`, signals `mostrarDialogDescricao`/`carregandoDescricao`/`demandaDescricao`/`nomeDemandaDescricao`/`campoDescricao`/`editandoDescricao`/`salvandoDescricaoDemanda`, form `formularioDescricaoDemanda`, `camposEditaveis`, mapa `rotulosDescricao`, métodos `abrirDescricaoDemanda`/`cabecalhoDescricao`/`conteudoDescricao`/`severidadeIconeDescricao`/`podeEditarCampoDescricao`/`abrirEdicaoDescricao`/`salvarDescricaoDemanda`/`aceitarDescricaoAssistenteDemanda`; SCSS `&__demanda`/`&__demanda-nome`/`&__demanda-acoes`/`&__descricao-rica`.

---

## Stack em Uso

| Camada | Tecnologia | Versão |
|---|---|---|
| Backend | NestJS + TypeScript | — |
| Frontend | Angular | 21 |
| UI | PrimeNG | 21 |
| Banco | PostgreSQL | 16 |
| Query layer | Knex.js | — |
| Monorepo | npm workspaces | — |
| Container | Docker + Docker Compose | — |

---

## Implementado

- **00-monorepo-docker** — `package.json` raiz com npm workspaces, `docker-compose.yml` com PostgreSQL 16, `.env`, `.env.example` e `.gitignore`
- **01-shared-package** — pacote `@project20/shared` com 7 enums de negócio, interfaces `StandardResponse` e `PaginatedResult`, e estrutura de pastas de DTOs (9 módulos, prontos para ser populados)
- **02-backend-scaffold** — projeto NestJS estruturado com `package.json`, `tsconfig.json`, `nest-cli.json`, `knexfile.ts`; módulo `DatabaseModule` com Knex configurado; `main.ts` com `ValidationPipe` e `GlobalPrefix`; 10 módulos de negócio esqueleto; placeholders de `core/` (BaseEntity, BaseRepository, exceções, filtro, interceptor) e `config/`
- **03-migrations** — 12 arquivos de migration Knex criados e executados com sucesso; função `fn_atualizar_updated_date()` + 11 tabelas com campos BaseEntity, CHECKs, índices filtrados por `is_deleted = false` e triggers `updated_date`; fix no `knexfile.ts` para usar `path.resolve(__dirname)` e carregar o `.env` corretamente
- **04-core-module** — `BaseEntity`, `BaseRepository` (com `@Inject(DATABASE_CONNECTION)`, JSDoc e todos os métodos protegidos), 3 exceptions (`BusinessException`, `ResourceNotFoundException`, `UnauthorizedAccessException`), `GlobalExceptionFilter` (com tratamento específico para erros de ValidationPipe), `ResponseFormatInterceptor` (com verificação de resposta já encapsulada), re-exports de `StandardResponse` e `PaginatedResult` em `core/interfaces/`, e `CoreModule` registrando filter e interceptor globalmente via `APP_FILTER`/`APP_INTERCEPTOR`; `CoreModule` importado no `AppModule`
- **05-config-service** — `config.interface.ts` com interfaces em português (`ConfiguracaoBancoDados`, `ConfiguracaoJwt`, `ConfiguracaoAnthropic`, `ConfiguracaoAplicacao`, `ConfiguracaoNegocio`, `Configuracao`); `ConfigService` com todas as vars carregadas no constructor e expostas via `obter()`; `ConfigModule` global; `database.provider.ts` e `main.ts` atualizados para usar `configService.obter()`
- **06-usuario-module** — 9 DTOs em `shared/src/dtos/usuario/` (`UsuarioCriarDto`, `UsuarioCriadoDto`, `UsuarioResumoDto`, `UsuarioRecuperadoDto`, `UsuarioListarDto`, `UsuarioAtualizarDto`, `UsuarioAtualizadoDto`, `UsuarioSenhaAlterarDto`, `UsuarioSenhaAlteradaDto`); `Usuario` model em `backend/`; `UsuarioRepository` com SQL bruto (existeLogin, buscarLogin, buscarComSenha, buscarIdentificador, listar, inserir, atualizar, atualizarSenha, excluir, listarGestoresAtivos); `UsuarioService` com regras de negócio (login único, bcrypt rounds=10, validação de senha atual); `UsuarioController` com 6 endpoints (POST, GET, GET/:id, PUT/:id, DELETE/:id, PATCH/:id/senha); `UsuarioModule` registrado no `AppModule`
- **07-autenticacao-module** — 2 DTOs em `shared/src/dtos/autenticacao/` (`AutenticacaoLoginDto`, `AutenticacaoTokenDto`); `JwtPayload` interface; `JwtStrategy` (passport-jwt); `JwtAuthGuard` (global, verifica @Public()); `GestorGuard` (global, verifica @GestorOnly()); decorators `@Public()`, `@GestorOnly()`, `@ActiveUser()`; `AutenticacaoService` (validarCredenciais, gerarToken, login); `AutenticacaoController` com `POST /autenticacao/login` público; `AutenticacaoModule` com `PassportModule`, `JwtModule.registerAsync`, guards globais via `APP_GUARD`; `UsuarioController` e `UsuarioService` atualizados com guards e validação de permissão por tipo de usuário
- **08-tag-module** — 6 DTOs em `shared/src/dtos/tag/` (`TagCriarDto`, `TagCriadaDto`, `TagAtualizarDto`, `TagAtualizadaDto`, `TagResumoDto`, `TagRecuperadaDto`); `Tag` model no backend; `TagRepository` com SQL bruto (existeNome, inserir, buscarIdentificador, listar, atualizar, excluir); `TagService` com regras de negócio (nome duplicado → BusinessException, tag não encontrada → ResourceNotFoundException); `TagController` com 5 endpoints (POST restrito a gestor, GET, GET/:id, PUT/:id restrito a gestor, DELETE/:id restrito a gestor); `TagModule` registrado no `AppModule`
- **09-projeto-module** — 7 DTOs em `shared/src/dtos/projeto/` (`ProjetoCriarDto`, `ProjetoCriadoDto`, `ProjetoResumoDto`, `ProjetoRecuperadoDto`, `ProjetoListarDto`, `ProjetoAtualizarDto`, `ProjetoAtualizadoDto`); `Projeto` model no backend; `ProjetoRepository` com SQL bruto (existeCodigo, inserir, buscarIdentificador, listarTodos, listarPorUsuario, atualizar, excluir); `ProjetoService` com regras de negócio (código duplicado → BusinessException, acesso por tipo de usuário via listarTodos/listarPorUsuario, desenvolvedor sem acesso → ResourceNotFoundException, validação de datas); `ProjetoController` com 5 endpoints (POST/PUT/DELETE restritos a gestor, GET e GET/:id com @ActiveUser para controle de escopo); `ProjetoModule` registrado no `AppModule`
- **10-demanda-crud** — `BaseRepository` atualizado para aceitar `Knex.Transaction` opcional em `executarConsulta` e `executarComando`; 10 DTOs em `shared/src/dtos/demanda/` (`DemandaCriarDto`, `DemandaCriadaDto`, `DemandaResumoDto`, `DemandaRecuperadaDto`, `DemandaListarDto`, `DemandaAtualizarDto`, `DemandaAtualizadaDto`, `DemandaGrafoNoDto`, `DemandaGrafoArestaDto`, `DemandaGrafoDto`); `Demanda` model no backend; `DemandaRepository` com SQL bruto (inserir, inserirDemandaUsuario, inserirComAtribuicao transacional, buscarIdentificador com filtro opcional de usuário, listar com JOIN condicional em demanda_usuario, atualizar, excluir, buscarIdGestoresAtivos, usuarioTemAcessoProjeto, recuperarGrafo); `DemandaService` com regras de negócio (verificação de acesso de desenvolvedor via demanda_usuario, validação de demanda pai no mesmo projeto, auto-atribuição atômica do criador + gestores ativos, controle de acesso no listar/recuperar/atualizar por tipo); `DemandaController` com 6 endpoints (POST, GET, GET/grafo, GET/:id, PUT/:id, DELETE/:id restrito a gestor); `DemandaModule` registrado no `AppModule`
- **11-demanda-hierarquia** — 2 DTOs em `shared/src/dtos/demanda/` (`DemandaArvoreItemDto` com campo recursivo `filhos`, `DemandaAncestralDto`); `DemandaRepository` com `buscarDescendentes` (CTE recursivo descendente — inclui raiz com nível 0) e `buscarAncestral` (CTE recursivo invertido — apenas ancestrais com nível > 0, ordem raiz-para-pai invertida); `DemandaService` com `recuperarArvore` (converte lista plana em árvore aninhada via Map) e `recuperarAncestral` (repassa lista ordenada); `DemandaController` com 2 endpoints (`GET /:id/arvore`, `GET /:id/ancestral`), declarados antes de `GET /:id` para garantir rota correta no NestJS
- **12-demanda-grafo** — 3 DTOs em `shared/src/dtos/demanda/` (`DemandaConexaoCriarDto`, `DemandaConexaoCriadaDto`, `DemandaConexaoResumoDto`); `DemandaRepository` com `verificarCriariaCiclo` (CTE recursivo conforme SCHEMA.md), `existeConexao` (helper para prevenir duplicatas), `inserirConexao`, `listarConexoes` (CASE para identificar direção saída/entrada/bidirecional), `excluirConexao` (soft delete direto em `demanda_conexao`), `conexaoPertenceADemanda` (autorização no delete); `recuperarGrafo` atualizado para retornar arestas reais via JOIN com `demanda_conexao`; `DemandaService` com `criarConexao` (6 validações: acesso, existência origem/destino, auto-referência, duplicata, ciclo), `listarConexoes`, `excluirConexao`; `DemandaController` com 3 endpoints (`POST /:id/conexao`, `GET /:id/conexao`, `DELETE /:id/conexao/:conexaoId` restrito a gestor)
- **13-demanda-tags-atribuicoes** — 5 DTOs em `shared/src/dtos/demanda/` (`DemandaTagsAtribuirDto`, `DemandaTagsAtribuidasDto`, `DemandaUsuarioAtribuirDto`, `DemandaUsuarioAtribuidoDto`, `DemandaMembroDto`); `DemandaRepository` com 8 métodos novos (`listarTagsDemanda`, `atribuirTagsDemanda` com sync, `removerTagDemanda`, `listarMembrosDemanda`, `atribuirMembroDemanda`, `removerMembroDemanda`, `membroJaAtribuido`, `contarMembrosDemanda`); `DemandaModule` importa `TagModule` e `UsuarioModule`; `DemandaService` com 5 métodos (`atualizarTagsDemanda`, `listarTagsDemanda`, `listarMembros`, `atribuirMembro`, `removerMembro`) com injeção de `TagRepository` e `UsuarioRepository`; `DemandaController` com 5 endpoints (`PUT /:id/tag` gestor, `GET /:id/tag`, `GET /:id/membro`, `POST /:id/membro` gestor, `DELETE /:id/membro/:usuarioId` gestor)
- **14-atividade-module** — 9 DTOs em `shared/src/dtos/atividade/` (`AtividadeCriarDto`, `AtividadeCriadaDto`, `AtividadeResumoDto`, `AtividadeRecuperadaDto`, `AtividadeListarDto`, `AtividadeAtualizarDto`, `AtividadeAtualizadaDto` como re-export de Recuperada, `AtividadeTagsAtribuirDto`, `AtividadeTagsAtribuidasDto`); `Atividade` model no backend; `AtividadeRepository` com SQL bruto (inserir, buscarIdentificador com JOIN a usuario, listar com paginação e filtro de status, atualizar, excluir, usuarioTemAcessoDemanda via demanda_usuario, listarTags, atualizarTags com sync); `AtividadeService` com 7 métodos (criar com verificação de acesso via demanda_usuario, listar com restrição de desenvolvedor, recuperar, atualizar com regra autor-ou-membro para desenvolvedor, excluir, atualizarTags, listarTags); `AtividadeController` com 7 endpoints (POST, GET, GET/:id, PUT/:id, DELETE/:id gestor, PUT/:id/tag gestor, GET/:id/tag); `AtividadeModule` importa `DemandaModule` e `TagModule`; registrado no `AppModule`
- **15-execucao-module** — 8 DTOs em `shared/src/dtos/execucao/` (`ExecucaoIniciarDto`, `ExecucaoIniciadaDto`, `ExecucaoEncerrarDto`, `ExecucaoEncerradaDto`, `ExecucaoAtualizarDto`, `ExecucaoAtualizadaDto` como re-export de Encerrada, `ExecucaoListarDto`, `ExecucaoResumoDto`); `Execucao` model no backend; `ExecucaoRepository` com 8 métodos (inserir, encerrar, buscarIdentificador com `duracaoMinutos` calculado via EXTRACT EPOCH, listar com JOIN em atividade e usuario e filtros opcionais de atividadeId/usuarioId/data, buscarExecucaoAtiva via JOIN com atividade para checar usuario_id, atualizar, excluir, buscarUsuarioExecucao para autorização); `ExecucaoService` com 6 métodos (iniciar com validação de atividade + acesso demanda + concorrência, encerrar com validação fimData e dono, listar com restrição de desenvolvedor às próprias, recuperar, atualizar com regra de dono para desenvolvedor, excluir); `ExecucaoController` com 6 endpoints (POST, GET, GET/:id, PATCH/:id/encerrar, PUT/:id, DELETE/:id gestor); `ExecucaoModule` importa `AtividadeModule`; registrado no `AppModule`
- **16-calendario-module** — 6 DTOs em `shared/src/dtos/calendario/` (`DiaNaoUtilCriarDto`, `DiaNaoUtilCriadoDto`, `DiaNaoUtilResumoDto`, `DiaNaoUtilAtualizarDto`, `DiaNaoUtilAtualizadoDto` como re-export de Criado, `CalendarioConsultarDto`); `DiaNaoUtil` model no backend; `CalendarioRepository` com 7 métodos (inserir, buscarIdentificador, listar, atualizar com SET dinâmico, excluir, `ehDiaNaoUtil` com CTE de recorrência anual via EXTRACT MONTH/DAY, `buscarTipoPorData` para retornar tipo do dia ao serviço); `CalendarioService` com 6 métodos (criar, listar, recuperar, atualizar, excluir, `verificarDiaUtil` que detecta fim de semana via `getUTCDay()` e consulta repositório para obter motivo); `CalendarioController` com 6 endpoints (POST gestor, `GET /verificar` com query param `data` declarado antes de `GET /:id`, GET, GET/:id, PUT/:id gestor, DELETE/:id gestor); `CalendarioModule` registrado no `AppModule`
- **17-ponto-module** — 3 DTOs em `shared/src/dtos/ponto/` (`PontoConsultarDto`, `IntervaloDto`, `PontoDiarioDto`); sem repository próprio — injeta `ExecucaoRepository`, `CalendarioRepository` e `UsuarioRepository` diretamente; `PontoService` com `consultarDiario` (autorização, detecção de fim de semana via `getUTCDay()`, verificação de dia não útil via `CalendarioRepository.ehDiaNaoUtil`, obtenção de `motivoNaoUtil` via `buscarTipoPorData` mapeado para português, busca e reordenação ASC das execuções, cálculo de `totalMinutosTrabalhados`, cálculo de intervalos via `calcularIntervalos`, distribuição entre `minutosTrabalhadosDiaUtil` e `minutosTrabalhadosExtra`, cálculo de `saldoMinutos`); `PontoController` com `GET /ponto/diario`; `PontoModule` importa `ExecucaoModule`, `CalendarioModule` e `UsuarioModule`; registrado no `AppModule`
- **18-assistente-module** — 2 DTOs em `shared/src/dtos/assistente/` (`AssistenteDescricaoAuxiliarDto` com validação de `textoOriginal` ≥10 chars, `tipoEntidade` via `@IsIn` e `contextoEntidade`; `AssistenteDescricaoAuxiliadaDto` com `textoOriginal` e `textoAuxiliado`); `AssistenteService` instancia `Anthropic` no construtor via `ConfigService`, monta prompt contextualizado pelo tipo de entidade e chama `messages.create` com modelo e `max_tokens` lidos da configuração; erros da API Anthropic capturados e relançados como `BusinessException`; `AssistenteController` com `POST /assistente/auxiliar-descricao` protegido por `JwtAuthGuard`; `AssistenteModule` importa `ConfigModule`; registrado no `AppModule`
- **19-backend-correcao-nomenclatura** — correção pura de nomenclatura sem nova lógica de negócio: (1) 14 DTOs Atualizar/Atualizado renomeados para Alterar/Alterado em `shared/src/dtos/` (usuario, projeto, demanda, atividade, execucao, calendario, tag); (2) `AtividadeAlteradaDto`, `ExecucaoAlteradaDto`, `DiaNaoUtilAlteradoDto` deixaram de ser alias/re-export e passaram a ter campos próprios; (3) 4 DTOs de validação criados: `UsuarioValidarLoginDto`, `ProjetoValidarCodigoDto`, `DemandaValidarConexaoDto`, `TagValidarNomeDto`; (4) métodos `atualizar()` → `alterar()` em todos os repositórios, services e controllers (7 módulos); (5) métodos `existe*()` → `validar*(dto)` nos repositórios com parâmetro sempre DTO; (6) `DemandaRepository.buscarIdGestoresAtivos()` removido — `DemandaService` usa `UsuarioRepository.listarGestoresAtivos()` diretamente
- **20-backend-correcao-recuperar-dto** — zero primitivos em assinaturas de serviços e repositórios: (1) 34 novos DTOs internos criados em `shared/src/dtos/` para os módulos projeto, demanda, atividade, execucao, calendario, tag e usuario; (2) `buscarIdentificador(id: number)` renomeado para `recuperar(dto: EntidadeRecuperarDto)` em todos os repositórios (tag, calendario, projeto, usuario, atividade, execucao, demanda); (3) todos os helpers com parâmetros primitivos convertidos para DTO (`encerrar`, `buscarExecucaoAtiva`, `buscarUsuarioExecucao`, `alterarSenha`, `excluir`, `inserirDemandaUsuario`, `verificarCriariaCiclo`, `conexaoPertenceADemanda`, `excluirConexao`, `listarTagsDemanda`, `atribuirTagsDemanda`, `removerTagDemanda`, `listarMembrosDemanda`, `atribuirMembroDemanda`, `removerMembroDemanda`, `membroJaAtribuido`, `contarMembrosDemanda`, `listarPorUsuario`); (4) todos os serviços atualizados para usar as novas assinaturas DTO-based (usuario, projeto, tag, calendario, atividade, execucao, demanda, ponto)
- **21-frontend-scaffold** — projeto Angular 21 criado em `frontend/` com: `package.json` com dependências (PrimeNG 21, @primeng/themes, primeicons, d3, tailwindcss, postcss, autoprefixer); `angular.json` com builder `@angular-devkit/build-angular:application`, SCSS como estilo padrão; `tsconfig.json` e `tsconfig.app.json`; `tailwind.config.js`; `proxy.conf.json` para `/api → localhost:3000`; `src/styles.scss` com diretivas Tailwind e variáveis SCSS; `environments/environment.ts` e `environment.production.ts` com `ambiente.apiUrl`; `app.config.ts` com `provideRouter`, `provideHttpClient(withInterceptors)`, `provideAnimationsAsync` e `providePrimeNG` com tema Aura; `app.routes.ts` com rota `/autenticacao` (lazy), layout guard e lazy children para 8 módulos; `app.component.ts` standalone com `<router-outlet>`; `core/` e `shared/` com esqueletos de signals, interceptors, guards, componentes e serviços; `modules/` com rotas esqueleto para 8 módulos de negócio
- **22-frontend-core** — implementação completa da infraestrutura cross-cutting do frontend: (1) signal `carregamento` (renomeado de `carregamentoAtivo`) e `usuarioAutenticado` tipado como `UsuarioRecuperadoDto | null` via `@project20/shared`; (2) `authTokenInterceptor` com chave `access_token` e skip de `/autenticacao/login`; (3) `errorHandlerInterceptor` com `MessageService` do PrimeNG, tratando 401 (limpa token + redireciona), 403 (toast erro), 400 (toast com `erros[0]` ou `mensagem`), 404 (toast aviso), demais (toast erro genérico); (4) `loadingInterceptor` atualizado para usar signal `carregamento`; (5) `autenticacaoGuard` com chave `access_token`; (6) `gestorGuard` com `UsuarioTipoEnum.GESTOR`; (7) `AutenticacaoService` com `login()`, `logout()` e `estaAutenticado()`; (8) `LoadingSpinnerComponent` com `p-progressSpinner` do PrimeNG; (9) `ErrorMessageComponent` com `p-message` do PrimeNG; (10) `AssistenteDescricaoComponent` completo com signals `textoAuxiliado`, `carregandoAuxilio`, `mostrarComparacao`, métodos `auxiliar()`, `aceitar()`, `descartar()` e template de comparação lado a lado com `p-panel`; (11) `app.config.ts` com `MessageService` provider; (12) `LayoutComponent` com `<p-toast>` para exibição de mensagens; (13) `TopbarComponent` atualizado para usar `AutenticacaoService.logout()`
- **23-frontend-auth** — tela de login completa: (1) `LoginPage` standalone com `ReactiveFormsModule`, `CardModule`, `InputTextModule`, `PasswordModule`, `ButtonModule`, `MessageModule` do PrimeNG; (2) formulário reativo com signals `carregando` e `erroLogin`; (3) validação inline por campo (`required`, `minLength`) exibida apenas após `touched`; (4) `p-password` com `[feedback]="false"` e `[toggleMask]="true"`; (5) `p-button` com `[loading]="carregando()"`; (6) `p-message` exibindo erro de credenciais via signal; (7) redirecionamento para `/ponto` após login bem-sucedido; (8) `somentePublicoGuard` adicionado ao `autenticacao.guard.ts` — redireciona para `/` se já autenticado; (9) `app.routes.ts` atualizado para aplicar `somentePublicoGuard` na rota `/autenticacao`
- **24-frontend-usuario** — módulo de usuário completo: (1) `UsuarioService` com 6 métodos HTTP (`listar`, `criar`, `recuperar`, `alterar`, `excluir`, `alterarSenha`); (2) `usuario.routes.ts` com lazy loading para 4 rotas (`''`, `'novo'`, `':id'`, `':id/anotacoes'`); (3) `UsuarioListagemPage` com `p-table` lazy+paginada, filtros por tipo/status via `p-select` com Reactive Forms, badges `p-tag`, confirmação de exclusão via `p-confirmDialog`; (4) `UsuarioFormularioPage` para criação com todos os campos, `p-select` para tipo e horas diárias; (5) `UsuarioPerfilPage` com exibição de dados, `p-dialog` de edição e `p-dialog` de troca de senha com validador customizado `senhasNaoConferem`, permissão via `UsuarioSessaoService.eGestor()`; (6) `UsuarioAnotacoesPage` com `p-editor` (EditorModule) em Reactive Form, redirecionamento automático se não for o próprio usuário; (7) `UsuarioCartaoComponent` com avatar por iniciais, nome e cargo — para uso em listagens de membros
- **25-frontend-projeto** — módulos de projeto e tag completos: (1) `ProjetoService` com 5 métodos HTTP (`listar`, `criar`, `recuperar`, `alterar`, `excluir`); (2) `projeto.routes.ts` com rotas lazy para listagem (livre), formulário (`gestorGuard`) e detalhe; (3) `ProjetoListagemPage` com grid de cards — barra colorida no topo do card (`cor`), badge de status com severidade (verde/amarelo/azul/vermelho), filtro por status via `p-select`, botão "Novo Projeto" condicional para gestor, paginação com `p-paginator`, confirmação de exclusão; (4) `ProjetoFormularioPage` para criação com `p-colorpicker` (formato hex), `p-datepicker` para datas, validação cross-field `previsaoFimData > inicioData`, código uppercase via SCSS; (5) `ProjetoDetalhePage` com header contendo bolinha de cor + nome + código + badge de status, abas `p-tabs` com "Demandas" (lista das demandas via chamada HTTP direta a `/demanda?projetoId=X`) e "Informações" (dados completos), `p-dialog` de edição para gestores, botão "Nova Demanda" com queryParam `projetoId`; (6) `TagService` com 5 métodos HTTP; (7) `tag.routes.ts` com rota única protegida por `gestorGuard`; (8) `TagListagemPage` com `p-table`, preview de tag com cor (bolinha + badge estilizado com opacidade), dialogs para criação e edição com `p-colorpicker`, prévia em tempo real da tag, confirmação de exclusão
- **27-frontend-atividade** — módulo de atividade completo: (1) `AtividadeService` com 7 métodos HTTP (`listar` com query `demandaId`, `criar`, `recuperar`, `alterar`, `excluir`, `alterarTags`, `listarTags`); (2) `atividade.model.ts` com `ATIVIDADE_STATUS_OPCOES`, `ATIVIDADE_STATUS_FILTRO_OPCOES` e helpers `severidadeStatusAtividade`/`rotuloStatusAtividade` (PLANEJADA→secondary/cinza, PENDENTE→warn/amarelo, DESENVOLVENDO→info/azul, DESENVOLVIDA→success/verde); (3) `atividade.routes.ts` com 3 rotas lazy (`''` listagem, `'nova'` formulário, `':id'` detalhe); (4) `AtividadeListagemPage` recebe `demandaId` via query param, `p-table` lazy+paginada, filtro por status, executor com avatar de iniciais, badge de status, coluna de tags (chips), exclusão restrita a gestor; (5) `AtividadeFormularioPage` para criação com demanda pré-selecionada (nome carregado via `DemandaService.recuperar`), `AssistenteDescricaoComponent`, status, `p-inputNumber` para ordem de exibição (executor definido pelo backend); (6) `AtividadeDetalhePage` com header (nome, status editável via `p-select` inline, executor, link da demanda), descrição editável via dialog com assistente, tags como chips editáveis por gestor via `p-multiSelect`, botão "Iniciar Execução" (dialog para descrição → POST `/execucao` → navega para `/execucao`) e lista das 5 execuções recentes com link para histórico completo
- **28-frontend-execucao** — módulo de execução completo: (1) `ExecucaoService` com 6 métodos HTTP (`iniciar`, `encerrar` via `PATCH /:id/encerrar`, `listar` com query params `atividadeId`/`usuarioId`/`data`/paginação, `recuperar`, `alterar` via `PUT /:id`, `buscarAtiva` via `GET /execucao/ativa`); (2) `execucao.model.ts` com funções puras `segundosDecorridos(inicioData)` (clamp em 0 contra relógio atrasado) e `formatarRelogio(segundos)` (formato `HH:mm:ss`); (3) `ExecucaoTimerComponent` standalone com `@Input({ required: true }) inicioData`, signal `tempoDecorrido`, `setInterval` a cada segundo e `clearInterval` no `OnDestroy`, reaproveitando os helpers do model; (4) `ExecucaoAtivaPage` — no init chama `buscarAtiva()`: se há execução ativa exibe `execucao-timer` + links de atividade (`/atividade/:id`) e demanda (`/demanda?projetoId`) carregados via `AtividadeService.recuperar`+`DemandaService.recuperar`, textarea de descrição editável salva ao encerrar (`AssistenteDescricaoComponent` integrado) e botão "Encerrar"; se não há, exibe formulário de início com `p-autoComplete` de atividades (busca por nome via `AtividadeService.listar({ busca })` — backend já restringe desenvolvedor às próprias), descrição com assistente e botão "Iniciar"; (5) `ExecucaoHistoricoPage` — `p-table` lazy/paginada server-side com colunas atividade (link), data, início, fim, duração (pipe `minutosParagHoras`), descrição (truncada com tooltip) e usuário (só gestor); filtros `p-datepicker` (data), `p-autoComplete` (atividade) e `p-select` de usuário (só gestor); clique na linha abre dialog de edição de descrição (`PUT /:id`) restrito a gestor; (6) `execucao.routes.ts` com 2 rotas lazy (`''` → ativa, `'historico'` → histórico)
- **29-frontend-ponto** (redesenhada) — módulo de ponto em dois modos, backend + frontend. **Backend:** (1) DTOs no shared — `PontoTodosConsultarDto` (`{data}`), `PontoMensalConsultarDto` (`{ano, mes, usuarioId?}`, validado com `@IsNumber`/`@Min`/`@Max`), `PontoDiaResumoDto`, `PontoMensalDto`, e internos `ExecucaoDiaAgruparDto`/`ExecucaoDiaResumoDto`, `CalendarioMesConsultarDto`/`CalendarioDiaNaoUtilMesDto`; (2) `ExecucaoRepository.agruparPorDia({usuarioId, dataInicio, dataFim})` — `GROUP BY DATE(inicio_data)` com `MIN(inicio)`, `MAX(fim)` e soma de minutos só de execuções encerradas; (3) `CalendarioRepository.listarDiasNaoUteisDoMes({ano, mes})` — `EXTRACT(DAY)` dos dias não úteis exatos do ano/mês + recorrentes do mês, com tipo; (4) `UsuarioRepository.listarAtivosComMeta()` — id/nome/`horasDiariasNecessarias` dos ativos; (5) `PontoService` refatorado em helpers privados `resolverInfoDia(data)` (status útil + motivo, calculado uma vez por data) e `montarPontoDiario(usuario, data, infoDia)`; `consultarTodos` (lista ativos, resolve infoDia de hoje uma vez, monta o diário de cada — @GestorOnly) e `consultarMensal` (constrói todos os dias do mês mesclando agregação + dias não úteis; `metaMinutosMes = horasDiarias × diasUteisMes` do mês inteiro; `saldoMinutosMes = trabalhado − meta`); (6) `PontoController` com `GET /ponto/todos` (@GestorOnly) e `GET /ponto/mensal`, além do `GET /ponto/diario` mantido. **Frontend:** (7) `PontoService` com `consultarDiario`/`consultarTodos`/`consultarMensal`; (8) `PontoPage` (`pages/ponto/`) container que decide o modo via `sessao.eGestor()` + filtro de usuário — gestor sem filtro → grid de `app-ponto-usuario-card` (todos hoje, sem seletor de data); gestor com filtro **ou** desenvolvedor → mensal com `p-datepicker view="month"` (`maxDate`=mês atual), 3 `app-ponto-resumo-card` (Meta/Trabalhado/Saldo do mês) + lista de `app-ponto-mes-dia`; recarrega via `@HostListener('document:visibilitychange')`; (9) `PontoUsuarioCardComponent` — card compacto (nome, total, saldo do dia colorido, mini-lista de execuções com `ExecucaoTimerComponent` ao vivo, contagem de intervalos), `.ts`+`.html`+`.scss`; (10) `PontoMesDiaComponent` — linha de dia (semana/número, período início→fim, total, saldo) expansível só quando há registro → `consultarDiario({data, usuarioId})` sob demanda → `p-timeline` + `app-ponto-intervalo-lista`; (11) `PontoResumoCardComponent` (`@Input` rotulo/valor/icone/cor) e `PontoIntervaloListaComponent` (template inline, título "Intervalos detectados (≥ X min)") reaproveitados; (12) `ponto.model.ts` com helpers puros `formatarSaldoMinutos`/`abreviacaoDiaSemana`/`numeroDoDia`; (13) `intervaloMinimoMinutos: 15` em `environment.ts`/`environment.production.ts`; rota lazy `''` → `PontoPage`
- **30-frontend-calendario** — módulo de calendário no frontend (CRUD de dias não úteis + visualização): (1) `CalendarioService` com 6 métodos HTTP (`listar`, `criar`, `recuperar`, `alterar`, `excluir`, `verificarDiaUtil`); (2) `dia-nao-util.model.ts` com `DIA_NAO_UTIL_TIPO_OPCOES`, helpers `severidadeTipoDiaNaoUtil`/`rotuloTipoDiaNaoUtil` (FERIADO→danger, RECESSO→warn, PONTO_FACULTATIVO→info), `formatarDataIso` (Date→`YYYY-MM-DD` local) e `partesDaData` (ISO→ano/mês/dia tratando date-only como meio-dia local), além da interface `VerificacaoDiaUtil`; (3) `calendario.routes.ts` com rota lazy única `''`; (4) `CalendarioListagemPage` com **layout unificado**: verificador de dia útil no topo (resultado verde/vermelho) e, abaixo, duas colunas — `p-datepicker [inline]` à esquerda (com `pTemplate="date"` marcando dias não úteis via mapas computados, exatos por `ano-mes-dia` e recorrentes por `mes-dia`) e, à direita, `p-table` com os dias não úteis **do mês exibido no calendário** (`mesExibido` atualizado por `onMonthChange`, lista derivada por `computed` incluindo recorrentes do mês); clicar num dia marcado destaca e rola até a linha correspondente na tabela (`diaDestacadoId` + `scrollIntoView`); dialog de criação/edição (`p-select` de tipo, `p-checkbox` recorrente com `p-message` de aviso, data desabilitada na edição); ações de escrita restritas a gestor via `eGestor`
- **26-frontend-demanda** — módulo de demanda completo: (1) `DemandaGrafoNoDto` e `DemandaGrafoArestaDto` no shared atualizados com `horasEstimadas`, `demandaPaiId`, `id` e `tipo`; (2) backend `DemandaRepository.recuperarGrafo` atualizado com UNION ALL combinando arestas de `demanda_conexao` (tipo `conexao`) e relações pai→filho via `demanda_pai_id` (tipo `hierarquia`); (3) `DemandaService` com 13 métodos HTTP incluindo `listarTags`, `criarConexao`, `excluirConexao`, `listarMembros`, `atribuirMembro`, `removerMembro`; (4) `DemandaGrafoComponent` com D3 v7 force simulation, zoom/pan, drag, nós circulares coloridos por status com raio proporcional a `horasEstimadas`, arestas hierarquia (azul) vs conexão (cinza tracejado), estética Obsidian dark; (5) `DemandaArvoreItemComponent` recursivo via `forwardRef`, exibe badge de status/prioridade, horas e filhos aninhados; (6) `DemandaConexaoListaComponent` para CRUD de conexões; (7) `DemandaMembroListaComponent` para CRUD de membros; (8) `DemandaProjetoPage` com toggle grafo/lista, painel lateral de filtros e legenda, `construirArvore()` O(n) via Map a partir do grafo; (9) `DemandaFormularioPage` para criação e edição com 3 abas de descrição + `AssistenteDescricaoComponent`; (10) `DemandaDetalhePage` com 6 abas (Visão Geral, Sub-demandas, Atividades, Conexões, Tags, Membros), `forkJoin` para carga paralela
- **31-atividade-linha-sem-visualizacao** — refinamento de UX em `AtividadeListagemPage` (somente frontend): removido o handler `(click)="abrirAtividade(atividade)"` e a classe `atividade-listagem__linha` do `<tr>`, removido o `(click)="$event.stopPropagation()"` da célula de ações (código morto) e o bloco SCSS `&__linha` (cursor pointer + hover); a dialog de visualização passa a abrir exclusivamente pelo ícone de olho (`pi pi-eye`) da coluna de ações
- **32-atividade-status-inline** — troca de status inline pelo chip em `AtividadeListagemPage` (somente frontend): o `p-tag` de status vira clicável (`<button>` envolvente + seta `pi pi-chevron-down`) apenas para quem pode editar (gestor ou desenvolvedor dono); clique abre `p-popover` (`#statusPopover`, `PopoverModule`) com as 4 opções de `ATIVIDADE_STATUS_OPCOES` destacando o atual; escolher status diferente chama `AtividadeService.alterar(id, { status })` e atualiza a linha in-memory no signal `atividades()` (sem refazer a busca) + toast; em erro mantém o status anterior; novo helper `podeEditar` (delega a `podeExecutar`), métodos `abrirSeletorStatus`/`alterarStatus`/`severidadeOpcao`, signal `atividadeStatusEdicao`; chip somente leitura para quem não pode editar
- **33-atividade-atribuir-tags** — atribuir tags na listagem (frontend) + ajuste de permissão no backend. **Backend:** `PUT /atividade/:id/tag` deixou de ser `@GestorOnly`, passou a injetar `@ActiveUser() usuarioAtivo: JwtPayload` e repassá-lo a `alterarTags`; `AtividadeService.alterarTags` recebe `usuarioAtivo` e aplica a mesma autorização de `alterar()` (DESENVOLVEDOR precisa ser autor ou ter acesso à demanda via `validarAcessoDemanda`, senão `UnauthorizedAccessException`); ApiOperation/ApiResponse atualizados; sem mudança em DTO/repositório/migrations. **Frontend (`AtividadeListagemPage`):** botão `pi pi-plus-circle` ("Atribuir tags") na coluna Ações visível via `podeEditar`; dialog com `p-multiSelect` de todas as tags (`TagService.listar()` injetado, cacheado em `todasAsTags`), pré-seleciona as tags atuais via `tagsDe`, cada opção com bolinha de cor (`pTemplate="item"`); salvar via `AtividadeService.alterarTags(id, { tagIds })` recarrega só as tags daquela atividade em `tagsPorAtividade` + toast; novos signals `mostrarDialogTags`/`salvandoTags`/`carregandoTags`/`todasAsTags`/`atividadeTags`, form `formularioTags`, métodos `abrirDialogTags`/`salvarTags`/`carregarTodasAsTags`/`recarregarTagsDaAtividade`
- **34-atividade-descricoes-demanda** — botões das três descrições da demanda na listagem de atividades + edição inline. **Backend:** `AtividadeResumoDto` ganhou 3 flags (`demandaTemDescricaoCliente`/`demandaTemDescricaoTecnica`/`demandaTemDocumentacao`) derivadas em `AtividadeRepository.listar` via `COALESCE(TRIM(demanda.<campo>), '') <> ''`. **Frontend (`AtividadeListagemPage`):** na coluna **Demanda**, à esquerda do nome, três `p-button` pequenos distintos — `pi pi-user` (cliente/`info`), `pi pi-code` (técnica/`help`), `pi pi-book` (documentação/`warn`); cor de destaque só quando há conteúdo (`severidadeIconeDescricao`, senão `secondary`), 3 botões sempre visíveis. Clique → `abrirDescricaoDemanda` carrega a demanda via `DemandaService.recuperar` com cache por `demandaId` (`cacheDemandas`) e abre `p-dialog` exibindo o campo como HTML via `[innerHTML]`; cabeçalho `nomeDemanda — rótulo`, corpo "Sem conteúdo" quando vazio. **Edição inline somente gestor + somente `descricaoTecnica`/`documentacao`** (`descricaoCliente` read-only): footer "Editar" → `p-editor`+`app-assistente-descricao` → "Salvar" via `DemandaService.alterar(demandaId, { [campo]: valor })` atualizando cache, `demandaDescricao` e flag em memória (`atualizarFlagDescricao`). Novos: tipo `CampoDescricaoDemanda`, signals de dialog/edição, form `formularioDescricaoDemanda`, `camposEditaveis`, `rotulosDescricao`, métodos `abrirDescricaoDemanda`/`cabecalhoDescricao`/`conteudoDescricao`/`severidadeIconeDescricao`/`podeEditarCampoDescricao`/`abrirEdicaoDescricao`/`salvarDescricaoDemanda`/`aceitarDescricaoAssistenteDemanda`; SCSS `&__demanda`/`&__demanda-nome`/`&__demanda-acoes`/`&__descricao-rica`
- **36-atividade-tempo-executado** — coluna "Tempo executado" na listagem de atividades (backend shared DTO + repositório, frontend coluna; sem migration). **Backend:** `AtividadeResumoDto` ganhou `totalMinutosExecutados: number`; `AtividadeRepository.listar` calcula o total no SELECT via subconsulta escalar correlacionada `(SELECT COALESCE(SUM(EXTRACT(EPOCH FROM (execucao.fim_data - execucao.inicio_data)) / 60), 0)::int FROM execucao WHERE execucao.atividade_id = atividade.id AND execucao.is_deleted = false AND execucao.fim_data IS NOT NULL)` — execuções em andamento ficam de fora, atividades sem execução retornam 0, e o `COUNT(atividade.id)` da paginação permanece intocado. **Frontend (`AtividadeListagemPage`):** nova `<th>Tempo executado</th>` entre Status e Tags, célula exibindo `atividade.totalMinutosExecutados | minutosParagHoras` (pipe já importado); `colspan` do template `empty` ajustado de `7/6` para `8/7`
- **39-demanda-desenvolvedor-editar-descricoes** — desenvolvedor atribuído passa a **visualizar as três descrições** e a **editar técnica/documentação** (tela de demanda e tela de atividades); edição da **descrição do cliente** permanece exclusiva do gestor. **Backend (`DemandaService.alterar`):** regra de campo — `DESENVOLVEDOR` + `dto.descricaoCliente !== undefined` → `UnauthorizedAccessException('Apenas gestores podem alterar a descrição do cliente')` (`!== undefined` barra também limpeza com `''`/`null`); demais campos seguem liberados ao desenvolvedor atribuído; controller sem `@GestorOnly` (gating fino por campo). **Frontend (`demanda-detalhe-dialog`):** três botões de descrição saíram do `@if (sessao.eGestor())` e ficam sempre visíveis (Editar/excluir continuam gestor-only no mesmo `__cabecalho-acoes`); novo `podeEditarDescricao(campo)` = `eGestor() || campo !== 'descricaoCliente'`, `computed` `podeEditarDescricaoAtual`, guard em `salvarDescricao()`; dialog de descrição com **modo somente-leitura** (`@else`) — `&__descricao-leitura` (`white-space: pre-wrap`) ou `&__descricao-vazia` ("Sem conteúdo"), sem assistente, footer só "Fechar". **Frontend (`atividade-listagem`):** `podeEditarCampoDescricao()` perdeu a exigência de `eGestor()`, virou `!!campo && camposEditaveis.includes(campo) && !!demandaDescricao()?.podeEditar`. **+ Correção de acesso (além da spec):** desenvolvedor passou a **visualizar** qualquer demanda de projeto a que tem acesso (corrige "Demanda não encontrado" ao abrir, pelo grafo/árvore, demanda da qual não é membro) e a **editar só as que é membro** — `DemandaRepository.recuperar` com filtro de desenvolvedor por **acesso ao projeto** (`EXISTS` em `demanda_usuario` do mesmo `projeto_id`), `DemandaRecuperadaDto.podeEditar`, gate de `validarMembro` em `alterar`/`criarConexao`, e gating de edição no frontend por `podeEditar`. **Com migration? Não. Com DTO:** `DemandaRecuperadaDto` ganhou `podeEditar`
- **37-execucao-data-hora-inicio-fim** — data + horário de início e fim na listagem de execuções (somente frontend, template + estilo). Nas colunas **"Início"** e **"Fim"** de `ExecucaoHistoricoPage`, cada célula exibe um bloco `&__momento` (flex coluna) com a **data** (`date: 'dd/MM/yy'`, `&__momento-data` menor/`--p-text-muted-color`) acima do **horário** (`date: 'HH:mm'`, `&__momento-hora` maior/`font-weight: 600`), tornando evidente a virada de dia (início num dia, fim no seguinte). Coluna **Fim** mantém o `<span>` "Em andamento" quando `fimData` é nulo. Dialog de edição (gestor, `p-datepicker [showTime]`) intocado. Sem backend, DTO ou migration
- **38-calendario-dia-meio-periodo** — dia não útil de **meio período** (atributo de duração independente do tipo, sem distinção manhã/tarde) que reduz a meta diária à metade. **Shared:** enum `DiaNaoUtilDuracaoEnum` (`INTEGRAL`/`MEIO_PERIODO`) + campo `duracao` em todos os DTOs de `dia_nao_util` e em `CalendarioDiaNaoUtilMesDto`. **Migration** `20240014` adiciona `duracao VARCHAR(20)` com backfill `INTEGRAL` (sem DEFAULT), `NOT NULL` e CHECK. **Backend calendário:** `duracao` em model/repository/service; `recuperarTipo` retorna `{ tipo, duracao }`; `verificarDiaUtil` sufixa motivo com "(meio período)". **Backend ponto:** `InfoDia.fracaoMeta` ∈ {0, 0.5, 1}; `montarPontoDiario` aplica meta pela metade com capacidade útil limitada (saldo do dia nunca positivo no meio período) e preserva exatamente 0/1; `consultarMensal` soma frações de dia útil para `metaMinutosMes`/`diasUteisMes`. **Frontend calendário:** opções/rótulo de duração, `p-select` de Duração no dialog, coluna "Duração" (badge "½ dia") e sufixo no tooltip; ponto exibe motivo/saldo já prontos do backend. **Acréscimos:** i18n pt-BR global dos `p-datepicker` (`core/config/primeng-traducao-pt-br.ts` via `providePrimeNG`) e duplo clique no dia do calendário abrindo edição (data com feriado) ou criação pré-preenchida (data livre), só gestor
- **35-atividade-execucoes-compactas** — lista de execuções compacta em `AtividadeListagemPage` (somente frontend, template + estilo). As duas listas de "Últimas execuções" (aba da dialog de visualizar e seção da dialog de iniciar/encerrar execução) passaram a renderizar cada execução em **uma única linha** `data · duração · descrição`: removida a `<div class="&__execucao-cabecalho">` (classe eliminada de HTML e SCSS), descrição migrada de `<p>` para `<span>`. O `&__execucao-item` virou `display: flex; align-items: baseline; gap: 0.5rem` com `&__execucao-data` (`flex-shrink: 0`, `white-space: nowrap`) e `&__execucao-descricao` truncada (`flex: 1`, `min-width: 0`, `white-space: nowrap` + `overflow: hidden` + `text-overflow: ellipsis`) com `[pTooltip]="execucao.descricao"` `tooltipPosition="top"` (TooltipModule já importado na página). Duração via `minutosParagHoras`; em andamento mantém o indicador azul. Lista mais densa (`gap: 0.25rem`, item `0.25rem 0.5rem`, sem borda/fundo por item — só hover `surface-50`), `max-height: 16rem` + scroll preservados
- **40-atividade-registrar-execucao-manual** — gestor registra, pela listagem de atividades, uma execução **já encerrada** (início, fim, descrição) para o dono da atividade, com validação de sobreposição. Sem migration. **Shared:** 3 DTOs em `execucao/` — `ExecucaoRegistrarDto` (entrada, `inicioData`/`fimData` como `@IsDateString`), `ExecucaoRegistradaDto` (saída, campos próprios), `ExecucaoSobreposicaoValidarDto` (interno). **Backend:** `ExecucaoRepository.registrar()` (`INSERT ... SELECT` com `inicio_data`/`fim_data` preenchidos + `duracaoMinutos`; `inserir` original com `fim_data = NULL` intocado) e `validarSobreposicao()` (`SELECT EXISTS` por `atividade.usuario_id`, sobreposição `inicio_data < :fimData AND :inicioData < COALESCE(fim_data, 'infinity'::timestamptz)` — em andamento estende ao infinito); `ExecucaoService.registrar(dto, usuarioAtivo)` valida atividade (404), datas não no futuro, `fim > inicio` e sobreposição contra o dono, retorna `ExecucaoRegistradaDto` (regra de execução ativa única **não** se aplica); `POST /execucao/registro` `@GestorOnly()` no controller (sem conflito com `POST /execucao`). **Frontend (`AtividadeListagemPage`):** `ExecucaoService.registrar` → `POST /execucao/registro`; novo botão Ações só-gestor `pi pi-history`/`severity="help"` (botão de tags ganhou `severity="secondary"`); dialog "Registrar execução" com `p-datepicker [showTime]` (início/fim) + `pTextarea` + `app-assistente-descricao`, validador cross-field `fimPosteriorAoInicio`, envia datas ISO e em sucesso recarrega a tabela (coluna Tempo executado reflete); injeção de `ExecucaoService`, signals/métodos `*Registro`
- **41-execucao-colunas-projeto-demanda-total-dia** — colunas **Projeto** e **Demanda** na listagem de execuções + **soma de todos os tempos do dia filtrado** no rodapé. Sem migration. **Shared:** `ExecucaoResumoDto` ganhou `demandaId`/`nomeDemanda`/`projetoId`/`nomeProjeto`; novo `ExecucaoListaDto` (campos de paginação + `totalMinutosDia`, sem re-export de `PaginatedResult`), exportado no barrel. **Backend (`ExecucaoRepository.listar`):** JOINs `demanda` (por `atividade.demanda_id`) e `projeto` (por `demanda.projeto_id`) nas três queries (COUNT, total e itens) com `is_deleted = false` de ambos; SELECT de itens acrescido de `demanda.id/nome` e `projeto.id/nome`; **query separada do total do dia** somando a mesma expressão de duração da coluna (`COALESCE(fim_data, NOW())`, em andamento conta até agora) sob a **mesma cláusula WHERE** e sem paginação; retorno `{ itens, total, totalMinutosDia }`. `ExecucaoService.listar` retorna `StandardResponse<ExecucaoListaDto>` repassando `totalMinutosDia`; escopo do desenvolvedor inalterado (o total respeita o filtro). **Frontend (`ExecucaoHistoricoPage`):** `ExecucaoService.listar` tipado com `ExecucaoListaDto`; signal `totalMinutosDia` setado no `next`; colunas Projeto/Demanda (texto) antes de Atividade; `<ng-template #footer>` "Total do dia" (`colspan` 6/5) + valor `| minutosParagHoras` sob Duração + célula final (`colspan` 2/1); `emptymessage` de `7/5` → `9/7`; SCSS `&__rodape-rotulo`/`&__rodape-valor`
- **68-atividade-gestor-editar-nome** — gestor edita o **nome da atividade inline no título** do `AtividadeVisualizarDialogComponent` (corrigir nome digitado errado pelo dev). **Frontend apenas** (backend já aceita `nome` e libera gestor — specs 14/46). Header do `p-dialog` migrou de `[header]` string para `<ng-template #header>`: modo leitura (`__titulo` + lápis `__editar-nome` só `@if (sessao.eGestor())`) / modo edição (`formularioNome` required/min3/max255, `input pInputText` + ✔/✗, `__nome-erro`). Estado `editandoNome`/`salvandoNome`, métodos `iniciarEdicaoNome`/`cancelarEdicaoNome`/`salvarNome` (espelham `salvarDescricao`), output `aoAlterar` assinado **só** pela `AtividadeListagemPage` (`buscarAtividades()`); histórico/ponto não recarregam (decisão de escopo). Sem backend/shared/migration
- **44-auditoria-conformidade-padroes** — task de **auditoria + geração de specs** (não altera código de produção). Varreu `shared/`/`backend/`/`frontend/`/migrations/`docs/` contra as 25 Proibições do `SYSTEM.SPEC.md` §16 e o `CONVENTIONS.md`. Produziu **`docs/AUDITORIA.md`** (sumário por módulo e categoria + tabela detalhada + verificações negativas + lista de specs). Achados: primitivo `id` em `repository.alterar` de **usuario/projeto/demanda/atividade/calendario/tag** (E §16 #21; `ExecucaoRepository.alterar(dto)` é a referência conforme); `fn_atualizar_updated_date` em pt-BR (A §16 #12); DTOs de relatório de **ponto** sem verbo-particípio (B §5.1 — lacuna doc); contradição cross-cutting §7.2 × §16 #21. Conforme sem spec: execucao, autenticacao, assistente e **todo o frontend**. **Gerou 8 specs no backlog (48–55)**, uma por módulo afetado, cada uma com reforço documental obrigatório
- **80-projeto-relatorio-execucoes** — novo **módulo `relatorio`** (backend controller+service sem repositório próprio, no padrão do `ponto`) + shared (2 enums `RelatorioPeriodoTipoEnum`/`RelatorioFormatoEnum` + 8 DTOs em `dtos/relatorio/`) + frontend (`RelatorioService` + `RelatorioExecucaoDialogComponent` + botão só-gestor no `projeto-detalhe`). Gestor gera relatório de **todas as execuções** de um projeto por período (**ANUAL**/**MENSAL**/**CUSTOM**), pré-visualiza em `p-table`, **baixa CSV** (BOM UTF-8, colunas fixas, `StreamableFile` via `@Res` passthrough — `ResponseFormatInterceptor` ganhou guarda p/ não envelopar binário) e dispõe da base (**groundwork**) da **revisão por IA** (`POST /relatorio/execucao/revisar` → Anthropic via `ConfigService`, parse defensivo de JSON). `ExecucaoRepository.listarParaRelatorio` reaproveita a cadeia `execucao→…→projeto` filtrando projeto+período (duração `COALESCE(fim_data, NOW())` pós-75; `statusAtividade` via JOIN `tipo_atividade_status` pós-79). **Todas as rotas `@GestorOnly()`**. **XLSX** fica de follow-up (sem `exceljs` — `formato=XLSX` → `BusinessException`; front só oferece CSV). Sem migration. Stub do front (`class-validator.stub.ts`) ganhou `IsInt`/`ValidateIf`

---

## Em Andamento

*Nenhuma task em andamento.*

---

## Próxima Task

Definir a próxima task a partir do backlog (`docs/specs/backlog/`). A spec **80-projeto-relatorio-execucoes** foi concluída nesta sessão (shared + backend + frontend; spec movida para `done/`). Backlog pendente (em ordem de execução): `81`, `82`, `83`.

> Cada spec referenciou `docs/AUDITORIA.md` e a linha do achado, e incluiu o passo obrigatório de **Atualização de Documentação** (fonte da verdade) para impedir recorrência. Padrão **estrutural** das correções de assinatura: `ExecucaoRepository.alterar(dto)`; padrão de **nomenclatura** (verbo no fim, §5): `EntidadeInternoAlterarDto` — a spec 56 corrigiu o próprio `ExecucaoAlterarInternoDto` → `ExecucaoInternoAlterarDto` (errata `docs/AUDITORIA.md` §8).

---

## Estrutura de Pastas Atual

```
project-2.0/
  docs/               ✅ criado
  backend/            ✅ criado (task 02)
  frontend/           ✅ criado (task 21)
  shared/             ✅ criado (task 01)
  docker-compose.yml  ✅ criado
  package.json        ✅ criado
  .env                ✅ criado
  .env.example        ✅ criado
  .gitignore          ✅ criado
```

---

## Módulos do Backend

| Módulo | Status |
|---|---|
| core (base, exceptions, filters, interceptors) | ✅ implementado (task 04) |
| config | ✅ implementado (task 05) |
| database (DatabaseModule + Knex) | ✅ implementado (task 02) |
| autenticacao | ✅ implementado (task 07) |
| usuario | ✅ implementado (task 06) |
| projeto | ✅ implementado (task 09) |
| demanda | ✅ implementado (task 10) |
| atividade | ✅ implementado (task 14) |
| execucao | ✅ implementado (task 15) |
| ponto | ✅ implementado (task 17) |
| calendario | ✅ implementado (task 16) |
| tag | ✅ implementado (task 08) |
| assistente | ✅ implementado (task 18) |
| relatorio | ✅ implementado (task 80) — sem repositório próprio (consome execucao/projeto) |

---

## Módulos do Frontend

| Módulo | Status |
|---|---|
| core (interceptors, guards, signals) | ✅ implementado (task 22) |
| shared (components, pipes) | ✅ implementado (task 22) |
| autenticacao | ✅ implementado (task 23) |
| usuario | ✅ implementado (task 24) |
| projeto | ✅ implementado (task 25) |
| demanda | ✅ implementado (task 26) |
| atividade | ✅ implementado (task 27) |
| execucao | ✅ implementado (task 28) |
| ponto | ✅ implementado (task 29) |
| calendario | ✅ implementado (task 30) |
| tag | ✅ implementado (task 25) |
| relatorio | ✅ implementado (task 80) — dialog de relatório de execuções na tela de projeto |

---

## Banco de Dados

| Item | Status |
|---|---|
| Migrations criadas | ✅ `20240001`–`20240025`: 12 de criação (task 03) + `20240013` (status demanda) + `20240014` (duração dia não útil, task 38) + `20240015` (seed gestor inicial) + `20240016` (rename `fn_set_updated_date`) + `20240017`/`20240018` (seed tags padrão) + `20240019` (remover `ordem_exibicao`) + `20240020` (padronização de nomes de constraints — `chk_demanda_status`, `chk_dia_nao_util_duracao`, task 70) + `20240021` (seed dos 9 feriados nacionais de data fixa em `dia_nao_util`, task 74) + `20240022` (colunas de data/hora → `timestamptz`, correção de fuso, task 75) + `20240023` (remover coluna `demanda.prioridade` + índice `ix_demanda_prioridade`, task 76) + `20240024` (adicionar `usuario.anotacoes_alteracao_data TIMESTAMPTZ` nullable, task 77) + `20240025` (7 enums `VARCHAR + CHECK` → tabelas de referência `tipo_*` + colunas FK, task 79). |
| Tabelas no banco | ✅ 11 tabelas de negócio + 7 de referência (`tipo_*`, task 79) + trigger fn (task 03); `dia_nao_util.duracao` adicionado (task 38); colunas de data/hora (`*_date` de auditoria + `execucao.inicio_data/fim_data`) em `timestamptz` com sessão fixada em `APP_TIMEZONE` (task 75); colunas de enum agora `INTEGER` FK para as tabelas de referência (task 79) |
| Conexão configurada | ✅ DatabaseModule com Knex (task 02) |

---

## Decisões Tomadas

> Decisões registradas ao longo das sessões de implementação.

- **[revisão pós-18]** Junction tables (`demanda_tag`, `atividade_tag`, `demanda_usuario`) ficam no módulo pai — sem módulo NestJS próprio
- **[revisão pós-18]** `atualizar`/`atualizado` renomeado para `alterar`/`alterado` em toda nomenclatura de DTOs e métodos de negócio
- **[revisão pós-18]** Métodos de existência (`existe*`) renomeados para `validar*` — método é sempre verbo de ação
- **[revisão pós-18]** Zero primitivos em qualquer assinatura de método de service ou repository — sempre DTO mesmo para um único campo
- **[revisão pós-18]** Nenhum DTO pode ser alias/re-export de outro — campos próprios obrigatórios em cada DTO
- **[revisão pós-18]** Recuperação individual de entidade sempre via `EntidadeRecuperarDto { id: number }` e método `recuperar()`
- **[task 48 — reconciliação §7.2 × §16 #21]** **DTO em tudo** — service e repository **nunca** recebem `id`/`*Id` primitivo; o **controller** é o único ponto autorizado a montar o DTO: ao extrair `@Param('id')` (ou `@Query`), injeta o `id` no DTO antes de repassar (`recuperar({ id }, …)`, `alterar({ ...dto, id }, …)`). Objetos de contexto (`usuarioAtivo: JwtPayload` via `@ActiveUser()`) não são primitivos e permanecem como parâmetro próprio. Aplicada ao módulo canônico `usuario`; as specs 49–53 e 56 replicam o padrão sem reabrir a discussão. SPEC §7.2/§7.4/§16 #21 e CONVENTIONS já reescritos.
- **[task 48]** Padrão de nomenclatura **verbo-no-fim** para DTO interno: o qualificador `Interno` faz parte do complemento → `UsuarioInternoAlterarDto`/`UsuarioSenhaInternoAlterarDto` ✅ (nunca `UsuarioAlterarInternoDto`/`UsuarioAlterarSenhaInternoDto` ❌, com o verbo no meio).
- **[task 70]** **Convenção de nomes de objetos de banco** formalizada: padrão `<prefixo>_<tabela>_<proposito>`, prefixo por tipo — `pk_` (primary key), `fk_` (foreign key), `uix_` (unique index), `ix_` (index), `chk_` (check constraint), `trg_` (trigger), `fn_` (function); nomes genéricos em inglês, nunca deixar o PostgreSQL auto-gerar. Constraints de check usam **`chk_`** (não `ck_`). Documentada em `SCHEMA.md` e `CONVENTIONS.md`; as duas exceções (`demanda_status_check`, `ck_dia_nao_util_duracao`) foram alinhadas via migration `20240020` (`RENAME CONSTRAINT`, sem editar as migrations já aplicadas).
- **[task 79]** **Enums como tabelas de referência — FK por id + enum TS mantido.** Os 7 enums de domínio fechado deixaram de ser `VARCHAR + CHECK` e viraram tabelas `tipo_<tabela>_<complemento?>` (BaseEntity + `codigo` + `descricao`); a coluna de negócio é `INTEGER` FK para o `id` da referência. O enum TypeScript de `shared/` permanece como **contrato tipado** (`@IsEnum`, DTOs, frontend) e foi **renomeado** para casar com a tabela (`UsuarioTipoEnum` → `TipoUsuarioEnum`, etc.). A tradução `codigo ⇄ id` é encapsulada **só no repositório** (subselect no INSERT/UPDATE, JOIN expondo `codigo AS <campo>` no SELECT/RETURNING) — DTOs, services, models e frontend continuam usando o **valor do enum**, nunca o id. **Reconciliação spec×banco:** a spec listava `tipo_demanda_status` com `EM_DESENVOLVIMENTO`, mas o estado real (pós-`20240013`) é `PENDENTE/PLANEJADA/CONCLUIDA` — seedado com os valores reais (senão o `UPDATE ... SET ... NOT NULL` falharia). **Fora da lista da spec (necessário):** `execucao.repository.recuperarTipoUsuarioDaAtividade` também lia `usuario.tipo` (cross-module) e ganhou o JOIN em `tipo_usuario`; idem `demanda.repository.listarMembrosDemanda` e `atividade.repository.listar` (`usuario.tipo`/`usuarioTipo`).
- **[task 72]** O `id`/`*Id` injetado pela controller (campo `@IsOptional` no DTO de `@Body`, para o `whitelist` não rejeitar o corpo sem ele) é tipado como **obrigatório no boundary do service** via interseção `& { id: number }` / `& { demandaId: number }` / `& { demandaOrigemId: number }` — eliminando o `!` (non-null assertion) na leitura. Mesmo padrão já aceito no `ExecucaoService.encerrar(dto: ExecucaoEncerrarDto & { id: number }, …)` (exceção C3). `!` de **invariante real** (não injetados pela controller — ex.: `ponto.service` `diaNaoUtil!`, `demanda.service` `raiz!`/`mapa.get()!`) ficam **fora** deste escopo.
- **[task 48 — desvio da letra da spec, registrado]** No `UsuarioService`, `excluir` usa `UsuarioExcluirDto` (DTO dedicado, `id` obrigatório) em vez do `UsuarioRecuperarDto` sugerido (id opcional → atrito de tipo com `repositorio.excluir`); e `alterarSenha` recebe o DTO do corpo com `id` injetado (`UsuarioSenhaAlterarDto & { id }`), **não** `UsuarioSenhaInternoAlterarDto` — este tem `senhaEncriptada` e é input do **repositório**, pois a encriptação (bcrypt) acontece no service. Ambos preservam o boundary "service nunca recebe primitivo".
- **[task 78 — herança de DTO esclarecida]** A proibição #23 foi reescrita: um DTO de **negócio** nunca herda outro DTO de **negócio** (nem subclasse vazia `extends X {}`) — declara os próprios campos explicitamente, mesmo idênticos; mas **herdar um DTO core** (genérico/arquitetural, teste de linguagem §4 — `PaginatedResult<TItem>`, `StandardResponse<TData>`) é **esperado** e evita duplicar os campos genéricos. Por isso `PaginatedResult` virou **classe** (não interface; retornos por object-literal seguem válidos por tipagem estrutural); `StandardResponse` segue interface (nenhum DTO a herda). Aplicada: 6 subclasses vazias de DTO de negócio (`UsuarioAlteradoDto`, `ProjetoRecuperadoDto`, `ProjetoAlteradoDto`, `TagRecuperadaDto`, `TagAlteradaDto`, `DemandaAlteradaDto`) ganharam campos explícitos espelhando o pai (mirror do estado **atual** — `DemandaAlteradaDto` sem `prioridade`/com `podeEditar`, `UsuarioAlteradoDto` com `anotacoesAlteracaoData`, divergindo da lista da spec que era anterior às tasks 76/77). Documentada em SPEC §5.1/§16 #23 e CONVENTIONS.
- **[task 78 — rename do par item/wrapper de execução]** A listagem de execução foi remodelada no padrão item/wrapper: o **item** (13 campos) passou de `ExecucaoResumoDto` → **`ExecucaoItemDto`** (DTO de negócio, campos explícitos); o **wrapper** de listagem deixou de duplicar `PaginatedResult` — `ExecucaoListaDto` foi **removido** e o novo **`ExecucaoResumoDto extends PaginatedResult<ExecucaoItemDto> { totalMinutosDia }`** ocupa o papel de retorno de `listar`. Importadores atualizados (backend: `execucao.repository`/`execucao.service`/`ponto.service` + `PontoDiarioDto`; frontend: `execucao.service`, `atividade.service`, `execucao-historico`, `atividade-listagem`/`atividade-detalhe`/`atividade-visualizar-dialog`). Sem mudança de SQL/campos — só nomes, tipos de retorno e a herança; a forma do JSON de `GET /execucao` (`{ itens, totalItens, …, totalMinutosDia }`) é preservada.
- Todas as decisões de arquitetura estão documentadas em `docs/SYSTEM.SPEC.md`
- Schema completo em `docs/SCHEMA.md`
- Convenções de código em `docs/CONVENTIONS.md`
- `knexfile.ts` usa `path.resolve(__dirname, '../../../.env')` para carregar o `.env` da raiz corretamente; o path relativo `'../.env'` não funcionava porque o Knex muda o cwd para o diretório do knexfile
- Os arquivos em `core/` (base, exceptions, filters, interceptors) foram criados já com implementação conforme `SYSTEM.SPEC.md`, antecipando a task 04
- Os 9 DTO index files em `shared/src/dtos/*/index.ts` estavam vazios (placeholders da task 01), causando erro de build assim que o primeiro import de `@project20/shared` foi adicionado; corrigidos com `export {};` mínimo — serão substituídos com os DTOs reais nas tasks futuras
- Subpath imports (`@project20/shared/dtos/usuario`) não funcionam com o setup atual (sem `exports` field no package.json nem `paths` no tsconfig); todos os imports do shared usam `@project20/shared` diretamente, que resolve para `src/index.ts` via campo `"main"`
- `UsuarioRepository` tem método `buscarComSenha(id)` além do especificado: necessário para o fluxo `alterarSenha` que precisa comparar a senha atual via bcrypt
- Guards globais (`JwtAuthGuard` e `GestorGuard`) registrados via `APP_GUARD` no próprio `AutenticacaoModule` — padrão NestJS que mantém a lógica de auth coesa no módulo correto; `AutenticacaoModule` importado no `AppModule`
- `UsuarioModule` permanece importado diretamente no `AppModule` além de ser importado transitivamente via `AutenticacaoModule`; NestJS deduplica o módulo no grafo, sem duplicação de providers/controllers
- No `ProjetoService.recuperar`, quando o usuário é DESENVOLVEDOR, a verificação de acesso reutiliza `listarPorUsuario` para checar se o projeto aparece na lista do usuário; isso é semanticamente correto mas realiza uma query extra — aceito pois é consistente com a regra de negócio e evita query SQL inline duplicada
- `BaseRepository.executarConsulta` e `executarComando` passaram a aceitar `Knex.Transaction` como terceiro parâmetro opcional; quando fornecido, usa o executor da transação em vez de `conexaoBancoDados` diretamente
- `DemandaRepository.inserirComAtribuicao` centraliza a transação no repositório, mantendo o serviço limpo e sem injeção de Knex; recebe `{ usuarioIds }` e insere uma linha em `demanda_usuario` por usuário atomicamente (task 71: gestor não é mais atribuído — apenas o criador-dev e membros selecionados; lista pode ser vazia)
- `DemandaRepository.buscarIdentificador` e `listar` aceitam `usuarioId` opcional: quando fornecido, fazem JOIN com `demanda_usuario` para filtrar acesso de desenvolvedor — evita método duplicado e mantém o controle de acesso no SQL
- Rota `GET /demanda/grafo` declarada antes de `GET /demanda/:id` no controller para evitar conflito de rotas no NestJS (ParseIntPipe rejeitaria "grafo" mas a ordem garante a rota correta)
- `DemandaGrafoDto` retornava `arestas: []` até a task 11; a task 12 atualizou `recuperarGrafo` para incluir arestas reais via JOIN com `demanda_conexao` (filtrado por projeto nas duas pontas)
- `DemandaArvoreItemDto.filhos` usa `@ApiProperty({ type: () => DemandaArvoreItemDto, isArray: true })` com lazy getter para suporte a tipos recursivos no Swagger
- A construção da árvore na service usa um `Map<number, DemandaArvoreItemDto>` para O(n) em vez de busca recursiva; o nó raiz é identificado quando `demandaPaiId` é null ou não está no mapa (CTE inicia em demandaId, então seu pai nunca estará no resultado)
- `DemandaRepository.existeConexao` não está listada na spec mas é necessária para implementar a validação de duplicata (#4 de `criarConexao`) com mensagem amigável antes de atingir a constraint do banco
- `listarConexoes` usa `:demandaId` 4 vezes no SQL; Knex com named bindings substitui cada ocorrência pelo valor do objeto — comportamento correto com pg driver
- Arestas do grafo são filtradas com INNER JOIN nas duas pontas (origem e destino no mesmo projeto) — conexões cross-projeto não aparecem no grafo do projeto
- `DemandaService` passa a injetar `TagRepository` e `UsuarioRepository` via `DemandaModule` que importa `TagModule` e `UsuarioModule`; essa dependência entre módulos é necessária para validar existência de tags e usuários antes das operações de atribuição, sem duplicar SQL no repositório de demanda
- `atribuirTagsDemanda` do repositório implementa sync: lista tags atuais, faz soft delete das que saíram, insere apenas as novas — tags que permanecem na lista não são tocadas e não violam a UNIQUE INDEX filtrada por `is_deleted = false`
- `contarMembrosDemanda` não estava na spec mas é necessário para implementar a regra "não remover o último membro" sem trazer toda a lista de membros para a service
- `AtividadeRepository.atualizar` executa dois SELECTs: o UPDATE ... RETURNING não inclui `nome_completo` do usuário (que está em outra tabela), então uma segunda query busca o nome após o UPDATE; alternativa seria um UPDATE com JOIN mas PostgreSQL não suporta JOIN diretamente em UPDATE ... RETURNING sem CTE
- ~~`AtividadeAtualizadaDto` é re-exportado como alias de `AtividadeRecuperadaDto`~~ **(histórico/obsoleto)** — não corresponde ao código atual: o DTO foi renomeado (`Alterar`/`Alterado`, §16 #24) e **nenhum alias por re-export existe hoje** (cada DTO define seus próprios campos, §16 #23)
- `AtividadeModule` importa `DemandaModule` (para `DemandaRepository` verificar existência da demanda) e `TagModule` (para `TagRepository` validar tags antes do sync)
- ~~`ExecucaoAtualizadaDto` é re-exportado como alias de `ExecucaoEncerradaDto`~~ **(histórico/obsoleto)** — não corresponde ao código atual: o DTO foi renomeado (`Alterar`/`Alterado`, §16 #24) e **nenhum alias por re-export existe hoje** (cada DTO define seus próprios campos, §16 #23)
- `ExecucaoRepository.buscarExecucaoAtiva` faz JOIN com atividade para checar `atividade.usuario_id = :usuarioId`, garantindo que apenas execuções do próprio usuário sejam consideradas ativas — sem coluna `usuario_id` na tabela `execucao`
- `ExecucaoRepository.buscarUsuarioExecucao` centraliza a busca do dono da execução via JOIN com atividade, usada nas três operações que exigem verificação de propriedade (encerrar, recuperar, atualizar)
- `duracaoMinutos` é calculado em SQL via `EXTRACT(EPOCH FROM (fim_data - inicio_data))::int / 60` no RETURNING do UPDATE e no SELECT do buscarIdentificador; quando `fim_data IS NULL` retorna NULL
- `ExecucaoService.listar` passa `{ usuarioId: usuarioAtivo.sub }` como `restricao` (DTO `ExecucaoAcessoFiltrarDto`, spec 56 — antes era o primitivo `usuarioIdRestricao`) quando o tipo é DESENVOLVEDOR, sobrescrevendo qualquer `usuarioId` que o desenvolvedor tente passar nos filtros
- `ExecucaoModule` importa apenas `AtividadeModule` — usa `AtividadeRepository.buscarIdentificador` e `usuarioTemAcessoDemanda` para validar existência e acesso na operação de iniciar
- `CalendarioRepository` expõe tanto `ehDiaNaoUtil(data)` (bool, para uso futuro pelo módulo ponto) quanto `buscarTipoPorData(data)` (retorna o tipo, para `verificarDiaUtil` da service que precisa do motivo); rota `GET /calendario/verificar` declarada antes de `GET /calendario/:id` para evitar que "verificar" seja interpretado como ID pelo ParseIntPipe; `verificarDiaUtil` usa `getUTCDay()` (não `getDay()`) para evitar variações de fuso horário ao detectar fim de semana
- `PontoService` cria `filtrosListagem: ExecucaoListarDto` como objeto literal com `itensPorPagina: 500` para buscar todas as execuções do dia (o repositório usa o valor passado, não aplica validação do class-validator); execuções chegam em DESC do repositório e são reordenadas ASC in-memory para o cálculo de intervalos — volume diário não justifica query adicional
- `motivoNaoUtil` usa `buscarTipoPorData` (que já existia desde task 16) mapeado via `mapearTipoParaMotivo` para strings legíveis em português ('Feriado', 'Recesso', 'Ponto facultativo') — sem query nova; fim de semana retorna fixo 'Fim de semana'
- `PontoDiarioDto` importa `ExecucaoResumoDto` por import relativo direto (`../execucao/ExecucaoResumoDto`) em vez do barrel `@project20/shared` para evitar dependência circular dentro do pacote shared
- **[task 20]** `DemandaRecuperarDto` aceita apenas `{ id: number }` — o filtro de acesso por usuário é separado em `DemandaFiltroAcessoDto`, passado como segundo parâmetro opcional em `recuperar(dto, filtro?)`; isso mantém o padrão `recuperar({ id })` uniforme sem carregar o dto com semântica de autorização
- **[task 20]** `DemandaInserirAtribuicaoDto` contém apenas `{ criadorId, gestorIds }` — o `dados` da demanda é mantido como parâmetro separado pois é um tipo backend-only (`DemandaCriarDados`) que não pode ser colocado no shared
- **[task 20]** `DemandaExcluirDto` criado após revisão — `excluir(id: number)` no repositório de demanda era o único método sem DTO; criado para manter consistência com todos os outros módulos
- **[pós-task 20]** Regra de complemento multi-palavra: o complemento inteiro — mesmo com duas palavras — vem antes do verbo sem exceção. Ex: `membro interno` = complemento composto → `DemandaMembroInternoAtribuirDto` ✅, não `DemandaMembroAtribuirInternoDto` ❌. Documentado em `SYSTEM.SPEC.md` e `CONVENTIONS.md`
- **[task 22]** Signal `carregamento` (renomeado de `carregamentoAtivo` que estava no scaffold) — todos os arquivos que referenciam o signal foram atualizados
- **[task 22]** Signal `usuarioAutenticado` usa `UsuarioRecuperadoDto | null` mas o `login()` do service faz `set(resposta.dados.usuario as any)` pois o token retorna apenas `{ id, login, nomeCompleto, tipo }`, não o DTO completo — casting intencional conforme spec
- **[task 22]** `MessageService` do PrimeNG adicionado como provider no `app.config.ts`; `<p-toast>` adicionado ao `LayoutComponent` — necessário para o `errorHandlerInterceptor` exibir mensagens via `MessageService.add()`
- **[task 22]** `TopbarComponent` atualizado para usar `AutenticacaoService.logout()` em vez de `localStorage.removeItem('token')` direto — necessário para consistência com chave `access_token`
- **[task 22]** `AssistenteDescricaoComponent` usa `templateUrl` com arquivo HTML separado criado nesta task; scaffold da task 21 tinha `template: ''` inline
- **[task 23]** `somentePublicoGuard` exportado do mesmo arquivo `autenticacao.guard.ts` — mantém a lógica de guarda pública/privada coesa no mesmo arquivo; aplicado na rota `/autenticacao` em `app.routes.ts`
- **[task 23]** Redirecionamento pós-login vai para `/ponto` (não `/`) — a rota raiz `''` já redireciona para `ponto`, mas ir direto evita uma navegação dupla
- **[task 24]** `UsuarioFormularioPage` é usada apenas para criação (`/usuario/novo`); edição é feita via `p-dialog` dentro da `UsuarioPerfilPage` — as rotas da spec não definem rota de edição separada
- **[task 24]** `ConfirmationService` fornecido localmente via `providers: [ConfirmationService]` na `UsuarioListagemPage` — não está no `app.config.ts` global
- **[task 24]** `UsuarioAnotacoesPage` redireciona para `/usuario/:id` se o usuário da sessão não for o dono — proteção client-side; o backend também bloqueia via autorização
- **[task 24]** `p-select` (PrimeNG 21) em vez de `p-dropdown` (deprecated) para todos os dropdowns; `styleClass` deprecated substituído por `class` nos elementos de seleção
- **[task 24]** Validador customizado `validarSenhasIguais` declarado como método privado e passado ao segundo argumento de `FormBuilder.group()` para validação cross-field sem classe separada
- **[task 25]** `ProjetoListagemPage` usa grid de cards com `p-paginator` (em vez de `p-table` com paginação integrada) — cards são mais adequados para projetos com cor e código visual; `p-table` reservado para dados tabulares como usuários e tags
- **[task 25]** `ProjetoDetalhePage` faz chamada HTTP direta a `/demanda?projetoId=X` via `HttpClient` injetado — o módulo demanda do frontend ainda não existe; evita criar dependência prematura em serviço de outro módulo
- **[task 25]** `p-colorpicker` usa `format="hex"` para garantir retorno no formato `#RRGGBB` compatível com a validação do backend (`Matches(/^#[0-9A-Fa-f]{6}$/)`)
- **[task 25]** Validação cross-field `previsaoFimData > inicioData` implementada como validador privado no grupo do form, padrão idêntico ao `validarSenhasIguais` da task 24
- **[task 25]** `TagListagemPage` usa dialogs para criação e edição (não edição inline em linha de tabela) — mais simples e evita complexidade de estado de edição por linha; spec dizia "Edição inline ou via dialog"
- **[task 25]** Preview de tag no dialog usa `cor + '22'` e `cor + '55'` (opacidade em hex) para simular aspecto de badge colorido sem variáveis CSS adicionais
- **[task 26]** `DemandaGrafoComponent` usa D3 v7 force simulation; nós importados do backend já incluem `demandaPaiId` e `horasEstimadas` no `DemandaGrafoNoDto`, evitando segunda chamada HTTP para construção do grafo e da árvore
- **[task 26]** `DemandaRepository.recuperarGrafo` usa UNION ALL para combinar dois tipos de aresta: `demanda_conexao` (tipo `conexao`) e relações hierárquicas pai→filho via `demanda_pai_id` (tipo `hierarquia`); o campo `tipo` é literal SQL string na mesma query
- **[task 26]** `DemandaProjetoPage.construirArvore()` constrói a árvore client-side a partir do grafo já carregado via `DemandaGrafoNoDto.demandaPaiId` — elimina chamada extra a `recuperarArvore()` na listagem do projeto
- **[task 26]** `DemandaArvoreItemComponent` usa `forwardRef(() => DemandaArvoreItemComponent)` no array `imports` para permitir recursão sem erro de referência circular no Angular
- **[task 26]** `UsuarioListarDto.itensPorPagina` tem `@Max(100)` — a carga de usuários na `DemandaDetalhePage` usa `{ itensPorPagina: 100 }`, não 200
- **[task 26]** `DemandaDetalhePage` usa `forkJoin` para carregar demanda, ancestrais, arvore, tags disponíveis e usuários em paralelo; tags da demanda (`listarTags`) são carregadas separadamente após o forkJoin pois dependem do `demandaId` confirmado
- **[task 27]** `AtividadeResumoDto` não carrega tags; para honrar a coluna "tags (chips)" da listagem, `AtividadeListagemPage` busca as tags por atividade via `listarTags` após o `listar` (N+1 chamadas), armazenadas num `Record<atividadeId, TagResumoDto[]>` — aceitável pois a listagem é escopada a uma única demanda (N pequeno) e evita alterar o contrato do backend
- **[task 27]** `AtividadeFormularioPage` injeta `DemandaService` (do módulo demanda, `providedIn: 'root'`) apenas para exibir o nome da demanda pré-selecionada; o `demandaId` vem por query param e o executor é definido pelo backend
- **[task 27]** Início de execução e listagem de execuções recentes usam `HttpClient` direto para `/execucao` (módulo execução do frontend ainda é esqueleto), seguindo o mesmo padrão da task 25 — evita dependência prematura em serviço inexistente; `ExecucaoIniciarDto` exige `descricao`, então o detalhe abre um dialog para capturá-la antes do POST
- **[task 27]** Dialogs do `AtividadeDetalhePage` usam `[visible]="signal()" (visibleChange)="signal.set($event)"` (não `[(visible)]`) pois o estado é signal — padrão consistente com `ProjetoDetalhePage`
- **[ajuste pós-27]** A listagem de atividades deixou de ser escopada a uma única demanda e passou a ser uma listagem global por usuário (ajuste implementado direto, sem nova spec): (1) `AtividadeListarDto` teve `demandaId` tornado opcional e ganhou `usuarioId?`, `status?: AtividadeStatusEnum[]` (multi), `busca?`, `dataInicio?`/`dataFim?` (ISO date); `AtividadeResumoDto` ganhou `nomeDemanda`, `projetoId`, `nomeProjeto` e `createdDate`; (2) `AtividadeService.listar` (backend) define o controle de acesso por tipo — gestor vê todas (e pode filtrar por um executor via `usuarioId`), desenvolvedor é forçado a `usuarioId = sub` (apenas as próprias); deixou de exigir/validar acesso à demanda na listagem; (3) `AtividadeRepository.listar` reescrito com JOINs `demanda`/`projeto`, filtros dinâmicos por `usuarioId`, `status = ANY(:status)`, busca `ILIKE` em nome de atividade/demanda/projeto, intervalo por `created_date::date`, ordenado por `created_date DESC`; (4) front: filtro de status virou `p-multiSelect` com default "não desenvolvidas" (`ATIVIDADE_STATUS_NAO_DESENVOLVIDA`), `p-select` de executor (somente gestor, carregado via `UsuarioService.listar`), busca textual com `debounceTime(400)`, intervalo de criação via `p-datepicker selectionMode="range"`, colunas Projeto/Demanda/Criada em adicionadas; o `demandaId` por query param continua suportado como filtro opcional (botão "Nova Atividade" só aparece nesse modo escopado)
- **[ajuste pós-27 fase 3]** Refinamentos na tela de atividades, mantendo tudo no módulo de atividade (a **task 28 — frontend-execucao** continua no backlog, intocada): (1) **criação** simplificada — a dialog passou a ter um único `p-select` de demanda (abaixo do Nome) listando as demandas atribuídas ao usuário no formato "Projeto - Demanda"; novo `DemandaAtribuidaDto` + endpoint `GET /demanda/atribuidas` (service/repo `listarAtribuidas`, JOIN `demanda_usuario`+`projeto`); status default `DESENVOLVENDO`; o campo "ordem de exibição" foi removido da UI (era inócuo — a lista ordena por `created_date`/`nome`) e o front envia `ordemExibicao: 0` fixo; (2) **listagem** reordenada para Projeto/Demanda/Atividade, coluna "Usuário" só para gestor, coluna "Criada em" removida; (3) **visualizar** deixou de navegar para `/atividade/:id` e virou dialog com `p-tabs` (Descrição / Últimas execuções), cabeçalho com usuário, **total de atividades do usuário** (`AtividadeRecuperadaDto.totalAtividadesUsuario`, subconsulta `COUNT` no `recuperar`), status em chip (`p-tag`) e tags; (4) **execução play/pause** por linha (só nas atividades do próprio usuário): novo `ExecucaoAtivaDto` + endpoint `GET /execucao/ativa` (`recuperarAtiva` agora retorna `descricao`); play abre dialog pedindo descrição + histórico de execuções (scroll, sem limite) + `AssistenteDescricaoComponent`; com execução ativa o botão vira pause e reabre a dialog com a descrição preenchida para revisão, encerrando o segmento (`PATCH /execucao/:id/encerrar` — modelo iniciar/encerrar; sem pausa retomável). As chamadas HTTP de execução foram adicionadas ao `AtividadeService` (frontend) para não criar o `modules/execucao/services` (reservado à task 28)
- **[task 28]** `ExecucaoService.buscarAtiva()` retorna `StandardResponse<ExecucaoAtivaDto | null>` (não `ExecucaoIniciadaDto | null` como sugeria a spec) — o endpoint `GET /execucao/ativa` já existia desde os ajustes pós-27 e retorna `ExecucaoAtivaDto` (`id`, `atividadeId`, `descricao`, `inicioData`); seguiu-se o contrato real do backend em vez do exemplo da spec
- **[task 28]** `ExecucaoService.recuperar` e `alterar` tipam o retorno como `ExecucaoEncerradaDto`/`ExecucaoAlteradaDto` respectivamente, espelhando os tipos reais dos métodos do `ExecucaoService` do backend (não há `ExecucaoRecuperadaDto` no shared)
- **[task 28]** O `execucao.model.ts` recebeu helpers puros (`segundosDecorridos`, `formatarRelogio`) reaproveitados pelo `ExecucaoTimerComponent`, mantendo o componente enxuto — segue o padrão de co-localizar helpers no `*.model.ts` do módulo (como `atividade.model.ts`)
- **[task 28]** Os templates do módulo execução usam apenas utilitários Tailwind + classes BEM em `.scss` próprio das páginas (sem `.scss` para o timer, que usa só utilitários Tailwind) — respeita a lista de "Arquivos a Criar" da spec (que não lista `.scss`) sem violar a proibição de `style=""` inline nem `.css`
- **[task 28]** `ExecucaoAtivaPage` exibe nome/links de atividade e demanda da execução ativa encadeando `AtividadeService.recuperar(atividadeId)` → `DemandaService.recuperar(demandaId)`, pois `ExecucaoAtivaDto` carrega apenas `atividadeId`; necessário para reconstruir o contexto após recarregar a página com execução já em andamento — **NOTA: `ExecucaoAtivaPage` e a rota `/execucao/ativa` foram removidas no ajuste pós-68 (ver topo); o fluxo iniciar/encerrar vive na tela de Atividades. `ExecucaoAtivaDto`/service/controller permanecem.**
- **[task 28]** O autocomplete de atividades (início de execução e filtro do histórico) usa `AtividadeService.listar({ busca })` — o backend já força `usuarioId = sub` para desenvolvedor, satisfazendo "filtra por demandas do usuário" sem lógica adicional no frontend
- **[task 28]** A edição de descrição no histórico é restrita a gestor no frontend (`abrirEdicao` retorna cedo se `!eGestor`, linha só clicável via classe condicional) — alinhado à regra "Editar execuções de outros: só gestor"; o backend também valida a permissão
- **[task 26]** `subDemandas` na `DemandaDetalhePage` é extraído de `arvore.dados[0]?.filhos` — `recuperarArvore` retorna árvore com a própria demanda como raiz (nivel=0) e seus filhos diretos dentro do nó raiz
- **[task 29 — redesenho]** A tela de ponto foi redesenhada (após alinhamento) em **dois modos** que substituem a visão de um único dia: **(a) "todos hoje"** — gestor sem filtro de usuário vê o grid de todos os ativos, fixo em hoje, sem seletor de data; **(b) "mensal"** — gestor com usuário filtrado, ou desenvolvedor sempre (próprio), vê lista de dias do mês com totais/saldo no topo e seletor de mês/ano (`maxDate`=mês atual). A spec foi reescrita para refletir isso (a original dizia "não implementar resumo mensal"). Desenvolvedor nunca acessa a visão "todos"
- **[task 29 — redesenho]** Saldo do mês conforme decisão do usuário: `metaMinutosMes = horasDiariasNecessarias × diasUteisMes`, contando os dias úteis do **mês inteiro** (não só os decorridos) — `diaUtil` = dia de semana que **não** está em `dia_nao_util` (feriado/recesso/ponto facultativo, exato do ano/mês ou recorrente do mês). `saldoMinutosMes = totalMinutosTrabalhadosMes − metaMinutosMes`
- **[task 29 — redesenho]** `consultarTodos` reaproveita o núcleo de `consultarDiario` extraído em `montarPontoDiario`; `resolverInfoDia(hoje)` é chamado **uma vez** e reutilizado para todos os usuários (a utilidade da data independe do usuário) — para um time pequeno (5–10) o custo de ~1 query de execuções por usuário é aceitável, sem otimização prematura
- **[task 29 — redesenho]** No mensal, o detalhe de um dia (timeline + intervalos) é carregado **sob demanda** ao expandir, reusando `GET /ponto/diario` — mantém a query mensal enxuta (1 agregação de execuções + 1 de dias não úteis), e só dias com registro são expansíveis
- **[task 29 — redesenho]** `PontoMensalConsultarDto` usa `@IsNumber` (não `@IsInt`): o `class-validator` resolvido pelo **frontend** não exporta `IsInt` (embora o backend exporte) — `@IsNumber` está disponível nos dois e segue o padrão do `PontoConsultarDto`
- **[task 29 — redesenho]** Helpers puros em `ponto.model.ts` (frontend): `formatarSaldoMinutos` (sinal explícito `+/-` com valor absoluto, evitando `-2h -30min` do pipe), `abreviacaoDiaSemana`/`numeroDoDia` (via `getUTCDay`/`getUTCDate` sobre a data ISO, sem depender de locale registrado no Angular)
- **[task 29 — redesenho]** `intervaloMinimoMinutos: 15` em `environment.ts`/`environment.production.ts` (usado por `PontoIntervaloListaComponent` e `PontoMesDiaComponent`) espelha `INTERVALO_MINIMO_MINUTOS` do `.env` do backend — `PontoDiarioDto` não carrega o mínimo e o frontend não acessa `ConfigService`; manter em sincronia manual
- **[task 29 — redesenho]** Atualização automática ao voltar à aba via `@HostListener('document:visibilitychange')` na `PontoPage` (mesmo padrão da `ExecucaoHistoricoPage`), recarregando o modo atual
- **[task 30]** Task foi exclusivamente frontend — o backend do calendário (controller/service/repository) já existia desde a task 16; nenhum arquivo backend ou shared foi alterado
- **[task 30]** O retorno de `GET /calendario/verificar` (`{ data, ehDiaUtil, motivo }`) não tem DTO no shared; modelado como interface local `VerificacaoDiaUtil` em `models/dia-nao-util.model.ts` — é mapeamento de resposta de endpoint, não DTO de negócio (a proibição de redefinir DTO/enum no frontend não se aplica)
- **[task 30]** Na edição (`DiaNaoUtilAlterarDto`) o backend não expõe `diaData`; o campo de data é exibido mas desabilitado no dialog de edição (`formulario.get('diaData')!.disable()`), evitando enviar um campo que o backend ignoraria — só descrição/tipo/recorrente são alteráveis
- **[task 30]** Datas tratadas com helpers puros para evitar deslocamento de fuso: `formatarDataIso` usa componentes locais do `Date` (não `toISOString`) ao montar `YYYY-MM-DD`, e `partesDaData` interpreta strings date-only como meio-dia local — mesma estratégia já adotada no `data-brasileira.pipe`
- **[task 30]** Marcação do calendário (`p-datepicker [inline]` + `pTemplate="date"`) usa dois `Map` computados a partir da lista: `mapaExatos` chaveado por `ano-mes-dia` (não recorrentes) e `mapaRecorrentes` por `mes-dia` (recorrentes, válidos em qualquer ano) — espelha a lógica de recorrência do backend (`EXTRACT(MONTH/DAY)`) sem chamada adicional; clique num dia marcado preenche um painel de detalhe lateral (em vez de tooltip-only)
- **[task 30]** Toggle tabela/calendário implementado com dois `p-button` + signal `visualizacao` (padrão da `DemandaProjetoPage`), evitando `[(ngModel)]` de `p-selectButton` — coerente com a regra de não usar template-driven forms
- **[pós-auditoria 44]** **DTO em tudo (reconciliação §7.2 × §16 #21):** decidido padronizar todo boundary para DTO único. Service e repository **nunca** recebem `id`/`*Id` primitivo; a controller é o único ponto autorizado a montá-lo — ao extrair `@Param('id')`/`@Query`, injeta o id no DTO (`service.recuperar({ id })`, `service.alterar({ ...dto, id })`). Essa "microinteligência" não é exceção ao §16 #21 — é onde o DTO nasce. Objetos de contexto (`usuarioAtivo: JwtPayload` via `@ActiveUser()`) permanecem como parâmetro próprio por não serem primitivos. O carve-out para primitivo de restrição de escopo (`listar(filtros, usuarioId?)`) foi **descartado** — vira DTO (`DemandaAcessoFiltrarDto`/`ExecucaoAcessoFiltrarDto`). Documentado em `SYSTEM.SPEC.md` §7.2/§7.4/§16 #21 e `CONVENTIONS.md`; aplicado no código pelas specs 48–56
- **[pós-auditoria 44]** **DTOs de relatório e value-objects (lacuna do §5.1):** relatórios/consultas computadas usam `Entidade + Recorte + Dto` **sem verbo** (`PontoDiarioDto`, `PontoMensalDto`); value-objects sem ciclo de vida usam o nome do conceito sem entidade nem verbo (`IntervaloDto`). Documentado em `SYSTEM.SPEC.md` §5.1 e `CONVENTIONS.md`
- **[pós-auditoria 44]** **Função de trigger renomeada:** `fn_atualizar_updated_date` → `fn_set_updated_date` (mecanismo genérico de BaseEntity → inglês, §16 #12). `SCHEMA.md` já atualizado; a migration de renomeação é a spec 55
- **[pós-auditoria 44]** **Erratas de documentação corrigidas:** `SYSTEM.SPEC.md` §13 (`createdAt`/`updatedAt`/`deletedAt` → `createdDate`/`updatedDate`/`deletedDate`) e §14 (`finalizado_em` → `fim_data`); `CONVENTIONS.md` (coluna BaseEntity `created_at`/`updated_at` → `created_date`/`updated_date`/`deleted_date`)

---

## Problemas Conhecidos

Violações de padrão identificadas na revisão pós-task 18. Corrigidas pelas tasks 19 e 20:

*Nenhum problema conhecido no momento.*

---

## Notas para a Próxima Sessão

- Ler `SYSTEM.SPEC.md` e `CONVENTIONS.md` antes de iniciar
- Verificar a seção "Próxima Task" acima
- Mover a spec de `backlog/` para `active/` antes de implementar
- Após concluir, mover spec de `active/` para `done/` e atualizar este arquivo

---

## Como Atualizar Este Arquivo

Ao finalizar uma sessão, o Claude Code deve:

1. Mover a task de `active/` para `done/`
2. Adicionar o item em **Implementado** com breve descrição
3. Atualizar o status do módulo correspondente (⬜ → ✅)
4. Atualizar **Próxima Task** com a task seguinte do backlog
5. Registrar em **Decisões Tomadas** qualquer decisão relevante feita durante a implementação
6. Registrar em **Problemas Conhecidos** qualquer dívida técnica ou bug identificado
7. Atualizar **Última Atualização** com data e nome da task concluída
